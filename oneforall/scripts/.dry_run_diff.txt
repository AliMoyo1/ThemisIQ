--- a/core/auth.py
+++ b/core/auth.py
@@ -49,9 +49,9 @@
         db.execute(
             "INSERT INTO sessions (token, user_id, ip_address, user_agent, expires_at) "
-            "VALUES (?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s)",
             (token_hash, user_id, ip, user_agent[:500] if user_agent else "", expires),
         )
         db.execute(
-            "UPDATE users SET last_login = ? WHERE id = ?",
+            "UPDATE users SET last_login = %s WHERE id = %s",
             (utcnow().isoformat(), user_id),
         )
@@ -70,5 +70,5 @@
     try:
         row = db.execute(
-            "SELECT s.user_id, s.expires_at FROM sessions s WHERE s.token = ?",
+            "SELECT s.user_id, s.expires_at FROM sessions s WHERE s.token = %s",
             (token_hash,),
         ).fetchone()
@@ -76,5 +76,5 @@
             return None
         if datetime.fromisoformat(row["expires_at"]) < utcnow():
-            db.execute("DELETE FROM sessions WHERE token = ?", (token_hash,))
+            db.execute("DELETE FROM sessions WHERE token = %s", (token_hash,))
             db.commit()
             return None
@@ -82,5 +82,5 @@
         user = db.execute(
             "SELECT id, username, email, full_name, is_active, must_change_password, "
-            "avatar_initials FROM users WHERE id = ?",
+            "avatar_initials FROM users WHERE id = %s",
             (row["user_id"],),
         ).fetchone()
@@ -91,5 +91,5 @@
             r["role_key"]
             for r in db.execute(
-                "SELECT role_key FROM user_roles WHERE user_id = ?",
+                "SELECT role_key FROM user_roles WHERE user_id = %s",
                 (user["id"],),
             ).fetchall()
@@ -116,5 +116,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM sessions WHERE token = ?", (token_hash,))
+        db.execute("DELETE FROM sessions WHERE token = %s", (token_hash,))
         db.commit()
     finally:
@@ -126,5 +126,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM sessions WHERE expires_at < ?", (utcnow().isoformat(),))
+        db.execute("DELETE FROM sessions WHERE expires_at < %s", (utcnow().isoformat(),))
         db.commit()
     finally:
@@ -140,5 +140,5 @@
         user = db.execute(
             "SELECT id, username, email, full_name, password_hash, is_active, "
-            "must_change_password, avatar_initials FROM users WHERE username = ?",
+            "must_change_password, avatar_initials FROM users WHERE username = %s",
             (username,),
         ).fetchone()
@@ -155,5 +155,5 @@
             r["role_key"]
             for r in db.execute(
-                "SELECT role_key FROM user_roles WHERE user_id = ?",
+                "SELECT role_key FROM user_roles WHERE user_id = %s",
                 (user["id"],),
             ).fetchall()
[WARN] core/auto_mapper.py:
  line 253: f-string SQL with ? — manual review needed
--- a/core/auto_mapper.py
+++ b/core/auto_mapper.py
@@ -440,5 +440,5 @@
                     "(source_framework_id, source_control_id, target_framework_id, target_control_id, "
                     " mapping_type, confidence, notes, auto_generated, match_method, created_by) "
-                    "VALUES (?,?,?,?,?,?,?,?,?,?) ON CONFLICT DO NOTHING",
+                    "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                     (m["source_framework_id"], m["source_control_id"],
                      m["target_framework_id"], m["target_control_id"],
@@ -452,5 +452,5 @@
                     "(source_framework_id, source_control_id, target_framework_id, target_control_id, "
                     " mapping_type, confidence, notes, created_by) "
-                    "VALUES (?,?,?,?,?,?,?,?) ON CONFLICT DO NOTHING",
+                    "VALUES (%s,%s,%s,%s,%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                     (m["source_framework_id"], m["source_control_id"],
                      m["target_framework_id"], m["target_control_id"],
--- a/core/email.py
+++ b/core/email.py
@@ -53,5 +53,5 @@
         db = get_db()
         try:
-            row = db.execute("SELECT value FROM settings WHERE key=?", (key,)).fetchone()
+            row = db.execute("SELECT value FROM settings WHERE key=%s", (key,)).fetchone()
             return row["value"] if row else default
         finally:
--- a/core/event_handlers.py
+++ b/core/event_handlers.py
@@ -37,5 +37,5 @@
         db.execute(
             "INSERT INTO notifications (user_id, module, title, message, link) "
-            "VALUES (?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s)",
             (user_id, module, title, message, link),
         )
@@ -73,5 +73,5 @@
             "source_entity_type, source_entity_id, category, likelihood, impact, "
             "risk_level, status, created_by) "
-            "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)",
             (title, description, source_module, entity_type, entity_id,
              category, likelihood, impact, risk_level, "open", user_id),
@@ -90,5 +90,5 @@
             "INSERT INTO task_board (title, description, module, entity_type, "
             "entity_id, priority, status, created_by) "
-            "VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s, %s, %s, %s)",
             (title, description, module, entity_type, entity_id,
              priority, "todo", user_id),
@@ -112,5 +112,5 @@
             "INSERT INTO evidence_items "
             "(title, description, category, tags, status, uploaded_by) "
-            "VALUES (?, ?, ?, ?, 'current', ?)",
+            "VALUES (%s, %s, %s, %s, 'current', %s)",
             (title, description, category, tags, user_id),
         )
@@ -120,5 +120,5 @@
             "INSERT INTO evidence_links "
             "(evidence_id, module, entity_type, entity_id, linked_by) "
-            "VALUES (?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s)",
             (ev_id, module, entity_type, entity_id, user_id),
         )
@@ -142,5 +142,5 @@
         row = db.execute(
             "SELECT MAX(likelihood * impact) AS max_score FROM erm_enterprise_risks "
-            "WHERE category=? AND status NOT IN ('closed','accepted')",
+            "WHERE category=%s AND status NOT IN ('closed','accepted')",
             (category,)
         ).fetchone()
@@ -148,5 +148,5 @@
 
         appetite = db.execute(
-            "SELECT max_score FROM erm_risk_appetite WHERE category=?", (category,)
+            "SELECT max_score FROM erm_risk_appetite WHERE category=%s", (category,)
         ).fetchone()
         if not appetite:
@@ -211,9 +211,9 @@
             return
         existing = db.execute(
-            "SELECT id FROM aria_frameworks WHERE name = ?", (name,)
+            "SELECT id FROM aria_frameworks WHERE name = %s", (name,)
         ).fetchone()
         if not existing:
             fw = db.execute(
-                "SELECT description, color FROM frameworks WHERE id = ?",
+                "SELECT description, color FROM frameworks WHERE id = %s",
                 (entity_id,)
             ).fetchone()
@@ -221,5 +221,5 @@
                 db.execute(
                     "INSERT INTO aria_frameworks (name, description, color, relevant_modules, is_active) "
-                    "VALUES (?, ?, ?, ?, 1)",
+                    "VALUES (%s, %s, %s, %s, 1)",
                     (name, fw[0], fw[1], modules),
                 )
@@ -228,5 +228,5 @@
         else:
             db.execute(
-                "UPDATE aria_frameworks SET is_active = 1 WHERE name = ?", (name,)
+                "UPDATE aria_frameworks SET is_active = 1 WHERE name = %s", (name,)
             )
             db.commit()
@@ -246,9 +246,9 @@
             return
         existing = db.execute(
-            "SELECT id FROM grid_frameworks WHERE name = ?", (name,)
+            "SELECT id FROM grid_frameworks WHERE name = %s", (name,)
         ).fetchone()
         if not existing:
             fw = db.execute(
-                "SELECT description, color FROM frameworks WHERE id = ?",
+                "SELECT description, color FROM frameworks WHERE id = %s",
                 (entity_id,)
             ).fetchone()
@@ -256,5 +256,5 @@
                 db.execute(
                     "INSERT INTO grid_frameworks (name, description, color, active) "
-                    "VALUES (?, ?, ?, 1)",
+                    "VALUES (%s, %s, %s, 1)",
                     (name, fw[0], fw[1]),
                 )
@@ -263,5 +263,5 @@
         else:
             db.execute(
-                "UPDATE grid_frameworks SET active = 1 WHERE name = ?", (name,)
+                "UPDATE grid_frameworks SET active = 1 WHERE name = %s", (name,)
             )
             db.commit()
@@ -277,5 +277,5 @@
     try:
         name = payload.get("name", "")
-        db.execute("UPDATE aria_frameworks SET is_active = 0 WHERE name = ?", (name,))
+        db.execute("UPDATE aria_frameworks SET is_active = 0 WHERE name = %s", (name,))
         db.commit()
     finally:
@@ -290,5 +290,5 @@
     try:
         name = payload.get("name", "")
-        db.execute("UPDATE grid_frameworks SET active = 0 WHERE name = ?", (name,))
+        db.execute("UPDATE grid_frameworks SET active = 0 WHERE name = %s", (name,))
         db.commit()
     finally:
@@ -312,5 +312,5 @@
         ctrl_ref = payload.get("ref", "")
         existing = db.execute(
-            "SELECT id FROM risk_register WHERE title LIKE ? AND status != 'closed'",
+            "SELECT id FROM risk_register WHERE title LIKE %s AND status != 'closed'",
             (f"%{ctrl_ref}%",)
         ).fetchone()
@@ -378,5 +378,5 @@
             # Find GRID controls with the same ref for cross-linking
             ctrl_row = db.execute(
-                "SELECT id FROM grid_controls WHERE control_id = ? LIMIT 1",
+                "SELECT id FROM grid_controls WHERE control_id = %s LIMIT 1",
                 (control_ref,)
             ).fetchone()
@@ -392,5 +392,5 @@
         doc_row = db.execute(
             "SELECT doc_id, title, doc_type, version, location, body "
-            "FROM aria_documents WHERE id=?",
+            "FROM aria_documents WHERE id=%s",
             (entity_id,),
         ).fetchone()
@@ -411,5 +411,5 @@
                 "(title, description, file_path, file_name, file_size, "
                 " mime_type, category, tags, status, uploaded_by) "
-                "VALUES (?,?,?,?,?,?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
                 (
                     title,
@@ -433,5 +433,5 @@
                 "INSERT INTO evidence_links "
                 "(evidence_id, module, entity_type, entity_id, linked_by) "
-                "VALUES (?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s)",
                 (vault_id, "aria", "document", entity_id, user_id),
             )
@@ -439,5 +439,5 @@
             if framework:
                 fw_row = db.execute(
-                    "SELECT id FROM aria_frameworks WHERE name=?", (framework,)
+                    "SELECT id FROM aria_frameworks WHERE name=%s", (framework,)
                 ).fetchone()
                 if fw_row:
@@ -445,5 +445,5 @@
                         "INSERT INTO evidence_links "
                         "(evidence_id, module, entity_type, entity_id, linked_by) "
-                        "VALUES (?,?,?,?,?)",
+                        "VALUES (%s,%s,%s,%s,%s)",
                         (vault_id, "aria", "framework", fw_row[0], user_id),
                     )
@@ -651,5 +651,5 @@
         if ref:
             grid_ctrl = db.execute(
-                "SELECT id FROM grid_controls WHERE control_id = ? LIMIT 1",
+                "SELECT id FROM grid_controls WHERE control_id = %s LIMIT 1",
                 (ref,)
             ).fetchone()
@@ -939,5 +939,5 @@
             "FROM grid_policy_requests pr "
             "LEFT JOIN grid_audits a ON pr.audit_id=a.id "
-            "WHERE pr.status='pending' AND pr.framework_name=?"
+            "WHERE pr.status='pending' AND pr.framework_name=%s"
         )
         params = [framework]
@@ -951,6 +951,6 @@
             db.execute(
                 "UPDATE grid_policy_requests "
-                "SET status='fulfilled', aria_document_id=?, resolved_at=datetime('now') "
-                "WHERE id=?",
+                "SET status='fulfilled', aria_document_id=%s, resolved_at=datetime('now') "
+                "WHERE id=%s",
                 (entity_id, m["id"]),
             )
@@ -1075,5 +1075,5 @@
         for term in terms[:8]:  # cap to avoid over-broad matching
             rows = db.execute(
-                "SELECT id, name FROM bcm_dependency_nodes WHERE name LIKE ? OR description LIKE ?",
+                "SELECT id, name FROM bcm_dependency_nodes WHERE name LIKE %s OR description LIKE %s",
                 (f"%{term}%", f"%{term}%"),
             ).fetchall()
@@ -1110,5 +1110,5 @@
                     visited.add(nid)
                     rows = db.execute(
-                        "SELECT target_id FROM bcm_dependency_edges WHERE source_id=?", (nid,)
+                        "SELECT target_id FROM bcm_dependency_edges WHERE source_id=%s", (nid,)
                     ).fetchall()
                     for r in rows:
@@ -1116,5 +1116,5 @@
                         if tid not in visited:
                             node = db.execute(
-                                "SELECT id, name, node_type, criticality FROM bcm_dependency_nodes WHERE id=?",
+                                "SELECT id, name, node_type, criticality FROM bcm_dependency_nodes WHERE id=%s",
                                 (tid,)
                             ).fetchone()
@@ -1128,5 +1128,5 @@
         for nid in matched_node_ids:
             root_node = db.execute(
-                "SELECT id, name, node_type, criticality FROM bcm_dependency_nodes WHERE id=?", (nid,)
+                "SELECT id, name, node_type, criticality FROM bcm_dependency_nodes WHERE id=%s", (nid,)
             ).fetchone()
             if root_node:
@@ -1201,5 +1201,5 @@
             "UPDATE task_board SET status = 'done' "
             "WHERE module = 'bcm' AND entity_type = 'incident' "
-            "AND entity_id = ? AND status != 'done'",
+            "AND entity_id = %s AND status != 'done'",
             (entity_id,),
         )
@@ -1242,5 +1242,5 @@
             "SELECT target_entity_id FROM cross_module_links "
             "WHERE source_module='bcm' AND source_entity_type='incident' "
-            "AND source_entity_id=? AND target_module='orm' AND target_entity_type='event'",
+            "AND source_entity_id=%s AND target_module='orm' AND target_entity_type='event'",
             (entity_id,),
         ).fetchall()
@@ -1248,5 +1248,5 @@
             db.execute(
                 "UPDATE orm_events SET status='resolved', resolved_at=datetime('now'), "
-                "updated_at=datetime('now') WHERE id=? AND status NOT IN ('resolved','closed')",
+                "updated_at=datetime('now') WHERE id=%s AND status NOT IN ('resolved','closed')",
                 (link["target_entity_id"],),
             )
@@ -1548,5 +1548,5 @@
                     "(regulator, regulation_name, obligation, applicable_departments, "
                     " evidence_required, due_date, status, notes, created_by, created_at, updated_at) "
-                    "VALUES (?,?,?,?,?,?,?,?,?,datetime('now'),datetime('now'))",
+                    "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,datetime('now'),datetime('now'))",
                     (
                         regulator,
@@ -1681,5 +1681,5 @@
             "INSERT INTO bcm_incidents "
             "(title, description, severity, status, started_at, created_at, updated_at) "
-            "VALUES (?,?,?,?,datetime('now'),datetime('now'),datetime('now'))",
+            "VALUES (%s,%s,%s,%s,datetime('now'),datetime('now'),datetime('now'))",
             (
                 f"DATA BREACH: {title}",
@@ -1853,5 +1853,5 @@
             rows = db.execute(
                 "SELECT id FROM aria_controls "
-                "WHERE LOWER(name) LIKE ? OR LOWER(description) LIKE ? "
+                "WHERE LOWER(name) LIKE %s OR LOWER(description) LIKE %s "
                 "LIMIT 3",
                 (f"%{kw}%", f"%{kw}%"),
@@ -1884,5 +1884,5 @@
         if source_risk_id:
             exists = db.execute(
-                "SELECT id FROM erm_enterprise_risks WHERE source_module=? AND source_risk_id=?",
+                "SELECT id FROM erm_enterprise_risks WHERE source_module=%s AND source_risk_id=%s",
                 (source_module, source_risk_id)
             ).fetchone()
@@ -1893,5 +1893,5 @@
                (title, description, category, likelihood, impact, velocity,
                 treatment, status, board_visibility, source_module, source_risk_id, created_by)
-               VALUES (?,?,?,?,?,?,?,?,?,?,?,?)""",
+               VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
             (title, description, category, likelihood, impact, 3,
              "mitigate", "open", board_visibility, source_module, source_risk_id, user_id)
@@ -2059,5 +2059,5 @@
         if sev == "critical":
             db.execute(
-                "UPDATE erm_enterprise_risks SET board_visibility=1 WHERE id=?",
+                "UPDATE erm_enterprise_risks SET board_visibility=1 WHERE id=%s",
                 (erm_risk_id,)
             )
@@ -2094,5 +2094,5 @@
         # Check if a post-incident audit already exists for this breach
         existing = db.execute(
-            "SELECT id FROM grid_audits WHERE name=?", (audit_name,)
+            "SELECT id FROM grid_audits WHERE name=%s", (audit_name,)
         ).fetchone()
         if existing:
@@ -2102,5 +2102,5 @@
             "INSERT INTO grid_audits "
             "(name, audit_type, status, scope, objective, criteria, created_at) "
-            "VALUES (?,?,?,?,?,?,datetime('now'))",
+            "VALUES (%s,%s,%s,%s,%s,%s,datetime('now'))",
             (
                 audit_name,
@@ -2176,5 +2176,5 @@
             "(title, description, event_type, severity, status, department, "
             " root_cause_category, detected_at, created_at, updated_at) "
-            "VALUES (?,?,?,?,?,?,?,datetime('now'),datetime('now'),datetime('now'))",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,datetime('now'),datetime('now'),datetime('now'))",
             (
                 f"[{regulation}] Data Breach: {title}",
@@ -2231,5 +2231,5 @@
             "(title, description, event_type, severity, status, department, "
             " root_cause_category, detected_at, created_at, updated_at) "
-            "VALUES (?,?,?,?,?,?,?,datetime('now'),datetime('now'),datetime('now'))",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,datetime('now'),datetime('now'),datetime('now'))",
             (
                 f"Overdue DSR: {request_type} request (#{entity_id})",
@@ -2286,5 +2286,5 @@
             matching_kris = db.execute(
                 "SELECT id, current_value, name FROM orm_kris "
-                "WHERE auto_update_event_type=? AND status='active'",
+                "WHERE auto_update_event_type=%s AND status='active'",
                 (event_type_val,),
             ).fetchall()
@@ -2292,10 +2292,10 @@
                 new_val = (kri["current_value"] or 0) + 1
                 db.execute(
-                    "UPDATE orm_kris SET current_value=?, trend='worsening', "
-                    "last_updated=datetime('now'), updated_at=datetime('now') WHERE id=?",
+                    "UPDATE orm_kris SET current_value=%s, trend='worsening', "
+                    "last_updated=datetime('now'), updated_at=datetime('now') WHERE id=%s",
                     (new_val, kri["id"]),
                 )
                 db.execute(
-                    "INSERT INTO orm_kri_history (kri_id, value, recorded_at) VALUES (?,?,datetime('now'))",
+                    "INSERT INTO orm_kri_history (kri_id, value, recorded_at) VALUES (%s,%s,datetime('now'))",
                     (kri["id"], new_val),
                 )
@@ -2438,5 +2438,5 @@
         erm_rows = db.execute(
             "SELECT id, title, category, owner_id FROM erm_enterprise_risks "
-            "WHERE source_module='sentinel' AND source_risk_id=? "
+            "WHERE source_module='sentinel' AND source_risk_id=%s "
             "AND status NOT IN ('closed','accepted')",
             (entity_id,),
@@ -2447,5 +2447,5 @@
                 "UPDATE erm_enterprise_risks "
                 "SET status='closed', workflow_step='closed', updated_at=datetime('now') "
-                "WHERE id=?",
+                "WHERE id=%s",
                 (erm["id"],),
             )
@@ -2472,5 +2472,5 @@
             "UPDATE risk_register SET status='closed', updated_at=datetime('now') "
             "WHERE source_module='sentinel' AND source_entity_type='breach' "
-            "AND source_entity_id=? AND status != 'closed'",
+            "AND source_entity_id=%s AND status != 'closed'",
             (entity_id,),
         )
@@ -2481,5 +2481,5 @@
             "SELECT target_entity_id FROM cross_module_links "
             "WHERE source_module='sentinel' AND source_entity_type='breach' "
-            "AND source_entity_id=? AND target_module='bcm' AND target_entity_type='incident'",
+            "AND source_entity_id=%s AND target_module='bcm' AND target_entity_type='incident'",
             (entity_id,),
         ).fetchall()
@@ -2491,5 +2491,5 @@
                 "UPDATE bcm_incidents "
                 "SET status='resolved', resolved_at=datetime('now') "
-                "WHERE id=? AND status NOT IN ('closed','resolved')",
+                "WHERE id=%s AND status NOT IN ('closed','resolved')",
                 (inc_id,),
             )
@@ -2503,5 +2503,5 @@
             "SELECT target_entity_id FROM cross_module_links "
             "WHERE source_module='sentinel' AND source_entity_type='breach' "
-            "AND source_entity_id=? AND target_module='orm' AND target_entity_type='event'",
+            "AND source_entity_id=%s AND target_module='orm' AND target_entity_type='event'",
             (entity_id,),
         ).fetchall()
@@ -2510,5 +2510,5 @@
             db.execute(
                 "UPDATE orm_events SET status='resolved', resolved_at=datetime('now'), "
-                "updated_at=datetime('now') WHERE id=? AND status NOT IN ('resolved','closed')",
+                "updated_at=datetime('now') WHERE id=%s AND status NOT IN ('resolved','closed')",
                 (orm_id,),
             )
@@ -2580,5 +2580,5 @@
 
         appetite = db.execute(
-            "SELECT max_score FROM erm_risk_appetite WHERE category=?",
+            "SELECT max_score FROM erm_risk_appetite WHERE category=%s",
             (category,),
         ).fetchone()
@@ -2589,5 +2589,5 @@
         current_max = db.execute(
             "SELECT MAX(likelihood*impact) FROM erm_enterprise_risks "
-            "WHERE category=? AND status NOT IN ('closed','accepted')",
+            "WHERE category=%s AND status NOT IN ('closed','accepted')",
             (category,),
         ).fetchone()[0] or 0
@@ -2612,5 +2612,5 @@
             "WHERE source_module='bcm' AND source_entity_type='incident' "
             "AND target_module='erm' AND target_entity_type='enterprise_risk' "
-            "AND target_entity_id=?",
+            "AND target_entity_id=%s",
             (entity_id,),
         ).fetchall()
@@ -2618,5 +2618,5 @@
             db.execute(
                 "UPDATE bcm_incidents SET status='resolved', resolved_at=datetime('now') "
-                "WHERE id=? AND status NOT IN ('resolved','closed')",
+                "WHERE id=%s AND status NOT IN ('resolved','closed')",
                 (link["source_entity_id"],),
             )
@@ -2627,5 +2627,5 @@
         db.execute(
             "UPDATE orm_events SET status='resolved', resolved_at=datetime('now'), "
-            "updated_at=datetime('now') WHERE erm_risk_id=? "
+            "updated_at=datetime('now') WHERE erm_risk_id=%s "
             "AND status NOT IN ('resolved','closed')",
             (entity_id,),
@@ -2635,5 +2635,5 @@
             "WHERE source_module='orm' AND source_entity_type='event' "
             "AND target_module='erm' AND target_entity_type='enterprise_risk' "
-            "AND target_entity_id=?",
+            "AND target_entity_id=%s",
             (entity_id,),
         ).fetchall()
@@ -2641,5 +2641,5 @@
             db.execute(
                 "UPDATE orm_events SET status='resolved', resolved_at=datetime('now'), "
-                "updated_at=datetime('now') WHERE id=? AND status NOT IN ('resolved','closed')",
+                "updated_at=datetime('now') WHERE id=%s AND status NOT IN ('resolved','closed')",
                 (link["source_entity_id"],),
             )
@@ -2716,5 +2716,5 @@
         rows = db.execute(
             "SELECT DISTINCT owner_id FROM erm_enterprise_risks "
-            "WHERE category=? AND status NOT IN ('closed','accepted') AND owner_id IS NOT NULL",
+            "WHERE category=%s AND status NOT IN ('closed','accepted') AND owner_id IS NOT NULL",
             (category,)
         ).fetchall()
--- a/core/events.py
+++ b/core/events.py
@@ -38,5 +38,5 @@
         event_id = insert_returning_id(db,
             "INSERT INTO events (event_type, source_module, source_entity_type, "
-            "source_entity_id, payload, created_by) VALUES (?, ?, ?, ?, ?, ?)",
+            "source_entity_id, payload, created_by) VALUES (%s, %s, %s, %s, %s, %s)",
             (
                 event_type,
@@ -70,5 +70,5 @@
     try:
         db.execute(
-            "UPDATE events SET status='processed', processed_at=? WHERE id=?",
+            "UPDATE events SET status='processed', processed_at=%s WHERE id=%s",
             (utcnow().isoformat(), event_id),
         )
@@ -84,5 +84,5 @@
     try:
         db.execute(
-            "UPDATE events SET status='failed', processed_at=? WHERE id=?",
+            "UPDATE events SET status='failed', processed_at=%s WHERE id=%s",
             (utcnow().isoformat(), event_id),
         )
--- a/core/framework_service.py
+++ b/core/framework_service.py
@@ -57,5 +57,5 @@
             "SELECT id, name, description, color, type, relevant_modules, "
             "is_active, total_controls, created_at, updated_at "
-            "FROM frameworks WHERE id = ?",
+            "FROM frameworks WHERE id = %s",
             (framework_id,),
         ).fetchone()
@@ -76,5 +76,5 @@
     try:
         db.execute(
-            "UPDATE frameworks SET is_active = 1, updated_at = ? WHERE id = ?",
+            "UPDATE frameworks SET is_active = 1, updated_at = %s WHERE id = %s",
             (utcnow().isoformat(), framework_id),
         )
@@ -97,5 +97,5 @@
     try:
         db.execute(
-            "UPDATE frameworks SET is_active = 0, updated_at = ? WHERE id = ?",
+            "UPDATE frameworks SET is_active = 0, updated_at = %s WHERE id = %s",
             (utcnow().isoformat(), framework_id),
         )
@@ -119,5 +119,5 @@
     db = get_db()
     try:
-        where = "WHERE framework_id = ?"
+        where = "WHERE framework_id = %s"
         params: list = [framework_id]
         if status:
@@ -152,5 +152,5 @@
         control_id = insert_returning_id(db,
             "INSERT INTO controls (framework_id, ref, name, description, category, "
-            "doc_type, priority, last_updated) VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
+            "doc_type, priority, last_updated) VALUES (%s, %s, %s, %s, %s, %s, %s, %s)",
             (framework_id, ref, name, description, category, doc_type, priority,
              utcnow().isoformat()),
@@ -159,6 +159,6 @@
         db.execute(
             "UPDATE frameworks SET total_controls = "
-            "(SELECT COUNT(*) FROM controls WHERE framework_id = ?), "
-            "updated_at = ? WHERE id = ?",
+            "(SELECT COUNT(*) FROM controls WHERE framework_id = %s), "
+            "updated_at = %s WHERE id = %s",
             (framework_id, utcnow().isoformat(), framework_id),
         )
@@ -180,5 +180,5 @@
                     "INSERT INTO controls (framework_id, ref, name, description, "
                     "category, doc_type, priority, last_updated) "
-                    "VALUES (?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                    "VALUES (%s, %s, %s, %s, %s, %s, %s, %s) ON CONFLICT DO NOTHING",
                     (
                         framework_id,
@@ -198,6 +198,6 @@
         db.execute(
             "UPDATE frameworks SET total_controls = "
-            "(SELECT COUNT(*) FROM controls WHERE framework_id = ?), "
-            "updated_at = ? WHERE id = ?",
+            "(SELECT COUNT(*) FROM controls WHERE framework_id = %s), "
+            "updated_at = %s WHERE id = %s",
             (framework_id, utcnow().isoformat(), framework_id),
         )
@@ -217,5 +217,5 @@
     try:
         db.execute(
-            "UPDATE controls SET status = ?, last_updated = ? WHERE id = ?",
+            "UPDATE controls SET status = %s, last_updated = %s WHERE id = %s",
             (status, utcnow().isoformat(), control_id),
         )
@@ -223,5 +223,5 @@
 
         row = db.execute(
-            "SELECT framework_id, ref, name FROM controls WHERE id = ?",
+            "SELECT framework_id, ref, name FROM controls WHERE id = %s",
             (control_id,),
         ).fetchone()
@@ -248,5 +248,5 @@
             "INSERT INTO cross_module_links (source_module, source_type, source_id, "
             "target_module, target_type, target_id, relationship, created_by) "
-            "VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s, %s, %s, %s)",
             (source_module, source_type, source_id,
              target_module, target_type, target_id, relationship, user_id),
@@ -266,6 +266,6 @@
             "target_module, target_type, target_id, relationship, created_at "
             "FROM cross_module_links "
-            "WHERE (source_module = ? AND source_type = ? AND source_id = ?) "
-            "   OR (target_module = ? AND target_type = ? AND target_id = ?) "
+            "WHERE (source_module = %s AND source_type = %s AND source_id = %s) "
+            "   OR (target_module = %s AND target_type = %s AND target_id = %s) "
             "ORDER BY created_at DESC",
             (module, entity_type, entity_id, module, entity_type, entity_id),
--- a/core/links.py
+++ b/core/links.py
@@ -108,5 +108,5 @@
             rows = db.execute(
                 "SELECT * FROM cross_module_links "
-                "WHERE source_module=? AND source_type=? AND source_id=?",
+                "WHERE source_module=%s AND source_type=%s AND source_id=%s",
                 (module, entity_type, entity_id),
             ).fetchall()
@@ -116,5 +116,5 @@
             rows = db.execute(
                 "SELECT * FROM cross_module_links "
-                "WHERE target_module=? AND target_type=? AND target_id=?",
+                "WHERE target_module=%s AND target_type=%s AND target_id=%s",
                 (module, entity_type, entity_id),
             ).fetchall()
@@ -139,5 +139,5 @@
         sql = (
             "SELECT target_id FROM cross_module_links "
-            "WHERE source_module=? AND source_type=? AND source_id=?"
+            "WHERE source_module=%s AND source_type=%s AND source_id=%s"
         )
         params: list = [module, entity_type, entity_id]
--- a/core/middleware.py
+++ b/core/middleware.py
@@ -223,5 +223,5 @@
         db.execute(
             "INSERT INTO audit_log (user_id, username, module, action, entity_type, "
-            "entity_id, details, ip_address) VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
+            "entity_id, details, ip_address) VALUES (%s, %s, %s, %s, %s, %s, %s, %s)",
             (
                 user["id"] if user else None,
--- a/core/reminder_scheduler.py
+++ b/core/reminder_scheduler.py
@@ -74,5 +74,5 @@
             if not to:
                 # No email address — just mark sent
-                db.execute("UPDATE email_reminders SET is_sent=1, sent_at=datetime('now') WHERE id=?", (rid,))
+                db.execute("UPDATE email_reminders SET is_sent=1, sent_at=datetime('now') WHERE id=%s", (rid,))
                 continue
 
@@ -85,5 +85,5 @@
             if result.get("ok"):
                 db.execute(
-                    "UPDATE email_reminders SET is_sent=1, sent_at=datetime('now') WHERE id=?",
+                    "UPDATE email_reminders SET is_sent=1, sent_at=datetime('now') WHERE id=%s",
                     (rid,),
                 )
@@ -99,5 +99,5 @@
                             "(module, entity_type, entity_id, title, message, "
                             " recipient_id, recipient_email, remind_at, repeat_interval, created_by) "
-                            "VALUES (?,?,?,?,?,?,?,datetime(?,?),?,?)",
+                            "VALUES (%s,%s,%s,%s,%s,%s,%s,datetime(%s,%s),%s,%s)",
                             (
                                 r["module"], r["entity_type"], r["entity_id"],
--- a/core/vendor_link.py
+++ b/core/vendor_link.py
@@ -35,11 +35,11 @@
         return None
     row = db.execute(
-        "SELECT id FROM canonical_vendors WHERE lower(trim(name))=?", (norm,)
+        "SELECT id FROM canonical_vendors WHERE lower(trim(name))=%s", (norm,)
     ).fetchone()
     if row:
         if contact_email:
             db.execute(
-                "UPDATE canonical_vendors SET contact_email=COALESCE(contact_email,?), "
-                "updated_at=datetime('now') WHERE id=? AND contact_email IS NULL",
+                "UPDATE canonical_vendors SET contact_email=COALESCE(contact_email,%s), "
+                "updated_at=datetime('now') WHERE id=%s AND contact_email IS NULL",
                 (contact_email, row["id"]),
             )
@@ -47,5 +47,5 @@
     try:
         cur = insert_returning_id(db,
-            "INSERT INTO canonical_vendors (name, contact_email) VALUES (?,?)",
+            "INSERT INTO canonical_vendors (name, contact_email) VALUES (%s,%s)",
             (name.strip(), contact_email),
         )
@@ -55,5 +55,5 @@
         # and our INSERT. Look it up again and return that id.
         row = db.execute(
-            "SELECT id FROM canonical_vendors WHERE lower(trim(name))=?", (norm,)
+            "SELECT id FROM canonical_vendors WHERE lower(trim(name))=%s", (norm,)
         ).fetchone()
         return row["id"] if row else None
@@ -66,5 +66,5 @@
     # Canonical base
     base = db.execute(
-        "SELECT * FROM canonical_vendors WHERE id=?", (canonical_id,)
+        "SELECT * FROM canonical_vendors WHERE id=%s", (canonical_id,)
     ).fetchone()
     if base:
@@ -75,5 +75,5 @@
         "SELECT id, name, type, dpa_status, dpa_expiry, risk_level, regulation, "
         "services, contact_name, contact_email, ai_assessment "
-        "FROM sentinel_vendors WHERE canonical_id=?",
+        "FROM sentinel_vendors WHERE canonical_id=%s",
         (canonical_id,),
     ).fetchone()
@@ -93,5 +93,5 @@
         "          WHERE vendor_id = v.id"
         "      ) "
-        "WHERE v.canonical_id=?",
+        "WHERE v.canonical_id=%s",
         (canonical_id,),
     ).fetchone()
@@ -115,5 +115,5 @@
         "SELECT id, name, criticality, tier, data_sensitivity, sla, "
         "contract_renewal, status, service_provided "
-        "FROM bcm_vendors WHERE canonical_id=?",
+        "FROM bcm_vendors WHERE canonical_id=%s",
         (canonical_id,),
     ).fetchone()
[WARN] database.py:
  line 2659: f-string SQL with ? — manual review needed
--- a/database.py
+++ b/database.py
@@ -135,8 +135,8 @@
 
 def _percent_s_to_question(sql: str) -> str:
-    """Replace %s with ? outside SQL string literals.
+    """Replace %s with %s outside SQL string literals.
 
     All SQL strings in the codebase use %s placeholders (psycopg2-style) after
-    Phase G.  This rewriter lets sqlite3 receive the ? it expects while keeping
+    Phase G.  This rewriter lets sqlite3 receive the %s it expects while keeping
     every call site engine-agnostic.
     """
@@ -2646,5 +2646,5 @@
                     continue
                 existing = conn.execute(
-                    "SELECT id FROM canonical_vendors WHERE lower(trim(name))=?", (norm,)
+                    "SELECT id FROM canonical_vendors WHERE lower(trim(name))=%s", (norm,)
                 ).fetchone()
                 if existing:
@@ -2652,5 +2652,5 @@
                 else:
                     cur = conn.execute(
-                        "INSERT INTO canonical_vendors (name, contact_email) VALUES (?,?)",
+                        "INSERT INTO canonical_vendors (name, contact_email) VALUES (%s,%s)",
                         (row[1].strip(), row[2]),
                     )
@@ -2693,5 +2693,5 @@
                 conn.execute(
                     "INSERT INTO frameworks (name, description, color, relevant_modules, is_active) "
-                    "VALUES (?, ?, ?, ?, 1)",
+                    "VALUES (%s, %s, %s, %s, 1)",
                     (name, desc, color, modules),
                 )
@@ -2734,5 +2734,5 @@
                     "INSERT INTO bcm_comm_templates "
                     "(title, category, incident_types, subject, body, variables, is_active, created_by) "
-                    "VALUES (?, ?, ?, ?, ?, ?, 1, 'system') ON CONFLICT DO NOTHING",
+                    "VALUES (%s, %s, %s, %s, %s, %s, 1, 'system') ON CONFLICT DO NOTHING",
                     (title, cat, inc_types, subject, body, variables),
                 )
@@ -2832,5 +2832,5 @@
                     "INSERT INTO bcm_scenario_library "
                     "(title, category, description, objectives, injects, estimated_duration_minutes, difficulty, is_builtin) "
-                    "VALUES (?,?,?,?,?,?,?,1) ON CONFLICT DO NOTHING",
+                    "VALUES (%s,%s,%s,%s,%s,%s,%s,1) ON CONFLICT DO NOTHING",
                     (title, cat, desc, objectives, injects, duration, difficulty),
                 )
@@ -2850,5 +2850,5 @@
             conn.execute(
                 "INSERT INTO sentinel_jurisdiction_config "
-                "(jurisdiction_key, is_active, is_primary) VALUES (?, 1, 1) ON CONFLICT DO NOTHING",
+                "(jurisdiction_key, is_active, is_primary) VALUES (%s, 1, 1) ON CONFLICT DO NOTHING",
                 ("GDPR",),
             )
@@ -2861,5 +2861,5 @@
                     conn.execute(
                         "INSERT INTO sentinel_jurisdiction_config "
-                        "(jurisdiction_key, is_active, is_primary) VALUES (?, 1, 0) ON CONFLICT DO NOTHING",
+                        "(jurisdiction_key, is_active, is_primary) VALUES (%s, 1, 0) ON CONFLICT DO NOTHING",
                         (reg,),
                     )
@@ -2876,5 +2876,5 @@
                 conn.execute(
                     "UPDATE sentinel_jurisdiction_config SET is_primary=1 "
-                    "WHERE jurisdiction_key=?", (first[0],),
+                    "WHERE jurisdiction_key=%s", (first[0],),
                 )
             conn.commit()
@@ -2898,5 +2898,5 @@
             for cat, lvl, max_s, desc in _APPETITE_SEEDS:
                 conn.execute(
-                    "INSERT INTO erm_risk_appetite (category, appetite_level, max_score, description) VALUES (?,?,?,?) ON CONFLICT DO NOTHING",
+                    "INSERT INTO erm_risk_appetite (category, appetite_level, max_score, description) VALUES (%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                     (cat, lvl, max_s, desc),
                 )
@@ -2942,5 +2942,5 @@
                     "(title, description, category, default_likelihood, default_impact, "
                     "typical_treatment, suggested_controls, applicable_industries, "
-                    "regulatory_references, tags) VALUES (?,?,?,?,?,?,?,?,?,?) ON CONFLICT DO NOTHING",
+                    "regulatory_references, tags) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                     row,
                 )
@@ -2958,5 +2958,5 @@
                 conn.execute(
                     "INSERT INTO aria_frameworks (name, description, color, relevant_modules, is_active) "
-                    "VALUES (?, ?, ?, ?, 1)",
+                    "VALUES (%s, %s, %s, %s, 1)",
                     (name, desc, color, modules),
                 )
@@ -3270,5 +3270,5 @@
                     "(title, description, category, event_type, severity, department, "
                     "process_affected, root_cause_category, corrective_action, preventive_action, "
-                    "basel_category, tags, is_active) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,1)",
+                    "basel_category, tags, is_active) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,1)",
                     (title, description, category, event_type, severity, department,
                      process_affected, rcc, corrective, preventive, basel, tags),
--- a/main.py
+++ b/main.py
@@ -81,5 +81,5 @@
                             "INSERT INTO frameworks "
                             "(name, description, color, relevant_modules, is_active) "
-                            "VALUES (?, ?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                            "VALUES (%s, %s, %s, %s, %s) ON CONFLICT DO NOTHING",
                             (r["name"], r["description"], r["color"],
                              r["relevant_modules"], r["is_active"]),
[WARN] modules/aria/ai_generator.py:
  line 95: f-string SQL with ? — manual review needed
--- a/modules/aria/ask_service.py
+++ b/modules/aria/ask_service.py
@@ -95,5 +95,5 @@
         db.execute(
             "DELETE FROM aria_ask_index "
-            "WHERE content_type=? AND content_id=?",
+            "WHERE content_type=%s AND content_id=%s",
             (content_type, str(content_id)),
         )
@@ -113,5 +113,5 @@
     try:
         doc = db.execute(
-            "SELECT * FROM aria_documents WHERE doc_id=?", (doc_id,)
+            "SELECT * FROM aria_documents WHERE doc_id=%s", (doc_id,)
         ).fetchone()
     finally:
@@ -139,5 +139,5 @@
                 "(content_type, content_id, title, section, body, "
                 " owner, framework, control_ref, url_path) "
-                "VALUES ('document', ?, ?, ?, ?, ?, ?, ?, ?)",
+                "VALUES ('document', %s, %s, %s, %s, %s, %s, %s, %s)",
                 (doc_id, doc["title"], section, text,
                  doc["owner"] or "", doc["framework"] or "",
@@ -158,5 +158,5 @@
             "FROM aria_controls c "
             "JOIN aria_frameworks f ON c.framework_id = f.id "
-            "WHERE c.id=?",
+            "WHERE c.id=%s",
             (control_id,),
         ).fetchone()
@@ -183,5 +183,5 @@
             "(content_type, content_id, title, section, body, "
             " owner, framework, control_ref, url_path) "
-            "VALUES ('control', ?, ?, ?, ?, ?, ?, ?, ?)",
+            "VALUES ('control', %s, %s, %s, %s, %s, %s, %s, %s)",
             (str(control_id),
              f"{ctrl['ref']} -- {ctrl['name']}",
@@ -203,5 +203,5 @@
     try:
         risk = db.execute(
-            "SELECT * FROM aria_risks WHERE risk_id=?", (risk_id,)
+            "SELECT * FROM aria_risks WHERE risk_id=%s", (risk_id,)
         ).fetchone()
     finally:
@@ -228,5 +228,5 @@
             "(content_type, content_id, title, section, body, "
             " owner, framework, control_ref, url_path) "
-            "VALUES ('risk', ?, ?, ?, ?, ?, ?, ?, ?)",
+            "VALUES ('risk', %s, %s, %s, %s, %s, %s, %s, %s)",
             (risk_id,
              f"{risk_id} -- {risk['description'][:60]}",
@@ -328,5 +328,5 @@
             "       bm25(aria_ask_index) AS score "
             "FROM aria_ask_index "
-            "WHERE aria_ask_index MATCH ? "
+            "WHERE aria_ask_index MATCH %s "
         )
         fw = (framework_filter or "").strip()
@@ -523,5 +523,5 @@
                 "(user_id, username, question, answer, covered, "
                 " citations, latency_ms) "
-                "VALUES (?, ?, ?, ?, ?, ?, ?)",
+                "VALUES (%s, %s, %s, %s, %s, %s, %s)",
                 (
                     (user or {}).get("id"),
--- a/modules/aria/routes.py
+++ b/modules/aria/routes.py
@@ -190,5 +190,5 @@
                     SUM(CASE WHEN status='Under Review' THEN 1 ELSE 0 END) as under_review,
                     SUM(CASE WHEN status='Needs Update' THEN 1 ELSE 0 END) as needs_update
-                FROM controls WHERE framework_id=?
+                FROM controls WHERE framework_id=%s
             """, (fw["id"],)).fetchone()
             total = stats["total"] or 0
@@ -255,5 +255,5 @@
                     SUM(CASE WHEN status='In Progress' THEN 1 ELSE 0 END) as in_progress,
                     SUM(CASE WHEN status='Not Started' THEN 1 ELSE 0 END) as not_started
-                FROM controls WHERE framework_id=?
+                FROM controls WHERE framework_id=%s
             """, (fw["id"],)).fetchone()
             total = stats["total"] or 0
@@ -284,5 +284,5 @@
     try:
         fw = db.execute(
-            "SELECT * FROM frameworks WHERE id=?", (fw_id,)
+            "SELECT * FROM frameworks WHERE id=%s", (fw_id,)
         ).fetchone()
         if not fw:
@@ -291,5 +291,5 @@
         # Lazy-seed controls if none exist for this framework
         ctrl_count = db.execute(
-            "SELECT COUNT(*) FROM controls WHERE framework_id=?", (fw_id,)
+            "SELECT COUNT(*) FROM controls WHERE framework_id=%s", (fw_id,)
         ).fetchone()[0]
         if ctrl_count == 0:
@@ -309,5 +309,5 @@
              " WHERE el.module='aria' AND el.entity_type='control' AND el.entity_id=c.id"
              ") AS evidence_count "
-             "FROM controls c WHERE c.framework_id=?")
+             "FROM controls c WHERE c.framework_id=%s")
         params = [fw_id]
         if status:
@@ -328,5 +328,5 @@
         cats = db.execute(
             "SELECT DISTINCT category FROM controls "
-            "WHERE framework_id=? ORDER BY category",
+            "WHERE framework_id=%s ORDER BY category",
             (fw_id,),
         ).fetchall()
@@ -338,5 +338,5 @@
                 SUM(CASE WHEN status='In Progress' THEN 1 ELSE 0 END) as in_progress,
                 SUM(CASE WHEN status='Approved' THEN 1 ELSE 0 END) as approved
-            FROM controls WHERE framework_id=?
+            FROM controls WHERE framework_id=%s
         """, (fw_id,)).fetchone()
     finally:
@@ -368,5 +368,5 @@
     try:
         old = db.execute(
-            "SELECT * FROM controls WHERE id=?", (ctrl_id,)
+            "SELECT * FROM controls WHERE id=%s", (ctrl_id,)
         ).fetchone()
         if not old:
@@ -392,5 +392,5 @@
             params.append(ctrl_id)
             db.execute(
-                "UPDATE controls SET " + ", ".join(updates) + " WHERE id=?",
+                "UPDATE controls SET " + ", ".join(updates) + " WHERE id=%s",
                 params,
             )
@@ -456,5 +456,5 @@
                     SUM(CASE WHEN status='Needs Update' THEN 1 ELSE 0 END) as needs_update,
                     COUNT(*) as total
-                FROM controls WHERE framework_id=?
+                FROM controls WHERE framework_id=%s
             """, (fw["id"],)).fetchone()
             data.append({
@@ -481,5 +481,5 @@
         controls = db.execute(
             "SELECT id, ref, name, description, doc_type, status "
-            "FROM controls WHERE framework_id=? ORDER BY ref",
+            "FROM controls WHERE framework_id=%s ORDER BY ref",
             (fw_id,),
         ).fetchall()
@@ -501,5 +501,5 @@
                         controls = db2.execute(
                             "SELECT id, ref, name, description, doc_type, status "
-                            "FROM controls WHERE framework_id=? ORDER BY ref",
+                            "FROM controls WHERE framework_id=%s ORDER BY ref",
                             (fw_id,),
                         ).fetchall()
@@ -519,5 +519,5 @@
     try:
         fw = db.execute(
-            "SELECT id, name FROM frameworks WHERE id=?", (fw_id,)
+            "SELECT id, name FROM frameworks WHERE id=%s", (fw_id,)
         ).fetchone()
         if not fw:
@@ -526,5 +526,5 @@
         # Check existing count
         existing = db.execute(
-            "SELECT COUNT(*) as c FROM controls WHERE framework_id=?",
+            "SELECT COUNT(*) as c FROM controls WHERE framework_id=%s",
             (fw_id,)
         ).fetchone()["c"]
@@ -563,5 +563,5 @@
                     "(framework_id, ref, name, description, category, "
                     "doc_type, priority, last_updated) "
-                    "VALUES (?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                    "VALUES (%s, %s, %s, %s, %s, %s, %s, %s) ON CONFLICT DO NOTHING",
                     (
                         fw_id,
@@ -582,9 +582,9 @@
         # Update total
         final_count = db.execute(
-            "SELECT COUNT(*) as c FROM controls WHERE framework_id=?",
+            "SELECT COUNT(*) as c FROM controls WHERE framework_id=%s",
             (fw_id,)
         ).fetchone()["c"]
         db.execute(
-            "UPDATE frameworks SET total_controls=? WHERE id=?",
+            "UPDATE frameworks SET total_controls=%s WHERE id=%s",
             (final_count, fw_id),
         )
@@ -616,5 +616,5 @@
         for fw in frameworks:
             count = db.execute(
-                "SELECT COUNT(*) FROM controls WHERE framework_id=?",
+                "SELECT COUNT(*) FROM controls WHERE framework_id=%s",
                 (fw["id"],),
             ).fetchone()[0]
@@ -728,5 +728,5 @@
              owner, approver, effective_date, review_date, location, comments,
              created_at, updated_at)
-            VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
+            VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)
         """, (doc_id, framework, control_ref, title, doc_type, version, status,
               owner, approver, effective_date or None, review_date or None,
@@ -772,5 +772,5 @@
     try:
         existing = db.execute(
-            "SELECT * FROM aria_documents WHERE doc_id=?", (doc_id,)
+            "SELECT * FROM aria_documents WHERE doc_id=%s", (doc_id,)
         ).fetchone()
         if not existing:
@@ -813,5 +813,5 @@
             params.append(doc_id)
             db.execute(
-                "UPDATE aria_documents SET " + ", ".join(updates) + " WHERE doc_id=?",
+                "UPDATE aria_documents SET " + ", ".join(updates) + " WHERE doc_id=%s",
                 params,
             )
@@ -855,5 +855,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM aria_documents WHERE doc_id=?", (doc_id,))
+        db.execute("DELETE FROM aria_documents WHERE doc_id=%s", (doc_id,))
         db.commit()
         log_audit(user, "aria", "Deleted document " + doc_id,
@@ -899,5 +899,5 @@
     db = get_db()
     try:
-        doc = db.execute("SELECT * FROM aria_documents WHERE doc_id=?", (doc_id,)).fetchone()
+        doc = db.execute("SELECT * FROM aria_documents WHERE doc_id=%s", (doc_id,)).fetchone()
         if not doc:
             return JSONResponse({"error": "Document not found"}, 404)
@@ -912,5 +912,5 @@
         if doc.get("body") and not doc.get("ai_draft_body"):
             db.execute(
-                "UPDATE aria_documents SET ai_draft_body=? WHERE doc_id=?",
+                "UPDATE aria_documents SET ai_draft_body=%s WHERE doc_id=%s",
                 (doc["body"], doc_id),
             )
@@ -922,5 +922,5 @@
             "(document_id, version, file_path, file_name, file_size, "
             " revision_type, notes, uploaded_by) "
-            "VALUES (?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
             (doc["id"], old_version, doc.get("file_path", ""),
              doc.get("file_name", ""), doc.get("file_size", 0),
@@ -939,7 +939,7 @@
         db.execute(
             "UPDATE aria_documents SET "
-            "file_path=?, file_name=?, file_size=?, version=?, "
+            "file_path=%s, file_name=%s, file_size=%s, version=%s, "
             "status='Under Review', updated_at=datetime('now') "
-            "WHERE doc_id=?",
+            "WHERE doc_id=%s",
             (str(stored_name), file.filename, len(content),
              new_version, doc_id),
@@ -981,5 +981,5 @@
         doc = db.execute(
             "SELECT file_path, file_name, branded_file_path, title "
-            "FROM aria_documents WHERE doc_id=?", (doc_id,)
+            "FROM aria_documents WHERE doc_id=%s", (doc_id,)
         ).fetchone()
         if not doc:
@@ -1010,5 +1010,5 @@
     db = get_db()
     try:
-        doc = db.execute("SELECT id FROM aria_documents WHERE doc_id=?", (doc_id,)).fetchone()
+        doc = db.execute("SELECT id FROM aria_documents WHERE doc_id=%s", (doc_id,)).fetchone()
         if not doc:
             return JSONResponse({"error": "Document not found"}, 404)
@@ -1017,5 +1017,5 @@
             "FROM aria_doc_revisions r "
             "LEFT JOIN users u ON r.uploaded_by=u.id "
-            "WHERE r.document_id=? ORDER BY r.created_at DESC",
+            "WHERE r.document_id=%s ORDER BY r.created_at DESC",
             (doc[0],),
         ).fetchall()
@@ -1091,5 +1091,5 @@
         if is_default == "1":
             db.execute(
-                "UPDATE aria_doc_templates SET is_default=0 WHERE doc_type=?",
+                "UPDATE aria_doc_templates SET is_default=0 WHERE doc_type=%s",
                 (doc_type,),
             )
@@ -1098,5 +1098,5 @@
             "(name, description, doc_type, file_path, file_name, file_size, "
             " is_default, created_by) "
-            "VALUES (?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
             (name.strip(), description.strip(), doc_type, stored,
              file.filename, len(content), int(is_default == "1"),
@@ -1117,5 +1117,5 @@
     db = get_db()
     try:
-        row = db.execute("SELECT file_path FROM aria_doc_templates WHERE id=?", (tid,)).fetchone()
+        row = db.execute("SELECT file_path FROM aria_doc_templates WHERE id=%s", (tid,)).fetchone()
         if row and row[0]:
             fp = ARIA_TEMPLATE_DIR / row[0]
@@ -1125,5 +1125,5 @@
             except OSError:
                 pass
-        db.execute("DELETE FROM aria_doc_templates WHERE id=?", (tid,))
+        db.execute("DELETE FROM aria_doc_templates WHERE id=%s", (tid,))
         db.commit()
         log_audit(user, "aria", f"Deleted template #{tid}", "template", tid)
@@ -1141,5 +1141,5 @@
     try:
         row = db.execute(
-            "SELECT file_path, file_name FROM aria_doc_templates WHERE id=?", (tid,)
+            "SELECT file_path, file_name FROM aria_doc_templates WHERE id=%s", (tid,)
         ).fetchone()
         if not row:
@@ -1166,5 +1166,5 @@
     db = get_db()
     try:
-        doc = db.execute("SELECT * FROM aria_documents WHERE doc_id=?", (doc_id,)).fetchone()
+        doc = db.execute("SELECT * FROM aria_documents WHERE doc_id=%s", (doc_id,)).fetchone()
         if not doc:
             return JSONResponse({"error": "Document not found"}, 404)
@@ -1174,5 +1174,5 @@
             return JSONResponse({"error": "No uploaded file to apply template to"}, 400)
 
-        tpl = db.execute("SELECT * FROM aria_doc_templates WHERE id=?", (template_id,)).fetchone()
+        tpl = db.execute("SELECT * FROM aria_doc_templates WHERE id=%s", (template_id,)).fetchone()
         if not tpl:
             return JSONResponse({"error": "Template not found"}, 404)
@@ -1206,6 +1206,6 @@
         db.execute(
             "UPDATE aria_documents SET "
-            "branded_file_path=?, template_id=?, updated_at=datetime('now') "
-            "WHERE doc_id=?",
+            "branded_file_path=%s, template_id=%s, updated_at=datetime('now') "
+            "WHERE doc_id=%s",
             (branded_name, template_id, doc_id),
         )
@@ -1262,5 +1262,5 @@
             (risk_id, framework, control_ref, description, category,
              likelihood, impact, owner, mitigation, status)
-            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, 'Open')
+            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, 'Open')
         """, (risk_id, framework, control_ref, description, category,
               likelihood, impact, owner, mitigation))
@@ -1304,5 +1304,5 @@
     db = get_db()
     try:
-        old = db.execute("SELECT * FROM aria_risks WHERE id=?", (risk_id,)).fetchone()
+        old = db.execute("SELECT * FROM aria_risks WHERE id=%s", (risk_id,)).fetchone()
         if not old:
             return JSONResponse({"error": "Risk not found"}, 404)
@@ -1317,5 +1317,5 @@
         if updates:
             params.append(risk_id)
-            db.execute("UPDATE aria_risks SET " + ", ".join(updates) + " WHERE id=?", params)
+            db.execute("UPDATE aria_risks SET " + ", ".join(updates) + " WHERE id=%s", params)
             db.commit()
             log_audit(user, "aria", f"Updated risk #{risk_id} status={status}",
@@ -1381,9 +1381,9 @@
         params: list = []
         if fw1 and fw2:
-            where = ("WHERE (m.source_framework_id=? AND m.target_framework_id=?) "
-                     "OR (m.source_framework_id=? AND m.target_framework_id=?)")
+            where = ("WHERE (m.source_framework_id=%s AND m.target_framework_id=%s) "
+                     "OR (m.source_framework_id=%s AND m.target_framework_id=%s)")
             params = [fw1, fw2, fw2, fw1]
         elif fw1:
-            where = "WHERE m.source_framework_id=? OR m.target_framework_id=?"
+            where = "WHERE m.source_framework_id=%s OR m.target_framework_id=%s"
             params = [fw1, fw1]
 
@@ -1414,5 +1414,5 @@
     try:
         rows = db.execute(
-            "SELECT id, ref, name, category FROM controls WHERE framework_id=? ORDER BY ref",
+            "SELECT id, ref, name, category FROM controls WHERE framework_id=%s ORDER BY ref",
             (fw_id,)
         ).fetchall()
@@ -1441,6 +1441,6 @@
     try:
         # Look up framework IDs from control IDs
-        src = db.execute("SELECT id, framework_id FROM controls WHERE id=?", (source_ctrl_id,)).fetchone()
-        tgt = db.execute("SELECT id, framework_id FROM controls WHERE id=?", (target_ctrl_id,)).fetchone()
+        src = db.execute("SELECT id, framework_id FROM controls WHERE id=%s", (source_ctrl_id,)).fetchone()
+        tgt = db.execute("SELECT id, framework_id FROM controls WHERE id=%s", (target_ctrl_id,)).fetchone()
         if not src or not tgt:
             return JSONResponse({"ok": False, "error": "One or both controls not found"}, status_code=404)
@@ -1452,5 +1452,5 @@
             (source_framework_id, source_control_id, target_framework_id, target_control_id,
              mapping_type, notes, created_by)
-            VALUES (?, ?, ?, ?, ?, ?, ?) ON CONFLICT DO NOTHING
+            VALUES (%s, %s, %s, %s, %s, %s, %s) ON CONFLICT DO NOTHING
         """, (src["framework_id"], source_ctrl_id,
               tgt["framework_id"], target_ctrl_id,
@@ -1460,5 +1460,5 @@
             # IGNORE triggered — already exists
             existing = db.execute(
-                "SELECT id FROM aria_control_mappings WHERE source_control_id=? AND target_control_id=?",
+                "SELECT id FROM aria_control_mappings WHERE source_control_id=%s AND target_control_id=%s",
                 (source_ctrl_id, target_ctrl_id)
             ).fetchone()
@@ -1475,5 +1475,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM aria_control_mappings WHERE id=?", (mapping_id,))
+        db.execute("DELETE FROM aria_control_mappings WHERE id=%s", (mapping_id,))
         db.commit()
     finally:
@@ -1618,5 +1618,5 @@
 
             controls = db.execute(
-                "SELECT * FROM controls WHERE framework_id=? ORDER BY ref",
+                "SELECT * FROM controls WHERE framework_id=%s ORDER BY ref",
                 (fw["id"],),
             ).fetchall()
@@ -1710,5 +1710,5 @@
             ctrls = db.execute(
                 "SELECT id, ref, name, description, doc_type, status "
-                "FROM controls WHERE framework_id=? ORDER BY ref",
+                "FROM controls WHERE framework_id=%s ORDER BY ref",
                 (fw_id,),
             ).fetchall()
@@ -1740,5 +1740,5 @@
                                 "(framework_id, ref, name, description, "
                                 "category, doc_type, priority, last_updated)"
-                                " VALUES (?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                                " VALUES (%s, %s, %s, %s, %s, %s, %s, %s) ON CONFLICT DO NOTHING",
                                 (
                                     fw_id,
@@ -1763,5 +1763,5 @@
                         "UPDATE frameworks SET total_controls = "
                         "(SELECT COUNT(*) FROM controls "
-                        "WHERE framework_id = ?) WHERE id = ?",
+                        "WHERE framework_id = %s) WHERE id = %s",
                         (fw_id, fw_id),
                     )
@@ -1772,5 +1772,5 @@
                         "SELECT id, ref, name, description, "
                         "doc_type, status "
-                        "FROM controls WHERE framework_id=? "
+                        "FROM controls WHERE framework_id=%s "
                         "ORDER BY ref",
                         (fw_id,),
@@ -1876,5 +1876,5 @@
             FROM controls c
             JOIN frameworks f ON c.framework_id = f.id
-            WHERE c.id=?
+            WHERE c.id=%s
         """, (control_id,)).fetchone()
 
@@ -1889,5 +1889,5 @@
                     JOIN controls c   ON c.id = m.target_control_id
                     JOIN frameworks f ON f.id = m.target_framework_id
-                    WHERE m.source_control_id = ? AND m.target_framework_id = ?
+                    WHERE m.source_control_id = %s AND m.target_framework_id = %s
                     UNION
                     SELECT c.ref, c.name, c.description, f.name AS fw_name
@@ -1895,5 +1895,5 @@
                     JOIN controls c   ON c.id = m.source_control_id
                     JOIN frameworks f ON f.id = m.source_framework_id
-                    WHERE m.target_control_id = ? AND m.source_framework_id = ?
+                    WHERE m.target_control_id = %s AND m.source_framework_id = %s
                 """, (control_id, tfw_id, control_id, tfw_id)).fetchall()
                 for row in mapped:
@@ -1931,10 +1931,10 @@
         try:
             db.execute(
-                "UPDATE controls SET last_updated=? WHERE id=?",
+                "UPDATE controls SET last_updated=%s WHERE id=%s",
                 (datetime.now().strftime("%Y-%m-%d"), control_id),
             )
             existing = db.execute(
                 "SELECT doc_id FROM aria_documents "
-                "WHERE control_ref=? AND framework=?",
+                "WHERE control_ref=%s AND framework=%s",
                 (ctrl["ref"], ctrl["fw_name"]),
             ).fetchone()
@@ -1943,5 +1943,5 @@
             if existing:
                 old_ver = db.execute(
-                    "SELECT version FROM aria_documents WHERE doc_id=?",
+                    "SELECT version FROM aria_documents WHERE doc_id=%s",
                     (existing["doc_id"],),
                 ).fetchone()["version"]
@@ -1953,8 +1953,8 @@
                 db.execute("""
                     UPDATE aria_documents
-                    SET version=?, status='Draft', updated_at=?,
-                        comments='AI Generated -- updated ' || ?,
-                        body=?
-                    WHERE doc_id=?
+                    SET version=%s, status='Draft', updated_at=%s,
+                        comments='AI Generated -- updated ' || %s,
+                        body=%s
+                    WHERE doc_id=%s
                 """, (new_ver, now, datetime.now().strftime("%Y-%m-%d"),
                       policy_body, existing["doc_id"]))
@@ -1969,5 +1969,5 @@
                      version, status, owner, created_at, updated_at,
                      comments, body)
-                    VALUES (?,?,?,?,?,?,?,?,?,?,?,?)
+                    VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)
                 """, (doc_id, ctrl["fw_name"], ctrl["ref"],
                       ctrl["name"] + " -- " + resolved_doc_type,
@@ -1997,9 +1997,9 @@
     try:
         fw = db.execute(
-            "SELECT * FROM frameworks WHERE id=?", (framework_id,)
+            "SELECT * FROM frameworks WHERE id=%s", (framework_id,)
         ).fetchone()
         controls = db.execute(
             "SELECT ref, name, status, priority "
-            "FROM controls WHERE framework_id=?",
+            "FROM controls WHERE framework_id=%s",
             (framework_id,),
         ).fetchall()
@@ -2032,5 +2032,5 @@
             SELECT question, answer, covered, citations, created_at
             FROM aria_ask_log
-            WHERE username=?
+            WHERE username=%s
             ORDER BY id DESC LIMIT 12
         """, (user["username"],)).fetchall()
@@ -2060,10 +2060,10 @@
         ).fetchone()[0]
         total_asked = db2.execute(
-            "SELECT COUNT(*) FROM aria_ask_log WHERE username=?",
+            "SELECT COUNT(*) FROM aria_ask_log WHERE username=%s",
             (user["username"],)
         ).fetchone()[0]
         covered_row = db2.execute(
             "SELECT ROUND(AVG(CASE WHEN covered=1 THEN 100.0 ELSE 0 END),0) "
-            "FROM aria_ask_log WHERE username=?",
+            "FROM aria_ask_log WHERE username=%s",
             (user["username"],)
         ).fetchone()[0]
@@ -2071,5 +2071,5 @@
         helpful_row = db2.execute(
             "SELECT ROUND(AVG(CASE WHEN feedback=1 THEN 100.0 ELSE 0 END),0) "
-            "FROM aria_ask_log WHERE username=? AND feedback IS NOT NULL",
+            "FROM aria_ask_log WHERE username=%s AND feedback IS NOT NULL",
             (user["username"],)
         ).fetchone()[0]
@@ -2139,5 +2139,5 @@
     try:
         db.execute(
-            "UPDATE aria_ask_log SET feedback=? WHERE id=? AND user_id=?",
+            "UPDATE aria_ask_log SET feedback=%s WHERE id=%s AND user_id=%s",
             (feedback, log_id, user["id"]),
         )
[WARN] modules/bcm/data_service.py:
  line 71: f-string SQL with ? — manual review needed
  line 77: f-string SQL with ? — manual review needed
  line 126: f-string SQL with ? — manual review needed
  line 135: f-string SQL with ? — manual review needed
  line 179: f-string SQL with ? — manual review needed
  line 185: f-string SQL with ? — manual review needed
  line 232: f-string SQL with ? — manual review needed
  line 241: f-string SQL with ? — manual review needed
  line 294: f-string SQL with ? — manual review needed
  line 303: f-string SQL with ? — manual review needed
  line 354: f-string SQL with ? — manual review needed
  line 358: f-string SQL with ? — manual review needed
  line 430: f-string SQL with ? — manual review needed
  line 436: f-string SQL with ? — manual review needed
  line 494: f-string SQL with ? — manual review needed
  line 500: f-string SQL with ? — manual review needed
  line 568: f-string SQL with ? — manual review needed
  line 574: f-string SQL with ? — manual review needed
  line 701: f-string SQL with ? — manual review needed
  line 707: f-string SQL with ? — manual review needed
  line 779: f-string SQL with ? — manual review needed
  line 783: f-string SQL with ? — manual review needed
  line 837: f-string SQL with ? — manual review needed
  line 881: f-string SQL with ? — manual review needed
  line 887: f-string SQL with ? — manual review needed
  line 1091: f-string SQL with ? — manual review needed
  line 1097: f-string SQL with ? — manual review needed
  line 1156: f-string SQL with ? — manual review needed
  line 1162: f-string SQL with ? — manual review needed
--- a/modules/bcm/data_service.py
+++ b/modules/bcm/data_service.py
@@ -35,5 +35,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_bia_records ORDER BY criticality DESC, process_name LIMIT ?",
+        "SELECT * FROM bcm_bia_records ORDER BY criticality DESC, process_name LIMIT %s",
         (limit,)).fetchall())
 
@@ -41,5 +41,5 @@
 def get_bia(bia_id):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_bia_records WHERE id=?", (bia_id,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_bia_records WHERE id=%s", (bia_id,)).fetchone())
 
 
@@ -51,5 +51,5 @@
             financial_impact_per_day, operational_impact, reputational_impact,
             regulatory_impact, criticality, dependencies)
-           VALUES (?,?,?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("process_name"), data.get("department"), data.get("owner"),
          data.get("description"), data.get("rto_hours"), data.get("rpo_hours"),
@@ -81,5 +81,5 @@
 def delete_bia(bia_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_bia_records WHERE id=?", (bia_id,))
+    db.execute("DELETE FROM bcm_bia_records WHERE id=%s", (bia_id,))
     db.commit()
 
@@ -92,5 +92,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_risks ORDER BY score DESC, created_at DESC LIMIT ?",
+        "SELECT * FROM bcm_risks ORDER BY score DESC, created_at DESC LIMIT %s",
         (limit,)).fetchall())
 
@@ -98,5 +98,5 @@
 def get_risk(risk_id):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_risks WHERE id=?", (risk_id,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_risks WHERE id=%s", (risk_id,)).fetchone())
 
 
@@ -110,5 +110,5 @@
            (title, category, description, likelihood, impact, score,
             treatment, mitigation, owner, status, due_date)
-           VALUES (?,?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("title"), data.get("category"), data.get("description"),
          likelihood, impact, score, data.get("treatment"), data.get("mitigation"),
@@ -139,5 +139,5 @@
 def delete_risk(risk_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_risks WHERE id=?", (risk_id,))
+    db.execute("DELETE FROM bcm_risks WHERE id=%s", (risk_id,))
     db.commit()
 
@@ -150,10 +150,10 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_plans ORDER BY updated_at DESC LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_plans ORDER BY updated_at DESC LIMIT %s", (limit,)).fetchall())
 
 
 def get_plan(plan_id):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_plans WHERE id=?", (plan_id,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_plans WHERE id=%s", (plan_id,)).fetchone())
 
 
@@ -163,5 +163,5 @@
         """INSERT INTO bcm_plans
            (title, scope, owner, version, status, content, last_reviewed, next_review)
-           VALUES (?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("title"), data.get("scope"), data.get("owner"),
          data.get("version", "1.0"), data.get("status", "draft"),
@@ -189,5 +189,5 @@
 def delete_plan(plan_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_plans WHERE id=?", (plan_id,))
+    db.execute("DELETE FROM bcm_plans WHERE id=%s", (plan_id,))
     db.commit()
 
@@ -201,13 +201,13 @@
     if status:
         return _dicts(db.execute(
-            "SELECT * FROM bcm_incidents WHERE status=? ORDER BY created_at DESC LIMIT ?",
+            "SELECT * FROM bcm_incidents WHERE status=%s ORDER BY created_at DESC LIMIT %s",
             (status, limit)).fetchall())
     return _dicts(db.execute(
-        "SELECT * FROM bcm_incidents ORDER BY created_at DESC LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_incidents ORDER BY created_at DESC LIMIT %s", (limit,)).fetchall())
 
 
 def get_incident(inc_id):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_incidents WHERE id=?", (inc_id,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_incidents WHERE id=%s", (inc_id,)).fetchone())
 
 
@@ -217,5 +217,5 @@
         """INSERT INTO bcm_incidents
            (title, description, severity, status, commander, affected_systems)
-           VALUES (?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s)""",
         (data.get("title"), data.get("description"), data.get("severity", "medium"),
          data.get("status", "open"), data.get("commander"), data.get("affected_systems")))
@@ -245,5 +245,5 @@
 def delete_incident(inc_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_incidents WHERE id=?", (inc_id,))
+    db.execute("DELETE FROM bcm_incidents WHERE id=%s", (inc_id,))
     db.commit()
 
@@ -253,5 +253,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_incident_updates WHERE incident_id=? ORDER BY created_at",
+        "SELECT * FROM bcm_incident_updates WHERE incident_id=%s ORDER BY created_at",
         (inc_id,)).fetchall())
 
@@ -260,5 +260,5 @@
     db = get_db()
     cur = insert_returning_id(db,
-        "INSERT INTO bcm_incident_updates (incident_id, author, note) VALUES (?,?,?)",
+        "INSERT INTO bcm_incident_updates (incident_id, author, note) VALUES (%s,%s,%s)",
         (inc_id, author, note))
     db.commit()
@@ -270,5 +270,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_incident_actions WHERE incident_id=? ORDER BY priority DESC, created_at",
+        "SELECT * FROM bcm_incident_actions WHERE incident_id=%s ORDER BY priority DESC, created_at",
         (inc_id,)).fetchall())
 
@@ -279,5 +279,5 @@
         """INSERT INTO bcm_incident_actions
            (incident_id, title, owner, status, priority, due_at, notes, created_by)
-           VALUES (?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s)""",
         (inc_id, data.get("title"), data.get("owner"), data.get("status", "open"),
          data.get("priority", "normal"), data.get("due_at"), data.get("notes"),
@@ -307,5 +307,5 @@
 def delete_incident_action(action_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_incident_actions WHERE id=?", (action_id,))
+    db.execute("DELETE FROM bcm_incident_actions WHERE id=%s", (action_id,))
     db.commit()
 
@@ -315,5 +315,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_incident_decisions WHERE incident_id=? ORDER BY decided_at",
+        "SELECT * FROM bcm_incident_decisions WHERE incident_id=%s ORDER BY decided_at",
         (inc_id,)).fetchall())
 
@@ -322,5 +322,5 @@
     db = get_db()
     cur = insert_returning_id(db,
-        "INSERT INTO bcm_incident_decisions (incident_id, decision, rationale, decided_by) VALUES (?,?,?,?)",
+        "INSERT INTO bcm_incident_decisions (incident_id, decision, rationale, decided_by) VALUES (%s,%s,%s,%s)",
         (inc_id, data.get("decision"), data.get("rationale"), data.get("decided_by")))
     db.commit()
@@ -332,5 +332,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_incident_stakeholders WHERE incident_id=? ORDER BY created_at",
+        "SELECT * FROM bcm_incident_stakeholders WHERE incident_id=%s ORDER BY created_at",
         (inc_id,)).fetchall())
 
@@ -341,5 +341,5 @@
         """INSERT INTO bcm_incident_stakeholders
            (incident_id, role, person, channel, notes)
-           VALUES (?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s)""",
         (inc_id, data.get("role"), data.get("person"), data.get("channel"), data.get("notes")))
     db.commit()
@@ -362,5 +362,5 @@
 def delete_incident_stakeholder(sh_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_incident_stakeholders WHERE id=?", (sh_id,))
+    db.execute("DELETE FROM bcm_incident_stakeholders WHERE id=%s", (sh_id,))
     db.commit()
 
@@ -372,5 +372,5 @@
         """SELECT l.*, p.title AS plan_title FROM bcm_incident_plan_links l
            JOIN bcm_plans p ON p.id = l.plan_id
-           WHERE l.incident_id=? ORDER BY l.created_at""",
+           WHERE l.incident_id=%s ORDER BY l.created_at""",
         (inc_id,)).fetchall())
 
@@ -379,5 +379,5 @@
     db = get_db()
     cur = insert_returning_id(db,
-        "INSERT INTO bcm_incident_plan_links (incident_id, plan_id, linked_by) VALUES (?,?,?) ON CONFLICT DO NOTHING",
+        "INSERT INTO bcm_incident_plan_links (incident_id, plan_id, linked_by) VALUES (%s,%s,%s) ON CONFLICT DO NOTHING",
         (inc_id, plan_id, linked_by))
     db.commit()
@@ -387,5 +387,5 @@
 def unlink_incident_plan(link_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_incident_plan_links WHERE id=?", (link_id,))
+    db.execute("DELETE FROM bcm_incident_plan_links WHERE id=%s", (link_id,))
     db.commit()
 
@@ -398,10 +398,10 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_exercises ORDER BY scheduled_date DESC LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_exercises ORDER BY scheduled_date DESC LIMIT %s", (limit,)).fetchall())
 
 
 def get_exercise(ex_id):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_exercises WHERE id=?", (ex_id,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_exercises WHERE id=%s", (ex_id,)).fetchone())
 
 
@@ -412,5 +412,5 @@
            (title, type, scenario, plan_id, scheduled_date, duration_minutes,
             facilitator, participants, objectives, status)
-           VALUES (?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("title"), data.get("type"), data.get("scenario"),
          data.get("plan_id"), data.get("scheduled_date"), data.get("duration_minutes"),
@@ -440,5 +440,5 @@
 def delete_exercise(ex_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_exercises WHERE id=?", (ex_id,))
+    db.execute("DELETE FROM bcm_exercises WHERE id=%s", (ex_id,))
     db.commit()
 
@@ -451,10 +451,10 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_vendors ORDER BY name LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_vendors ORDER BY name LIMIT %s", (limit,)).fetchall())
 
 
 def get_vendor(vid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_vendors WHERE id=?", (vid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_vendors WHERE id=%s", (vid,)).fetchone())
 
 
@@ -474,5 +474,5 @@
             contact_phone, criticality, tier, data_sensitivity, sla,
             contract_renewal, risk_score, status, notes, canonical_id)
-           VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("name"), data.get("category"), data.get("service_provided"),
          data.get("owner"), data.get("contact_name"), data.get("contact_email"),
@@ -504,5 +504,5 @@
 def delete_vendor(vid):
     db = get_db()
-    db.execute("DELETE FROM bcm_vendors WHERE id=?", (vid,))
+    db.execute("DELETE FROM bcm_vendors WHERE id=%s", (vid,))
     db.commit()
 
@@ -511,5 +511,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_vendor_assessments WHERE vendor_id=? ORDER BY assessed_on DESC",
+        "SELECT * FROM bcm_vendor_assessments WHERE vendor_id=%s ORDER BY assessed_on DESC",
         (vid,)).fetchall())
 
@@ -520,5 +520,5 @@
         """INSERT INTO bcm_vendor_assessments
            (vendor_id, assessor, score, summary, findings)
-           VALUES (?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s)""",
         (vid, data.get("assessor"), data.get("score"),
          data.get("summary"), data.get("findings")))
@@ -535,8 +535,8 @@
     if framework:
         return _dicts(db.execute(
-            "SELECT * FROM bcm_compliance_controls WHERE framework=? ORDER BY clause LIMIT ?",
+            "SELECT * FROM bcm_compliance_controls WHERE framework=%s ORDER BY clause LIMIT %s",
             (framework, limit)).fetchall())
     return _dicts(db.execute(
-        "SELECT * FROM bcm_compliance_controls ORDER BY framework, clause LIMIT ?",
+        "SELECT * FROM bcm_compliance_controls ORDER BY framework, clause LIMIT %s",
         (limit,)).fetchall())
 
@@ -544,5 +544,5 @@
 def get_compliance_control(cid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_compliance_controls WHERE id=?", (cid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_compliance_controls WHERE id=%s", (cid,)).fetchone())
 
 
@@ -552,5 +552,5 @@
         """INSERT INTO bcm_compliance_controls
            (framework, clause, title, description, status, owner, evidence_notes)
-           VALUES (?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("framework"), data.get("clause"), data.get("title"),
          data.get("description"), data.get("status", "not_started"),
@@ -578,5 +578,5 @@
 def delete_compliance_control(cid):
     db = get_db()
-    db.execute("DELETE FROM bcm_compliance_controls WHERE id=?", (cid,))
+    db.execute("DELETE FROM bcm_compliance_controls WHERE id=%s", (cid,))
     db.commit()
 
@@ -585,5 +585,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_compliance_evidence WHERE control_id=? ORDER BY created_at DESC",
+        "SELECT * FROM bcm_compliance_evidence WHERE control_id=%s ORDER BY created_at DESC",
         (control_id,)).fetchall())
 
@@ -594,5 +594,5 @@
         """INSERT INTO bcm_compliance_evidence
            (control_id, title, file_path, file_type, uploaded_by, notes)
-           VALUES (?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s)""",
         (control_id, data.get("title"), data.get("file_path"),
          data.get("file_type"), data.get("uploaded_by"), data.get("notes")))
@@ -603,5 +603,5 @@
 def delete_compliance_evidence(eid):
     db = get_db()
-    db.execute("DELETE FROM bcm_compliance_evidence WHERE id=?", (eid,))
+    db.execute("DELETE FROM bcm_compliance_evidence WHERE id=%s", (eid,))
     db.commit()
 
@@ -619,5 +619,5 @@
     try:
         row = db.execute(
-            "SELECT * FROM bcm_compliance_evidence WHERE id=?", (evidence_id,)
+            "SELECT * FROM bcm_compliance_evidence WHERE id=%s", (evidence_id,)
         ).fetchone()
         if not row:
@@ -625,5 +625,5 @@
 
         ctrl = db.execute(
-            "SELECT framework, clause, title FROM bcm_compliance_controls WHERE id=?",
+            "SELECT framework, clause, title FROM bcm_compliance_controls WHERE id=%s",
             (control_id,)
         ).fetchone()
@@ -636,5 +636,5 @@
             "INSERT INTO evidence_items "
             "(title, description, file_path, category, tags, status, uploaded_by) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (
                 row["title"] or f"BCM Evidence #{evidence_id}",
@@ -650,5 +650,5 @@
         db.execute(
             "INSERT INTO evidence_links "
-            "(evidence_id, module, entity_type, entity_id, linked_by) VALUES (?,?,?,?,?)",
+            "(evidence_id, module, entity_type, entity_id, linked_by) VALUES (%s,%s,%s,%s,%s)",
             (vault_id, "bcm", "compliance_control", control_id, user_id)
         )
@@ -670,10 +670,10 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_training_modules ORDER BY title LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_training_modules ORDER BY title LIMIT %s", (limit,)).fetchall())
 
 
 def get_training_module(mid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_training_modules WHERE id=?", (mid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_training_modules WHERE id=%s", (mid,)).fetchone())
 
 
@@ -684,5 +684,5 @@
            (title, description, category, required_roles, duration_minutes,
             owner, content, passing_score, renewal_months, status)
-           VALUES (?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("title"), data.get("description"), data.get("category"),
          data.get("required_roles"), data.get("duration_minutes"),
@@ -711,5 +711,5 @@
 def delete_training_module(mid):
     db = get_db()
-    db.execute("DELETE FROM bcm_training_modules WHERE id=?", (mid,))
+    db.execute("DELETE FROM bcm_training_modules WHERE id=%s", (mid,))
     db.commit()
 
@@ -719,9 +719,9 @@
     if module_id:
         return _dicts(db.execute(
-            "SELECT * FROM bcm_training_attestations WHERE module_id=? ORDER BY attested_at DESC",
+            "SELECT * FROM bcm_training_attestations WHERE module_id=%s ORDER BY attested_at DESC",
             (module_id,)).fetchall())
     if user_id:
         return _dicts(db.execute(
-            "SELECT * FROM bcm_training_attestations WHERE user_id=? ORDER BY attested_at DESC",
+            "SELECT * FROM bcm_training_attestations WHERE user_id=%s ORDER BY attested_at DESC",
             (user_id,)).fetchall())
     return _dicts(db.execute(
@@ -734,5 +734,5 @@
         """INSERT INTO bcm_training_attestations
            (module_id, user_id, user_name, user_email, signature, score, ip, user_agent, expires_at)
-           VALUES (?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("module_id"), data.get("user_id"), data.get("user_name"),
          data.get("user_email"), data.get("signature"), data.get("score"),
@@ -749,10 +749,10 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_documents ORDER BY created_at DESC LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_documents ORDER BY created_at DESC LIMIT %s", (limit,)).fetchall())
 
 
 def get_document(doc_id):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_documents WHERE id=?", (doc_id,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_documents WHERE id=%s", (doc_id,)).fetchone())
 
 
@@ -763,5 +763,5 @@
            (title, source_kind, filename, mime, bytes, uploaded_by, tags,
             content, chunk_count, linked_plan_id)
-           VALUES (?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("title"), data.get("source_kind"), data.get("filename"),
          data.get("mime"), data.get("bytes"), data.get("uploaded_by"),
@@ -787,6 +787,6 @@
 def delete_document(doc_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_document_chunks WHERE document_id=?", (doc_id,))
-    db.execute("DELETE FROM bcm_documents WHERE id=?", (doc_id,))
+    db.execute("DELETE FROM bcm_document_chunks WHERE document_id=%s", (doc_id,))
+    db.execute("DELETE FROM bcm_documents WHERE id=%s", (doc_id,))
     db.commit()
 
@@ -800,5 +800,5 @@
     """
     db = get_db()
-    db.execute("DELETE FROM bcm_document_chunks WHERE document_id=?", (doc_id,))
+    db.execute("DELETE FROM bcm_document_chunks WHERE document_id=%s", (doc_id,))
     for i, c in enumerate(chunks):
         if isinstance(c, dict):
@@ -808,7 +808,7 @@
             text, tokens = c
         db.execute(
-            "INSERT INTO bcm_document_chunks (document_id, chunk_index, content, token_count) VALUES (?,?,?,?)",
+            "INSERT INTO bcm_document_chunks (document_id, chunk_index, content, token_count) VALUES (%s,%s,%s,%s)",
             (doc_id, i, text, tokens))
-    db.execute("UPDATE bcm_documents SET chunk_count=? WHERE id=?", (len(chunks), doc_id))
+    db.execute("UPDATE bcm_documents SET chunk_count=%s WHERE id=%s", (len(chunks), doc_id))
     db.commit()
 
@@ -823,5 +823,5 @@
     if doc_id:
         return _dicts(db.execute(
-            "SELECT * FROM bcm_document_chunks WHERE document_id=? ORDER BY chunk_index",
+            "SELECT * FROM bcm_document_chunks WHERE document_id=%s ORDER BY chunk_index",
             (doc_id,)).fetchall())
     return []
@@ -842,5 +842,5 @@
     db = get_db()
     db.execute(
-        "INSERT INTO bcm_document_queries (user_id, question, answer, cited_chunk_ids, provider) VALUES (?,?,?,?,?)",
+        "INSERT INTO bcm_document_queries (user_id, question, answer, cited_chunk_ids, provider) VALUES (%s,%s,%s,%s,%s)",
         (user_id, question, answer, cited_ids, provider))
     db.commit()
@@ -854,10 +854,10 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_dependency_nodes ORDER BY node_type, name LIMIT ?", (limit,)).fetchall())
+        "SELECT * FROM bcm_dependency_nodes ORDER BY node_type, name LIMIT %s", (limit,)).fetchall())
 
 
 def get_dependency_node(nid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_dependency_nodes WHERE id=?", (nid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_dependency_nodes WHERE id=%s", (nid,)).fetchone())
 
 
@@ -867,5 +867,5 @@
         """INSERT INTO bcm_dependency_nodes
            (node_type, name, description, criticality, ref_table, ref_id)
-           VALUES (?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s)""",
         (data.get("node_type"), data.get("name"), data.get("description"),
          data.get("criticality"), data.get("ref_table"), data.get("ref_id")))
@@ -891,5 +891,5 @@
 def delete_dependency_node(nid):
     db = get_db()
-    db.execute("DELETE FROM bcm_dependency_nodes WHERE id=?", (nid,))
+    db.execute("DELETE FROM bcm_dependency_nodes WHERE id=%s", (nid,))
     db.commit()
 
@@ -899,5 +899,5 @@
     if source_id:
         return _dicts(db.execute(
-            "SELECT * FROM bcm_dependency_edges WHERE source_id=?", (source_id,)).fetchall())
+            "SELECT * FROM bcm_dependency_edges WHERE source_id=%s", (source_id,)).fetchall())
     return _dicts(db.execute("SELECT * FROM bcm_dependency_edges").fetchall())
 
@@ -906,5 +906,5 @@
     db = get_db()
     cur = insert_returning_id(db,
-        "INSERT INTO bcm_dependency_edges (source_id, target_id, label, strength, notes) VALUES (?,?,?,?,?)",
+        "INSERT INTO bcm_dependency_edges (source_id, target_id, label, strength, notes) VALUES (%s,%s,%s,%s,%s)",
         (data.get("source_id"), data.get("target_id"), data.get("label"),
          data.get("strength", 3), data.get("notes")))
@@ -915,5 +915,5 @@
 def delete_dependency_edge(eid):
     db = get_db()
-    db.execute("DELETE FROM bcm_dependency_edges WHERE id=?", (eid,))
+    db.execute("DELETE FROM bcm_dependency_edges WHERE id=%s", (eid,))
     db.commit()
 
@@ -942,10 +942,10 @@
             visited.add(nid)
             rows = db.execute(
-                "SELECT target_id FROM bcm_dependency_edges WHERE source_id=?", (nid,)).fetchall()
+                "SELECT target_id FROM bcm_dependency_edges WHERE source_id=%s", (nid,)).fetchall()
             for r in rows:
                 tid = r[0]
                 if tid not in visited:
                     node = _dict(db.execute(
-                        "SELECT * FROM bcm_dependency_nodes WHERE id=?", (tid,)).fetchone())
+                        "SELECT * FROM bcm_dependency_nodes WHERE id=%s", (tid,)).fetchone())
                     if node:
                         chain.append(node)
@@ -964,10 +964,10 @@
         return _dicts(db.execute(
             """SELECT l.*, p.title AS plan_title FROM bcm_bia_plan_links l
-               JOIN bcm_plans p ON p.id=l.plan_id WHERE l.bia_id=?""",
+               JOIN bcm_plans p ON p.id=l.plan_id WHERE l.bia_id=%s""",
             (bia_id,)).fetchall())
     if plan_id:
         return _dicts(db.execute(
             """SELECT l.*, b.process_name FROM bcm_bia_plan_links l
-               JOIN bcm_bia_records b ON b.id=l.bia_id WHERE l.plan_id=?""",
+               JOIN bcm_bia_records b ON b.id=l.bia_id WHERE l.plan_id=%s""",
             (plan_id,)).fetchall())
     return _dicts(db.execute("SELECT * FROM bcm_bia_plan_links").fetchall())
@@ -977,5 +977,5 @@
     db = get_db()
     cur = insert_returning_id(db,
-        "INSERT INTO bcm_bia_plan_links (bia_id, plan_id, coverage_type, notes, created_by) VALUES (?,?,?,?,?) ON CONFLICT DO NOTHING",
+        "INSERT INTO bcm_bia_plan_links (bia_id, plan_id, coverage_type, notes, created_by) VALUES (%s,%s,%s,%s,%s) ON CONFLICT DO NOTHING",
         (data.get("bia_id"), data.get("plan_id"), data.get("coverage_type", "primary"),
          data.get("notes"), data.get("created_by")))
@@ -986,5 +986,5 @@
 def delete_bia_plan_link(link_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_bia_plan_links WHERE id=?", (link_id,))
+    db.execute("DELETE FROM bcm_bia_plan_links WHERE id=%s", (link_id,))
     db.commit()
 
@@ -1007,5 +1007,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_plan_reviews WHERE plan_id=? ORDER BY created_at DESC",
+        "SELECT * FROM bcm_plan_reviews WHERE plan_id=%s ORDER BY created_at DESC",
         (plan_id,)).fetchall())
 
@@ -1018,5 +1018,5 @@
             standards, summary, strengths, gaps, recommendations,
             section_coverage, raw_response)
-           VALUES (?,?,?,?,?,?,?,?,?,?,?,?)""",
+           VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
         (data.get("plan_id"), data.get("reviewer_id"), data.get("reviewer_name"),
          data.get("provider"), data.get("overall_score"), data.get("standards"),
@@ -1035,5 +1035,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_chat_messages WHERE user_id=? ORDER BY created_at DESC LIMIT ?",
+        "SELECT * FROM bcm_chat_messages WHERE user_id=%s ORDER BY created_at DESC LIMIT %s",
         (user_id, limit)).fetchall())
 
@@ -1042,5 +1042,5 @@
     db = get_db()
     db.execute(
-        "INSERT INTO bcm_chat_messages (user_id, role, content, provider) VALUES (?,?,?,?)",
+        "INSERT INTO bcm_chat_messages (user_id, role, content, provider) VALUES (%s,%s,%s,%s)",
         (user_id, role, content, provider))
     db.commit()
@@ -1049,5 +1049,5 @@
 def clear_chat_history(user_id):
     db = get_db()
-    db.execute("DELETE FROM bcm_chat_messages WHERE user_id=?", (user_id,))
+    db.execute("DELETE FROM bcm_chat_messages WHERE user_id=%s", (user_id,))
     db.commit()
 
@@ -1061,5 +1061,5 @@
     if category and category != "all":
         return _dicts(db.execute(
-            "SELECT * FROM bcm_comm_templates WHERE category=? AND is_active=1 ORDER BY category, title",
+            "SELECT * FROM bcm_comm_templates WHERE category=%s AND is_active=1 ORDER BY category, title",
             (category,)).fetchall())
     return _dicts(db.execute(
@@ -1069,5 +1069,5 @@
 def get_comm_template(tid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_comm_templates WHERE id=?", (tid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_comm_templates WHERE id=%s", (tid,)).fetchone())
 
 
@@ -1076,5 +1076,5 @@
     cur = insert_returning_id(db,
         "INSERT INTO bcm_comm_templates (title, category, incident_types, subject, body, variables, is_active, created_by) "
-        "VALUES (?,?,?,?,?,?,?,?)",
+        "VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
         (data.get("title"), data.get("category", "general"), data.get("incident_types", ""),
          data.get("subject"), data.get("body", ""), data.get("variables", ""),
@@ -1101,5 +1101,5 @@
 def delete_comm_template(tid):
     db = get_db()
-    db.execute("DELETE FROM bcm_comm_templates WHERE id=?", (tid,))
+    db.execute("DELETE FROM bcm_comm_templates WHERE id=%s", (tid,))
     db.commit()
 
@@ -1117,5 +1117,5 @@
 def get_contact_node(nid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_contact_nodes WHERE id=?", (nid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_contact_nodes WHERE id=%s", (nid,)).fetchone())
 
 
@@ -1141,5 +1141,5 @@
     cur = insert_returning_id(db,
         "INSERT INTO bcm_contact_nodes (name, role, team, email, phone, mobile, escalation_level, parent_id, notes) "
-        "VALUES (?,?,?,?,?,?,?,?,?)",
+        "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
         (data.get("name"), data.get("role"), data.get("team"), data.get("email"),
          data.get("phone"), data.get("mobile"), data.get("escalation_level", 1),
@@ -1167,6 +1167,6 @@
     db = get_db()
     # Orphan children rather than cascade-delete the whole branch
-    db.execute("UPDATE bcm_contact_nodes SET parent_id=NULL WHERE parent_id=?", (nid,))
-    db.execute("DELETE FROM bcm_contact_nodes WHERE id=?", (nid,))
+    db.execute("UPDATE bcm_contact_nodes SET parent_id=NULL WHERE parent_id=%s", (nid,))
+    db.execute("DELETE FROM bcm_contact_nodes WHERE id=%s", (nid,))
     db.commit()
 
@@ -1180,5 +1180,5 @@
     if category and category != "all":
         return _dicts(db.execute(
-            "SELECT * FROM bcm_scenario_library WHERE category=? ORDER BY title",
+            "SELECT * FROM bcm_scenario_library WHERE category=%s ORDER BY title",
             (category,)).fetchall())
     return _dicts(db.execute(
@@ -1188,5 +1188,5 @@
 def get_scenario(sid):
     db = get_db()
-    return _dict(db.execute("SELECT * FROM bcm_scenario_library WHERE id=?", (sid,)).fetchone())
+    return _dict(db.execute("SELECT * FROM bcm_scenario_library WHERE id=%s", (sid,)).fetchone())
 
 
@@ -1195,5 +1195,5 @@
     cur = insert_returning_id(db,
         "INSERT INTO bcm_scenario_library (title, category, description, objectives, injects, "
-        "estimated_duration_minutes, difficulty, is_builtin) VALUES (?,?,?,?,?,?,?,0)",
+        "estimated_duration_minutes, difficulty, is_builtin) VALUES (%s,%s,%s,%s,%s,%s,%s,0)",
         (data.get("title"), data.get("category", "general"), data.get("description"),
          data.get("objectives"), data.get("injects"),
@@ -1206,5 +1206,5 @@
     db = get_db()
     # Only allow deleting custom scenarios (is_builtin=0)
-    db.execute("DELETE FROM bcm_scenario_library WHERE id=? AND is_builtin=0", (sid,))
+    db.execute("DELETE FROM bcm_scenario_library WHERE id=%s AND is_builtin=0", (sid,))
     db.commit()
 
@@ -1223,5 +1223,5 @@
     """
     db = get_db()
-    bia = _dict(db.execute("SELECT * FROM bcm_bia_records WHERE id=?", (bia_id,)).fetchone())
+    bia = _dict(db.execute("SELECT * FROM bcm_bia_records WHERE id=%s", (bia_id,)).fetchone())
     if not bia:
         return None
@@ -1274,5 +1274,5 @@
     calc_notes_str = " | ".join(validation + notes)
     db.execute(
-        "UPDATE bcm_bia_records SET calc_criticality=?, calc_notes=?, updated_at=? WHERE id=?",
+        "UPDATE bcm_bia_records SET calc_criticality=%s, calc_notes=%s, updated_at=%s WHERE id=%s",
         (criticality, calc_notes_str, _now(), bia_id))
     db.commit()
@@ -1306,6 +1306,6 @@
     db = get_db()
     db.execute(
-        "UPDATE bcm_plans SET is_active_plan=1, activated_at=?, activated_by=?, "
-        "activation_reason=?, updated_at=? WHERE id=?",
+        "UPDATE bcm_plans SET is_active_plan=1, activated_at=%s, activated_by=%s, "
+        "activation_reason=%s, updated_at=%s WHERE id=%s",
         (_now(), activated_by, reason, _now(), plan_id),
     )
@@ -1313,5 +1313,5 @@
         "INSERT INTO bcm_plan_activations "
         "(plan_id, action, reason, incident_id, activated_by, activated_by_id) "
-        "VALUES (?,?,?,?,?,?)",
+        "VALUES (%s,%s,%s,%s,%s,%s)",
         (plan_id, "activated", reason, incident_id, activated_by, activated_by_id),
     )
@@ -1324,5 +1324,5 @@
     db = get_db()
     db.execute(
-        "UPDATE bcm_plans SET is_active_plan=0, updated_at=? WHERE id=?",
+        "UPDATE bcm_plans SET is_active_plan=0, updated_at=%s WHERE id=%s",
         (_now(), plan_id),
     )
@@ -1330,5 +1330,5 @@
         "INSERT INTO bcm_plan_activations "
         "(plan_id, action, reason, activated_by, activated_by_id) "
-        "VALUES (?,?,?,?,?)",
+        "VALUES (%s,%s,%s,%s,%s)",
         (plan_id, "deactivated", reason, deactivated_by, deactivated_by_id),
     )
@@ -1339,5 +1339,5 @@
     db = get_db()
     return _dicts(db.execute(
-        "SELECT * FROM bcm_plan_activations WHERE plan_id=? ORDER BY created_at DESC",
+        "SELECT * FROM bcm_plan_activations WHERE plan_id=%s ORDER BY created_at DESC",
         (plan_id,),
     ).fetchall())
--- a/modules/bcm/routes.py
+++ b/modules/bcm/routes.py
@@ -348,5 +348,5 @@
             "FROM evidence_items e "
             "JOIN evidence_links el ON e.id=el.evidence_id "
-            "WHERE el.module='bcm' AND el.entity_type='plan' AND el.entity_id=? "
+            "WHERE el.module='bcm' AND el.entity_type='plan' AND el.entity_id=%s "
             "AND e.status != 'archived' ORDER BY e.created_at DESC",
             (plan_id,),
@@ -528,5 +528,5 @@
             "FROM evidence_items e "
             "JOIN evidence_links el ON e.id=el.evidence_id "
-            "WHERE el.module='bcm' AND el.entity_type='incident' AND el.entity_id=? "
+            "WHERE el.module='bcm' AND el.entity_type='incident' AND el.entity_id=%s "
             "AND e.status != 'archived' ORDER BY e.created_at DESC",
             (inc_id,),
--- a/modules/bcm/scheduler.py
+++ b/modules/bcm/scheduler.py
@@ -55,5 +55,5 @@
             db.execute(
                 "INSERT INTO notifications (user_id, module, title, message, link) "
-                "VALUES (?, ?, ?, ?, ?)",
+                "VALUES (%s, %s, %s, %s, %s)",
                 (r[0], "bcm", title, message, link),
             )
@@ -66,6 +66,6 @@
     try:
         row = db.execute(
-            "SELECT id FROM task_board WHERE module='bcm' AND entity_type=? "
-            "AND entity_id=? AND status!='done' AND title LIKE ?",
+            "SELECT id FROM task_board WHERE module='bcm' AND entity_type=%s "
+            "AND entity_id=%s AND status!='done' AND title LIKE %s",
             (entity_type, entity_id, f"%{title_fragment}%"),
         ).fetchone()
@@ -111,5 +111,5 @@
                 "INSERT INTO task_board "
                 "(title, description, module, entity_type, entity_id, priority, status) "
-                "VALUES (?,?,?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s,%s,%s)",
                 (
                     f"REVIEW DUE: {name}",
@@ -172,5 +172,5 @@
                 "INSERT INTO task_board "
                 "(title, description, module, entity_type, entity_id, priority, status) "
-                "VALUES (?,?,?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s,%s,%s)",
                 (
                     f"EXERCISE UPCOMING: {name}",
@@ -233,5 +233,5 @@
                 "INSERT INTO task_board "
                 "(title, description, module, entity_type, entity_id, priority, status) "
-                "VALUES (?,?,?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s,%s,%s)",
                 (
                     f"TRAINING DUE: {name}",
[WARN] modules/erm/data_service.py:
  line 41: f-string SQL with ? — manual review needed
  line 126: f-string SQL with ? — manual review needed
  line 136: f-string SQL with ? — manual review needed
  line 187: f-string SQL with ? — manual review needed
  line 191: f-string SQL with ? — manual review needed
  line 265: f-string SQL with ? — manual review needed
  line 288: f-string SQL with ? — manual review needed
  line 291: f-string SQL with ? — manual review needed
  line 339: f-string SQL with ? — manual review needed
  line 384: f-string SQL with ? — manual review needed
  line 387: f-string SQL with ? — manual review needed
  line 455: f-string SQL with ? — manual review needed
  line 458: f-string SQL with ? — manual review needed
  line 555: f-string SQL with ? — manual review needed
  line 573: f-string SQL with ? — manual review needed
  line 874: f-string SQL with ? — manual review needed
  line 885: f-string SQL with ? — manual review needed
  line 989: f-string SQL with ? — manual review needed
  line 1022: f-string SQL with ? — manual review needed
  line 1025: f-string SQL with ? — manual review needed
--- a/modules/erm/data_service.py
+++ b/modules/erm/data_service.py
@@ -58,5 +58,5 @@
             "SELECT e.*, u.full_name AS owner_name, 'erm' AS register_source "
             "FROM erm_enterprise_risks e LEFT JOIN users u ON u.id=e.owner_id "
-            "WHERE e.id=?", (risk_id,)
+            "WHERE e.id=%s", (risk_id,)
         ).fetchone())
     finally:
@@ -76,5 +76,5 @@
                 inherent_score, residual_score, qualitative_score,
                 risk_statement, workflow_step, response_deadline)
-               VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)""",
+               VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
             (data.get("title"), data.get("description"), data.get("category", "strategic"),
              data.get("sub_category"), data.get("likelihood", 3), data.get("impact", 3),
@@ -114,5 +114,5 @@
         existing = _dict(db.execute(
             "SELECT likelihood, impact, residual_likelihood, residual_impact "
-            "FROM erm_enterprise_risks WHERE id=?", (risk_id,)
+            "FROM erm_enterprise_risks WHERE id=%s", (risk_id,)
         ).fetchone())
         fields, vals = [], []
@@ -143,5 +143,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_enterprise_risks WHERE id=?", (risk_id,))
+        db.execute("DELETE FROM erm_enterprise_risks WHERE id=%s", (risk_id,))
         db.commit()
     finally:
@@ -168,5 +168,5 @@
     db = get_db()
     try:
-        return _dict(db.execute("SELECT * FROM erm_risk_appetite WHERE id=?", (appetite_id,)).fetchone())
+        return _dict(db.execute("SELECT * FROM erm_risk_appetite WHERE id=%s", (appetite_id,)).fetchone())
     finally:
         db.close()
@@ -178,5 +178,5 @@
     try:
         existing = db.execute(
-            "SELECT id FROM erm_risk_appetite WHERE category=?", (data.get("category"),)
+            "SELECT id FROM erm_risk_appetite WHERE category=%s", (data.get("category"),)
         ).fetchone()
         if existing:
@@ -195,5 +195,5 @@
             cur = insert_returning_id(db,
                 "INSERT INTO erm_risk_appetite (category, appetite_level, max_score, description, "
-                "tolerance_notes, approved_by, effective_date, review_date) VALUES (?,?,?,?,?,?,?,?)",
+                "tolerance_notes, approved_by, effective_date, review_date) VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
                 (data.get("category"), data.get("appetite_level", "medium"),
                  data.get("max_score", 12), data.get("description"),
@@ -210,5 +210,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_risk_appetite WHERE id=?", (appetite_id,))
+        db.execute("DELETE FROM erm_risk_appetite WHERE id=%s", (appetite_id,))
         db.commit()
     finally:
@@ -226,9 +226,9 @@
             max_risk = db.execute(
                 "SELECT MAX(likelihood*impact) FROM erm_enterprise_risks "
-                "WHERE category=? AND status NOT IN ('closed','accepted')", (cat,)
+                "WHERE category=%s AND status NOT IN ('closed','accepted')", (cat,)
             ).fetchone()[0] or 0
             count_open = db.execute(
                 "SELECT COUNT(*) FROM erm_enterprise_risks "
-                "WHERE category=? AND status NOT IN ('closed','accepted')", (cat,)
+                "WHERE category=%s AND status NOT IN ('closed','accepted')", (cat,)
             ).fetchone()[0]
             a["current_max_score"] = max_risk
@@ -238,5 +238,5 @@
             top = db.execute(
                 "SELECT id, title, (likelihood*impact) AS score FROM erm_enterprise_risks "
-                "WHERE category=? AND status NOT IN ('closed','accepted') "
+                "WHERE category=%s AND status NOT IN ('closed','accepted') "
                 "ORDER BY (likelihood*impact) DESC LIMIT 1", (cat,)
             ).fetchone()
@@ -273,5 +273,5 @@
     db = get_db()
     try:
-        return _dict(db.execute("SELECT * FROM erm_risk_library WHERE id=?", (item_id,)).fetchone())
+        return _dict(db.execute("SELECT * FROM erm_risk_library WHERE id=%s", (item_id,)).fetchone())
     finally:
         db.close()
@@ -298,5 +298,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE erm_risk_library SET is_active=0 WHERE id=?", (item_id,))
+        db.execute("UPDATE erm_risk_library SET is_active=0 WHERE id=%s", (item_id,))
         db.commit()
     finally:
@@ -310,5 +310,5 @@
             "INSERT INTO erm_risk_library (title, description, category, default_likelihood, "
             "default_impact, typical_treatment, suggested_controls, applicable_industries, "
-            "regulatory_references, tags) VALUES (?,?,?,?,?,?,?,?,?,?)",
+            "regulatory_references, tags) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("title"), data.get("description"), data.get("category"),
              data.get("default_likelihood", 3), data.get("default_impact", 3),
@@ -349,5 +349,5 @@
     db = get_db()
     try:
-        return _dict(db.execute("SELECT * FROM erm_regulatory_obligations WHERE id=?", (obl_id,)).fetchone())
+        return _dict(db.execute("SELECT * FROM erm_regulatory_obligations WHERE id=%s", (obl_id,)).fetchone())
     finally:
         db.close()
@@ -361,5 +361,5 @@
             "(regulator, regulation_name, obligation, applicable_departments, evidence_required, "
             "owner_id, due_date, status, linked_controls, linked_erm_risk_id, notes, created_by) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("regulator"), data.get("regulation_name"), data.get("obligation"),
              data.get("applicable_departments"), data.get("evidence_required"),
@@ -394,5 +394,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_regulatory_obligations WHERE id=?", (obl_id,))
+        db.execute("DELETE FROM erm_regulatory_obligations WHERE id=%s", (obl_id,))
         db.commit()
     finally:
@@ -411,5 +411,5 @@
             "(SELECT COUNT(*) FROM erm_assessment_questions WHERE assessment_id=a.id) AS question_count "
             "FROM erm_assessments a LEFT JOIN users u ON u.id=a.created_by "
-            "ORDER BY a.created_at DESC LIMIT ?", (limit,)
+            "ORDER BY a.created_at DESC LIMIT %s", (limit,)
         ).fetchall())
     finally:
@@ -420,8 +420,8 @@
     db = get_db()
     try:
-        a = _dict(db.execute("SELECT * FROM erm_assessments WHERE id=?", (assessment_id,)).fetchone())
+        a = _dict(db.execute("SELECT * FROM erm_assessments WHERE id=%s", (assessment_id,)).fetchone())
         if a:
             a["questions"] = _dicts(db.execute(
-                "SELECT * FROM erm_assessment_questions WHERE assessment_id=? ORDER BY order_idx",
+                "SELECT * FROM erm_assessment_questions WHERE assessment_id=%s ORDER BY order_idx",
                 (assessment_id,)
             ).fetchall())
@@ -436,5 +436,5 @@
         cur = insert_returning_id(db,
             "INSERT INTO erm_assessments (title, type, description, target_audience, status, due_date, created_by) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (data.get("title"), data.get("type", "risk"), data.get("description"),
              data.get("target_audience"), data.get("status", "draft"),
@@ -465,7 +465,7 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_assessment_responses WHERE assessment_id=?", (assessment_id,))
-        db.execute("DELETE FROM erm_assessment_questions WHERE assessment_id=?", (assessment_id,))
-        db.execute("DELETE FROM erm_assessments WHERE id=?", (assessment_id,))
+        db.execute("DELETE FROM erm_assessment_responses WHERE assessment_id=%s", (assessment_id,))
+        db.execute("DELETE FROM erm_assessment_questions WHERE assessment_id=%s", (assessment_id,))
+        db.execute("DELETE FROM erm_assessments WHERE id=%s", (assessment_id,))
         db.commit()
     finally:
@@ -479,5 +479,5 @@
             "INSERT INTO erm_assessment_questions "
             "(assessment_id, question, question_type, options, weight, order_idx, required) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (assessment_id, data.get("question"), data.get("question_type", "scale"),
              data.get("options"), data.get("weight", 1.0),
@@ -493,5 +493,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_assessment_questions WHERE id=?", (question_id,))
+        db.execute("DELETE FROM erm_assessment_questions WHERE id=%s", (question_id,))
         db.commit()
     finally:
@@ -504,5 +504,5 @@
         # Upsert: delete existing response for this question+respondent then insert
         db.execute(
-            "DELETE FROM erm_assessment_responses WHERE assessment_id=? AND question_id=? AND respondent_id=?",
+            "DELETE FROM erm_assessment_responses WHERE assessment_id=%s AND question_id=%s AND respondent_id=%s",
             (data.get("assessment_id"), data.get("question_id"), data.get("respondent_id")),
         )
@@ -510,5 +510,5 @@
             "INSERT INTO erm_assessment_responses "
             "(assessment_id, question_id, respondent_id, response, score, notes) "
-            "VALUES (?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s)",
             (data.get("assessment_id"), data.get("question_id"), data.get("respondent_id"),
              data.get("response"), data.get("score"), data.get("notes")),
@@ -528,5 +528,5 @@
             "JOIN erm_assessment_questions q ON q.id=r.question_id "
             "LEFT JOIN users u ON u.id=r.respondent_id "
-            "WHERE r.assessment_id=? ORDER BY r.question_id, r.submitted_at",
+            "WHERE r.assessment_id=%s ORDER BY r.question_id, r.submitted_at",
             (assessment_id,)
         ).fetchall())
@@ -652,5 +652,5 @@
             "OR event_type LIKE '%incident%' OR event_type LIKE '%escalat%' "
             "OR event_type LIKE 'erm.%' OR event_type LIKE 'orm.%' "
-            "ORDER BY created_at DESC LIMIT ?",
+            "ORDER BY created_at DESC LIMIT %s",
             (limit,)
         ).fetchall())
@@ -778,5 +778,5 @@
     try:
         rows = _dicts(db.execute(
-            "SELECT * FROM erm_chat_messages WHERE user_id=? ORDER BY created_at DESC LIMIT ?",
+            "SELECT * FROM erm_chat_messages WHERE user_id=%s ORDER BY created_at DESC LIMIT %s",
             (user_id, limit)
         ).fetchall())
@@ -791,5 +791,5 @@
     try:
         db.execute(
-            "INSERT INTO erm_chat_messages (user_id, role, content, provider) VALUES (?,?,?,?)",
+            "INSERT INTO erm_chat_messages (user_id, role, content, provider) VALUES (%s,%s,%s,%s)",
             (user_id, role, content, provider)
         )
@@ -802,5 +802,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_chat_messages WHERE user_id=?", (user_id,))
+        db.execute("DELETE FROM erm_chat_messages WHERE user_id=%s", (user_id,))
         db.commit()
     finally:
@@ -840,5 +840,5 @@
             "LEFT JOIN users u ON u.id=k.owner_id "
             "LEFT JOIN erm_enterprise_risks r ON r.id=k.linked_risk_id "
-            "WHERE k.id=?", (kri_id,)
+            "WHERE k.id=%s", (kri_id,)
         ).fetchone())
     finally:
@@ -852,5 +852,5 @@
             "INSERT INTO erm_kris (name, description, linked_risk_id, metric_type, "
             "threshold_warn, threshold_crit, current_value, unit, frequency, owner_id) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("name"), data.get("description"), data.get("linked_risk_id"),
              data.get("metric_type", "count"), data.get("threshold_warn"),
@@ -878,5 +878,5 @@
             fields.append("last_updated=?"); vals.append(_now())
             db.execute(
-                "INSERT INTO erm_kri_history (kri_id, value) VALUES (?,?)",
+                "INSERT INTO erm_kri_history (kri_id, value) VALUES (%s,%s)",
                 (kri_id, data["current_value"])
             )
@@ -892,5 +892,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_kris WHERE id=?", (kri_id,))
+        db.execute("DELETE FROM erm_kris WHERE id=%s", (kri_id,))
         db.commit()
     finally:
@@ -902,5 +902,5 @@
     try:
         return _dicts(db.execute(
-            "SELECT * FROM erm_kri_history WHERE kri_id=? ORDER BY recorded_at DESC LIMIT ?",
+            "SELECT * FROM erm_kri_history WHERE kri_id=%s ORDER BY recorded_at DESC LIMIT %s",
             (kri_id, limit)
         ).fetchall())
@@ -921,5 +921,5 @@
     try:
         risk = db.execute(
-            "SELECT workflow_step FROM erm_enterprise_risks WHERE id=?", (risk_id,)
+            "SELECT workflow_step FROM erm_enterprise_risks WHERE id=%s", (risk_id,)
         ).fetchone()
         if not risk:
@@ -930,5 +930,5 @@
         db.execute(
             "INSERT INTO erm_risk_workflow_history (risk_id, from_step, to_step, changed_by, notes) "
-            "VALUES (?,?,?,?,?)", (risk_id, from_step, to_step, user_id, notes)
+            "VALUES (%s,%s,%s,%s,%s)", (risk_id, from_step, to_step, user_id, notes)
         )
         # Keep status field in sync with meaningful workflow steps:
@@ -945,10 +945,10 @@
         if new_status:
             db.execute(
-                "UPDATE erm_enterprise_risks SET workflow_step=?, status=?, updated_at=? WHERE id=?",
+                "UPDATE erm_enterprise_risks SET workflow_step=%s, status=%s, updated_at=%s WHERE id=%s",
                 (to_step, new_status, _now(), risk_id)
             )
         else:
             db.execute(
-                "UPDATE erm_enterprise_risks SET workflow_step=?, updated_at=? WHERE id=?",
+                "UPDATE erm_enterprise_risks SET workflow_step=%s, updated_at=%s WHERE id=%s",
                 (to_step, _now(), risk_id)
             )
@@ -966,5 +966,5 @@
             "FROM erm_risk_workflow_history h "
             "LEFT JOIN users u ON u.id=h.changed_by "
-            "WHERE h.risk_id=? ORDER BY h.changed_at ASC",
+            "WHERE h.risk_id=%s ORDER BY h.changed_at ASC",
             (risk_id,)
         ).fetchall())
@@ -1004,5 +1004,5 @@
         cur = insert_returning_id(db,
             "INSERT INTO erm_risk_statements (category, cause, event, consequence, "
-            "full_statement, tags, industry) VALUES (?,?,?,?,?,?,?)",
+            "full_statement, tags, industry) VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (data.get("category"), data.get("cause"), data.get("event"),
              data.get("consequence"), full, data.get("tags"), data.get("industry"))
@@ -1032,5 +1032,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM erm_risk_statements WHERE id=?", (stmt_id,))
+        db.execute("DELETE FROM erm_risk_statements WHERE id=%s", (stmt_id,))
         db.commit()
     finally:
@@ -1043,8 +1043,8 @@
     try:
         db.execute(
-            "UPDATE erm_risk_statements SET usage_count=usage_count+1 WHERE id=?", (stmt_id,)
-        )
-        db.commit()
-        return _dict(db.execute("SELECT * FROM erm_risk_statements WHERE id=?", (stmt_id,)).fetchone())
+            "UPDATE erm_risk_statements SET usage_count=usage_count+1 WHERE id=%s", (stmt_id,)
+        )
+        db.commit()
+        return _dict(db.execute("SELECT * FROM erm_risk_statements WHERE id=%s", (stmt_id,)).fetchone())
     finally:
         db.close()
--- a/modules/erm/routes.py
+++ b/modules/erm/routes.py
@@ -143,8 +143,8 @@
     db = _get_db()
     try:
-        row = db.execute("SELECT id FROM risk_register WHERE id=?", (reg_id,)).fetchone()
+        row = db.execute("SELECT id FROM risk_register WHERE id=%s", (reg_id,)).fetchone()
         if not row:
             raise HTTPException(404, "Risk register entry not found")
-        db.execute("DELETE FROM risk_register WHERE id=?", (reg_id,))
+        db.execute("DELETE FROM risk_register WHERE id=%s", (reg_id,))
         db.commit()
         return JSONResponse({"ok": True})
[WARN] modules/evidence/routes.py:
  line 256: f-string SQL with ? — manual review needed
  line 262: f-string SQL with ? — manual review needed
  line 671: f-string SQL with ? — manual review needed
  line 725: f-string SQL with ? — manual review needed
  line 764: f-string SQL with ? — manual review needed
--- a/modules/evidence/routes.py
+++ b/modules/evidence/routes.py
@@ -67,5 +67,5 @@
             params.extend([f"%{search}%"] * 3)
         if module:
-            where.append("e.id IN (SELECT evidence_id FROM evidence_links WHERE module = ?)")
+            where.append("e.id IN (SELECT evidence_id FROM evidence_links WHERE module = %s)")
             params.append(module)
 
@@ -117,5 +117,5 @@
     try:
         existing = db.execute(
-            "SELECT id, title, status FROM evidence_items WHERE file_hash = ? AND status != 'archived'",
+            "SELECT id, title, status FROM evidence_items WHERE file_hash = %s AND status != 'archived'",
             (file_hash,),
         ).fetchone()
@@ -184,5 +184,5 @@
             if rid:
                 parent_row = db.execute(
-                    "SELECT id, version FROM evidence_items WHERE id = ? AND status != 'archived'",
+                    "SELECT id, version FROM evidence_items WHERE id = %s AND status != 'archived'",
                     (rid,),
                 ).fetchone()
@@ -192,5 +192,5 @@
                     # Mark old version as superseded
                     db.execute(
-                        "UPDATE evidence_items SET status = 'superseded', updated_at = datetime('now') WHERE id = ?",
+                        "UPDATE evidence_items SET status = 'superseded', updated_at = datetime('now') WHERE id = %s",
                         (rid,),
                     )
@@ -199,5 +199,5 @@
             "INSERT INTO evidence_items (title, description, file_path, file_name, file_size, "
             "file_hash, mime_type, category, tags, version, parent_id, uploaded_by) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 display_title, description, str(stored_name), original_name,
@@ -225,5 +225,5 @@
         item = db.execute(
             "SELECT e.*, u.full_name as uploaded_by_name FROM evidence_items e "
-            "LEFT JOIN users u ON e.uploaded_by = u.id WHERE e.id = ?", (eid,)
+            "LEFT JOIN users u ON e.uploaded_by = u.id WHERE e.id = %s", (eid,)
         ).fetchone()
         if not item:
@@ -233,5 +233,5 @@
             "SELECT el.*, u.full_name as linked_by_name FROM evidence_links el "
             "LEFT JOIN users u ON el.linked_by = u.id "
-            "WHERE el.evidence_id = ? AND el.deleted_at IS NULL "
+            "WHERE el.evidence_id = %s AND el.deleted_at IS NULL "
             "ORDER BY el.created_at DESC",
             (eid,)
@@ -273,5 +273,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE evidence_items SET status = 'archived', updated_at = datetime('now') WHERE id = ?", (eid,))
+        db.execute("UPDATE evidence_items SET status = 'archived', updated_at = datetime('now') WHERE id = %s", (eid,))
         db.commit()
     finally:
@@ -286,5 +286,5 @@
     db = get_db()
     try:
-        item = db.execute("SELECT file_path, file_name, mime_type FROM evidence_items WHERE id = ?", (eid,)).fetchone()
+        item = db.execute("SELECT file_path, file_name, mime_type FROM evidence_items WHERE id = %s", (eid,)).fetchone()
         if not item:
             raise HTTPException(404, "Evidence not found")
@@ -313,5 +313,5 @@
         while True:
             row = db.execute(
-                "SELECT parent_id FROM evidence_items WHERE id = ?", (root_id,)
+                "SELECT parent_id FROM evidence_items WHERE id = %s", (root_id,)
             ).fetchone()
             if not row or not row["parent_id"]:
@@ -333,5 +333,5 @@
             item = db.execute(
                 "SELECT id, title, version, status, file_hash, file_size, file_name, "
-                "parent_id, uploaded_by, created_at FROM evidence_items WHERE id = ?",
+                "parent_id, uploaded_by, created_at FROM evidence_items WHERE id = %s",
                 (current,),
             ).fetchone()
@@ -340,5 +340,5 @@
                 # Find children (newer versions pointing to this as parent)
                 children = db.execute(
-                    "SELECT id FROM evidence_items WHERE parent_id = ?", (current,)
+                    "SELECT id FROM evidence_items WHERE parent_id = %s", (current,)
                 ).fetchall()
                 for c in children:
@@ -360,5 +360,5 @@
     try:
         item = db.execute(
-            "SELECT file_path, file_hash, file_name, title FROM evidence_items WHERE id = ?",
+            "SELECT file_path, file_hash, file_name, title FROM evidence_items WHERE id = %s",
             (eid,),
         ).fetchone()
@@ -437,5 +437,5 @@
     try:
         db.execute(
-            "INSERT INTO evidence_links (evidence_id, module, entity_type, entity_id, linked_by) VALUES (?,?,?,?,?)",
+            "INSERT INTO evidence_links (evidence_id, module, entity_type, entity_id, linked_by) VALUES (%s,%s,%s,%s,%s)",
             (eid, module, entity_type, entity_id, user_id)
         )
@@ -449,8 +449,8 @@
             mapped_ctrls = db.execute("""
                 SELECT
-                    CASE WHEN gcm.source_control_id=? THEN gcm.target_control_id
+                    CASE WHEN gcm.source_control_id=%s THEN gcm.target_control_id
                          ELSE gcm.source_control_id END AS mapped_ctrl_id
                 FROM grid_control_mappings gcm
-                WHERE (gcm.source_control_id=? OR gcm.target_control_id=?)
+                WHERE (gcm.source_control_id=%s OR gcm.target_control_id=%s)
                   AND gcm.mapping_type='ims_equivalent'
             """, (entity_id, entity_id, entity_id)).fetchall()
@@ -460,5 +460,5 @@
                 # Don't duplicate if already linked
                 exists = db.execute(
-                    "SELECT 1 FROM evidence_links WHERE evidence_id=? AND entity_type=? AND entity_id=? AND deleted_at IS NULL",
+                    "SELECT 1 FROM evidence_links WHERE evidence_id=%s AND entity_type=%s AND entity_id=%s AND deleted_at IS NULL",
                     (eid, entity_type, mapped_id)
                 ).fetchone()
@@ -466,5 +466,5 @@
                     db.execute(
                         "INSERT INTO evidence_links "
-                        "(evidence_id, module, entity_type, entity_id, linked_by) VALUES (?,?,?,?,?) ON CONFLICT DO NOTHING",
+                        "(evidence_id, module, entity_type, entity_id, linked_by) VALUES (%s,%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                         (eid, module, entity_type, mapped_id, user_id)
                     )
@@ -476,13 +476,13 @@
             mapped_aria = db.execute("""
                 SELECT
-                    CASE WHEN m.source_control_id=? THEN m.target_control_id
+                    CASE WHEN m.source_control_id=%s THEN m.target_control_id
                          ELSE m.source_control_id END AS mapped_id
                 FROM aria_control_mappings m
-                WHERE (m.source_control_id=? OR m.target_control_id=?)
+                WHERE (m.source_control_id=%s OR m.target_control_id=%s)
                   AND m.mapping_type IN ('equivalent','ims_equivalent')
             """, (entity_id, entity_id, entity_id)).fetchall()
             for mr in mapped_aria:
                 exists = db.execute(
-                    "SELECT 1 FROM evidence_links WHERE evidence_id=? AND module='aria' AND entity_type='control' AND entity_id=? AND deleted_at IS NULL",
+                    "SELECT 1 FROM evidence_links WHERE evidence_id=%s AND module='aria' AND entity_type='control' AND entity_id=%s AND deleted_at IS NULL",
                     (eid, mr[0])
                 ).fetchone()
@@ -490,5 +490,5 @@
                     db.execute(
                         "INSERT INTO evidence_links "
-                        "(evidence_id, module, entity_type, entity_id, linked_by) VALUES (?,?,?,?,?) ON CONFLICT DO NOTHING",
+                        "(evidence_id, module, entity_type, entity_id, linked_by) VALUES (%s,%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                         (eid, "aria", "control", mr[0], user_id)
                     )
@@ -506,5 +506,5 @@
     try:
         db.execute(
-            "UPDATE evidence_links SET deleted_at = datetime('now'), deleted_by = ? WHERE id = ?",
+            "UPDATE evidence_links SET deleted_at = datetime('now'), deleted_by = %s WHERE id = %s",
             (_uid(request), lid),
         )
@@ -529,5 +529,5 @@
             "LEFT JOIN users lu ON el.linked_by = lu.id "
             "LEFT JOIN users du ON el.deleted_by = du.id "
-            "WHERE el.evidence_id = ? "
+            "WHERE el.evidence_id = %s "
             "ORDER BY el.created_at DESC",
             (eid,),
@@ -552,5 +552,5 @@
             "SELECT e.*, el.id as link_id FROM evidence_items e "
             "JOIN evidence_links el ON e.id = el.evidence_id "
-            "WHERE el.module = ? AND el.entity_type = ? AND el.entity_id = ? AND e.status != 'archived' "
+            "WHERE el.module = %s AND el.entity_type = %s AND el.entity_id = %s AND e.status != 'archived' "
             "ORDER BY e.updated_at DESC",
             (module, entity_type, int(entity_id))
@@ -583,5 +583,5 @@
             "FROM evidence_items e "
             "JOIN evidence_links el ON e.id = el.evidence_id "
-            "WHERE el.module = ? AND el.entity_type = ? AND el.entity_id = ? "
+            "WHERE el.module = %s AND el.entity_type = %s AND el.entity_id = %s "
             "AND e.tags LIKE '%auto%' AND e.status != 'archived' "
             "ORDER BY e.created_at DESC",
--- a/modules/evidence/scheduler.py
+++ b/modules/evidence/scheduler.py
@@ -46,5 +46,5 @@
             db.execute(
                 "INSERT INTO notifications (user_id, module, title, message, link) "
-                "VALUES (?, 'evidence', ?, ?, '/evidence/')",
+                "VALUES (%s, 'evidence', %s, %s, '/evidence/')",
                 (r[0], title, message),
             )
@@ -57,5 +57,5 @@
         row = db.execute(
             "SELECT id FROM task_board WHERE module='evidence' AND entity_type='evidence_item' "
-            "AND entity_id=? AND status!='done' AND title LIKE ?",
+            "AND entity_id=%s AND status!='done' AND title LIKE %s",
             (evidence_id, f"%EXPIRY%{days_label}%"),
         ).fetchone()
@@ -91,5 +91,5 @@
                 "LEFT JOIN users u ON e.uploaded_by = u.id "
                 "WHERE e.status = 'current' "
-                "  AND e.expiry_date = date('now', ? || ' days') "
+                "  AND e.expiry_date = date('now', %s || ' days') "
                 "ORDER BY e.title",
                 (f"+{days}",)
@@ -108,5 +108,5 @@
                     "INSERT INTO task_board "
                     "(title, description, module, entity_type, entity_id, priority, status) "
-                    "VALUES (?,?,?,?,?,?,?)",
+                    "VALUES (%s,%s,%s,%s,%s,%s,%s)",
                     (
                         f"EVIDENCE EXPIRY ({label}): {title}",
[WARN] modules/grid/data_service.py:
  line 311: f-string SQL with ? — manual review needed
  line 314: f-string SQL with ? — manual review needed
  line 431: f-string SQL with ? — manual review needed
  line 434: f-string SQL with ? — manual review needed
  line 811: f-string SQL with ? — manual review needed
  line 825: f-string SQL with ? — manual review needed
  line 858: f-string SQL with ? — manual review needed
  line 889: f-string SQL with ? — manual review needed
  line 931: f-string SQL with ? — manual review needed
  line 1078: f-string SQL with ? — manual review needed
  line 1081: f-string SQL with ? — manual review needed
  line 1432: f-string SQL with ? — manual review needed
  line 1435: f-string SQL with ? — manual review needed
--- a/modules/grid/data_service.py
+++ b/modules/grid/data_service.py
@@ -30,5 +30,5 @@
     """
     row = db.execute(
-        "SELECT id FROM frameworks WHERE name=?", (grid_fw_name,)
+        "SELECT id FROM frameworks WHERE name=%s", (grid_fw_name,)
     ).fetchone()
     if row:
@@ -76,5 +76,5 @@
                 db.execute(
                     "INSERT INTO grid_frameworks (name, description, color, type) "
-                    "VALUES (?,?,?,?)", (name, desc, color, ftype),
+                    "VALUES (%s,%s,%s,%s)", (name, desc, color, ftype),
                 )
             db.commit()
@@ -97,5 +97,5 @@
                     db.execute(
                         "INSERT INTO grid_frameworks (name, description, color, type) "
-                        "VALUES (?,?,?,?)",
+                        "VALUES (%s,%s,%s,%s)",
                         (row[0], row[1], row[2] or "#4f8ef7", row[3] or "Security"),
                     )
@@ -118,5 +118,5 @@
     db = get_db()
     try:
-        cur = insert_returning_id(db,"INSERT INTO grid_frameworks (name, description, color, type) VALUES (?,?,?,?)",
+        cur = insert_returning_id(db,"INSERT INTO grid_frameworks (name, description, color, type) VALUES (%s,%s,%s,%s)",
             (data.get("name", "Unnamed"), data.get("description", ""), data.get("color", "#4f8ef7"), data.get("type", "Security")))
         db.commit()
@@ -128,5 +128,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE grid_frameworks SET active=0 WHERE id=?", (fid,))
+        db.execute("UPDATE grid_frameworks SET active=0 WHERE id=%s", (fid,))
         db.commit()
     finally:
@@ -156,5 +156,5 @@
             SELECT a.*, f.name AS framework_name, f.color AS framework_color
             FROM grid_audits a LEFT JOIN grid_frameworks f ON a.framework_id=f.id
-            WHERE a.id=?""", (aid,)).fetchone())
+            WHERE a.id=%s""", (aid,)).fetchone())
         if not audit:
             return None
@@ -164,6 +164,6 @@
                    (SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=c.id) AS evidence_uploaded
             FROM grid_controls c LEFT JOIN users u ON c.assignee_id=u.id
-            WHERE c.audit_id=? ORDER BY c.control_id""", (aid,)).fetchall())
-        timeline = _dicts(db.execute("SELECT * FROM grid_timeline WHERE audit_id=? ORDER BY date", (aid,)).fetchall())
+            WHERE c.audit_id=%s ORDER BY c.control_id""", (aid,)).fetchall())
+        timeline = _dicts(db.execute("SELECT * FROM grid_timeline WHERE audit_id=%s ORDER BY date", (aid,)).fetchall())
         audit["controls"] = controls
         audit["timeline"] = timeline
@@ -187,5 +187,5 @@
         aid = insert_returning_id(db,
             "INSERT INTO grid_audits (name, framework_id, audit_type, auditor, lead_id, start_date, end_date, audit_date,"
-            " scope, objective, criteria, methodology, is_integrated, framework_ids) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)",
+            " scope, objective, criteria, methodology, is_integrated, framework_ids) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("name", "Untitled Audit"), data.get("framework_id"), data.get("audit_type", "External"),
              data.get("auditor"), data.get("lead_id"), data.get("start_date"), data.get("end_date"),
@@ -198,5 +198,5 @@
         # Helper: insert controls from one grid_framework into this audit
         def _insert_controls_for_grid_fw(gfid):
-            gf = db.execute("SELECT name FROM grid_frameworks WHERE id=?", (gfid,)).fetchone()
+            gf = db.execute("SELECT name FROM grid_frameworks WHERE id=%s", (gfid,)).fetchone()
             if not gf:
                 return []
@@ -206,5 +206,5 @@
             unified_ctrls = db.execute(
                 "SELECT ref, name, description, priority "
-                "FROM controls WHERE framework_id=? ORDER BY ref",
+                "FROM controls WHERE framework_id=%s ORDER BY ref",
                 (uf_id,),
             ).fetchall()
@@ -236,9 +236,9 @@
                     # Check if these two unified controls are mapped
                     ctrl_a = db.execute(
-                        "SELECT id FROM controls WHERE framework_id=? AND ref=?",
+                        "SELECT id FROM controls WHERE framework_id=%s AND ref=%s",
                         (uf_id_a, ref_a)
                     ).fetchone()
                     ctrl_b = db.execute(
-                        "SELECT id FROM controls WHERE framework_id=? AND ref=?",
+                        "SELECT id FROM controls WHERE framework_id=%s AND ref=%s",
                         (uf_id_b, ref_b)
                     ).fetchone()
@@ -247,6 +247,6 @@
                     is_mapped = db.execute(
                         "SELECT 1 FROM aria_control_mappings "
-                        "WHERE (source_control_id=? AND target_control_id=?) "
-                        "   OR (source_control_id=? AND target_control_id=?) LIMIT 1",
+                        "WHERE (source_control_id=%s AND target_control_id=%s) "
+                        "   OR (source_control_id=%s AND target_control_id=%s) LIMIT 1",
                         (ctrl_a[0], ctrl_b[0], ctrl_b[0], ctrl_a[0])
                     ).fetchone()
@@ -255,5 +255,5 @@
                             "INSERT INTO grid_control_mappings "
                             "(source_control_id, target_control_id, mapping_type, confidence) "
-                            "VALUES (?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                            "VALUES (%s, %s, %s, %s) ON CONFLICT DO NOTHING",
                             (gc_id_a, gc_id_b, "ims_equivalent", 1.0),
                         )
@@ -271,5 +271,5 @@
                                   ("Internal review deadline", mid), ("Auditor evidence submission", end - timedelta(days=14)),
                                   ("External audit date", end)]:
-                    db.execute("INSERT INTO grid_timeline (audit_id, title, date, status) VALUES (?,?,?,?)",
+                    db.execute("INSERT INTO grid_timeline (audit_id, title, date, status) VALUES (%s,%s,%s,%s)",
                                (aid, title, dt.strftime("%Y-%m-%d"), "Pending"))
             except ValueError:
@@ -286,5 +286,5 @@
         db = get_db()
     try:
-        row = db.execute("SELECT is_locked FROM grid_audits WHERE id=?", (audit_id,)).fetchone()
+        row = db.execute("SELECT is_locked FROM grid_audits WHERE id=%s", (audit_id,)).fetchone()
         return bool(row and row[0])
     finally:
@@ -320,21 +320,21 @@
     db = get_db()
     try:
-        cids = [r[0] for r in db.execute("SELECT id FROM grid_controls WHERE audit_id=?", (aid,)).fetchall()]
+        cids = [r[0] for r in db.execute("SELECT id FROM grid_controls WHERE audit_id=%s", (aid,)).fetchall()]
         for cid in cids:
-            eids = [r[0] for r in db.execute("SELECT id FROM grid_evidence_files WHERE control_id=?", (cid,)).fetchall()]
+            eids = [r[0] for r in db.execute("SELECT id FROM grid_evidence_files WHERE control_id=%s", (cid,)).fetchall()]
             for eid in eids:
-                db.execute("DELETE FROM grid_approvals WHERE evidence_id=?", (eid,))
-            db.execute("DELETE FROM grid_evidence_files WHERE control_id=?", (cid,))
-            db.execute("DELETE FROM grid_evidence_items WHERE control_id=?", (cid,))
-            db.execute("DELETE FROM grid_control_comments WHERE control_id=?", (cid,))
-            db.execute("DELETE FROM grid_reminders WHERE control_id=?", (cid,))
-            db.execute("DELETE FROM grid_control_mappings WHERE source_control_id=? OR target_control_id=?", (cid, cid))
-        db.execute("DELETE FROM grid_controls WHERE audit_id=?", (aid,))
-        db.execute("DELETE FROM grid_timeline WHERE audit_id=?", (aid,))
-        db.execute("DELETE FROM grid_non_conformances WHERE audit_id=?", (aid,))
-        db.execute("DELETE FROM grid_reminders WHERE audit_id=?", (aid,))
-        db.execute("DELETE FROM grid_share_links WHERE audit_id=?", (aid,))
-        db.execute("DELETE FROM grid_compliance_scores WHERE audit_id=?", (aid,))
-        db.execute("DELETE FROM grid_audits WHERE id=?", (aid,))
+                db.execute("DELETE FROM grid_approvals WHERE evidence_id=%s", (eid,))
+            db.execute("DELETE FROM grid_evidence_files WHERE control_id=%s", (cid,))
+            db.execute("DELETE FROM grid_evidence_items WHERE control_id=%s", (cid,))
+            db.execute("DELETE FROM grid_control_comments WHERE control_id=%s", (cid,))
+            db.execute("DELETE FROM grid_reminders WHERE control_id=%s", (cid,))
+            db.execute("DELETE FROM grid_control_mappings WHERE source_control_id=%s OR target_control_id=%s", (cid, cid))
+        db.execute("DELETE FROM grid_controls WHERE audit_id=%s", (aid,))
+        db.execute("DELETE FROM grid_timeline WHERE audit_id=%s", (aid,))
+        db.execute("DELETE FROM grid_non_conformances WHERE audit_id=%s", (aid,))
+        db.execute("DELETE FROM grid_reminders WHERE audit_id=%s", (aid,))
+        db.execute("DELETE FROM grid_share_links WHERE audit_id=%s", (aid,))
+        db.execute("DELETE FROM grid_compliance_scores WHERE audit_id=%s", (aid,))
+        db.execute("DELETE FROM grid_audits WHERE id=%s", (aid,))
         db.commit()
     finally:
@@ -368,15 +368,15 @@
                    (SELECT COUNT(*) FROM grid_evidence_items WHERE control_id=c.id) AS evidence_total,
                    (SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=c.id) AS evidence_uploaded
-            FROM grid_controls c LEFT JOIN users u ON c.assignee_id=u.id WHERE c.id=?""", (cid,)).fetchone())
+            FROM grid_controls c LEFT JOIN users u ON c.assignee_id=u.id WHERE c.id=%s""", (cid,)).fetchone())
         if not ctrl:
             return None
-        ctrl["evidence_items"] = _dicts(db.execute("SELECT * FROM grid_evidence_items WHERE control_id=?", (cid,)).fetchall())
+        ctrl["evidence_items"] = _dicts(db.execute("SELECT * FROM grid_evidence_items WHERE control_id=%s", (cid,)).fetchall())
         ctrl["evidence_files"] = _dicts(db.execute(
             "SELECT ef.*, u.full_name AS uploader_name FROM grid_evidence_files ef "
-            "LEFT JOIN users u ON ef.uploaded_by=u.id WHERE ef.control_id=? ORDER BY ef.created_at DESC", (cid,)).fetchall())
+            "LEFT JOIN users u ON ef.uploaded_by=u.id WHERE ef.control_id=%s ORDER BY ef.created_at DESC", (cid,)).fetchall())
         ctrl["comments"] = _dicts(db.execute(
             "SELECT cc.*, u.full_name AS user_name FROM grid_control_comments cc "
-            "LEFT JOIN users u ON cc.user_id=u.id WHERE cc.control_id=? ORDER BY cc.created_at DESC", (cid,)).fetchall())
-        ctrl["reminders"] = _dicts(db.execute("SELECT * FROM grid_reminders WHERE control_id=?", (cid,)).fetchall())
+            "LEFT JOIN users u ON cc.user_id=u.id WHERE cc.control_id=%s ORDER BY cc.created_at DESC", (cid,)).fetchall())
+        ctrl["reminders"] = _dicts(db.execute("SELECT * FROM grid_reminders WHERE control_id=%s", (cid,)).fetchall())
         return ctrl
     finally:
@@ -387,5 +387,5 @@
     try:
         cid = insert_returning_id(db,
-            "INSERT INTO grid_controls (audit_id, framework_id, control_id, name, description, risk_level, assignee_id, due_date) VALUES (?,?,?,?,?,?,?,?)",
+            "INSERT INTO grid_controls (audit_id, framework_id, control_id, name, description, risk_level, assignee_id, due_date) VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("audit_id"), data.get("framework_id"), data.get("control_id", ""),
              data.get("name", "Untitled Control"), data.get("description", ""),
@@ -396,5 +396,5 @@
         for ev_name in ev_req:
             if ev_name:
-                db.execute("INSERT INTO grid_evidence_items (control_id, name) VALUES (?,?)", (cid, ev_name))
+                db.execute("INSERT INTO grid_evidence_items (control_id, name) VALUES (%s,%s)", (cid, ev_name))
         db.commit()
         return cid
@@ -408,9 +408,9 @@
         for c in controls:
             cid = insert_returning_id(db,
-                "INSERT INTO grid_controls (audit_id, framework_id, control_id, name, description, risk_level) VALUES (?,?,?,?,?,?)",
+                "INSERT INTO grid_controls (audit_id, framework_id, control_id, name, description, risk_level) VALUES (%s,%s,%s,%s,%s,%s)",
                 (audit_id, framework_id, c.get("control_id", ""), c.get("name", ""), c.get("description", ""), c.get("risk_level", "Medium")))
             for ev in c.get("evidence_required", []):
                 if ev:
-                    db.execute("INSERT INTO grid_evidence_items (control_id, name) VALUES (?,?)", (cid, ev))
+                    db.execute("INSERT INTO grid_evidence_items (control_id, name) VALUES (%s,%s)", (cid, ev))
             created.append(cid)
         db.commit()
@@ -423,5 +423,5 @@
     try:
         # Check if parent audit is locked
-        row = db.execute("SELECT audit_id FROM grid_controls WHERE id=?", (cid,)).fetchone()
+        row = db.execute("SELECT audit_id FROM grid_controls WHERE id=%s", (cid,)).fetchone()
         if row:
             _assert_not_locked(row[0], db)
@@ -451,12 +451,12 @@
 
     items_required = db.execute(
-        "SELECT COUNT(*) FROM grid_evidence_items WHERE control_id=?", (cid,)
+        "SELECT COUNT(*) FROM grid_evidence_items WHERE control_id=%s", (cid,)
     ).fetchone()[0]
     files_total = db.execute(
-        "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=?", (cid,)
+        "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=%s", (cid,)
     ).fetchone()[0]
     files_approved = db.execute(
         "SELECT COUNT(*) FROM grid_evidence_files "
-        "WHERE control_id=? AND status IN ('approved', 'Approved')", (cid,)
+        "WHERE control_id=%s AND status IN ('approved', 'Approved')", (cid,)
     ).fetchone()[0]
 
@@ -466,20 +466,20 @@
             "SELECT COUNT(DISTINCT ei.id) FROM grid_evidence_items ei "
             "JOIN grid_evidence_files ef ON ef.evidence_item_id = ei.id "
-            "WHERE ei.control_id = ? AND ef.status IN ('approved', 'Approved')",
+            "WHERE ei.control_id = %s AND ef.status IN ('approved', 'Approved')",
             (cid,),
         ).fetchone()[0]
         if items_with_approval >= items_required:
-            db.execute("UPDATE grid_controls SET status='Complete' WHERE id=? AND status != 'Complete'", (cid,))
+            db.execute("UPDATE grid_controls SET status='Complete' WHERE id=%s AND status != 'Complete'", (cid,))
             db.commit()
         elif files_total > 0:
-            db.execute("UPDATE grid_controls SET status='In Progress' WHERE id=? AND status NOT IN ('Complete','In Progress')", (cid,))
+            db.execute("UPDATE grid_controls SET status='In Progress' WHERE id=%s AND status NOT IN ('Complete','In Progress')", (cid,))
             db.commit()
     else:
         # No explicit items — all files must be approved
         if files_total > 0 and files_approved >= files_total:
-            db.execute("UPDATE grid_controls SET status='Complete' WHERE id=? AND status != 'Complete'", (cid,))
+            db.execute("UPDATE grid_controls SET status='Complete' WHERE id=%s AND status != 'Complete'", (cid,))
             db.commit()
         elif files_total > 0:
-            db.execute("UPDATE grid_controls SET status='In Progress' WHERE id=? AND status NOT IN ('Complete','In Progress')", (cid,))
+            db.execute("UPDATE grid_controls SET status='In Progress' WHERE id=%s AND status NOT IN ('Complete','In Progress')", (cid,))
             db.commit()
 
@@ -487,13 +487,13 @@
     db = get_db()
     try:
-        eids = [r[0] for r in db.execute("SELECT id FROM grid_evidence_files WHERE control_id=?", (cid,)).fetchall()]
+        eids = [r[0] for r in db.execute("SELECT id FROM grid_evidence_files WHERE control_id=%s", (cid,)).fetchall()]
         for eid in eids:
-            db.execute("DELETE FROM grid_approvals WHERE evidence_id=?", (eid,))
-        db.execute("DELETE FROM grid_evidence_files WHERE control_id=?", (cid,))
-        db.execute("DELETE FROM grid_evidence_items WHERE control_id=?", (cid,))
-        db.execute("DELETE FROM grid_control_comments WHERE control_id=?", (cid,))
-        db.execute("DELETE FROM grid_reminders WHERE control_id=?", (cid,))
-        db.execute("DELETE FROM grid_control_mappings WHERE source_control_id=? OR target_control_id=?", (cid, cid))
-        db.execute("DELETE FROM grid_controls WHERE id=?", (cid,))
+            db.execute("DELETE FROM grid_approvals WHERE evidence_id=%s", (eid,))
+        db.execute("DELETE FROM grid_evidence_files WHERE control_id=%s", (cid,))
+        db.execute("DELETE FROM grid_evidence_items WHERE control_id=%s", (cid,))
+        db.execute("DELETE FROM grid_control_comments WHERE control_id=%s", (cid,))
+        db.execute("DELETE FROM grid_reminders WHERE control_id=%s", (cid,))
+        db.execute("DELETE FROM grid_control_mappings WHERE source_control_id=%s OR target_control_id=%s", (cid, cid))
+        db.execute("DELETE FROM grid_controls WHERE id=%s", (cid,))
         db.commit()
     finally:
@@ -504,8 +504,8 @@
     db = get_db()
     try:
-        items = _dicts(db.execute("SELECT * FROM grid_evidence_items WHERE control_id=?", (control_id,)).fetchall())
+        items = _dicts(db.execute("SELECT * FROM grid_evidence_items WHERE control_id=%s", (control_id,)).fetchall())
         files = _dicts(db.execute(
             "SELECT ef.*, u.full_name AS uploader_name FROM grid_evidence_files ef "
-            "LEFT JOIN users u ON ef.uploaded_by=u.id WHERE ef.control_id=? ORDER BY ef.created_at DESC", (control_id,)).fetchall())
+            "LEFT JOIN users u ON ef.uploaded_by=u.id WHERE ef.control_id=%s ORDER BY ef.created_at DESC", (control_id,)).fetchall())
         # Annotate items with their file counts and approval status
         for item in items:
@@ -528,5 +528,5 @@
         # Fetch items
         items = _dicts(db.execute(
-            "SELECT * FROM grid_evidence_items WHERE control_id=? ORDER BY id", (control_id,)
+            "SELECT * FROM grid_evidence_items WHERE control_id=%s ORDER BY id", (control_id,)
         ).fetchall())
         if not items:
@@ -564,5 +564,5 @@
     try:
         cur = insert_returning_id(db,
-            "INSERT INTO grid_evidence_items (control_id, name, description, required) VALUES (?,?,?,?)",
+            "INSERT INTO grid_evidence_items (control_id, name, description, required) VALUES (%s,%s,%s,%s)",
             (control_id, name, description, required),
         )
@@ -578,8 +578,8 @@
         # Un-map any files that were linked to this item (don't delete the files)
         db.execute(
-            "UPDATE grid_evidence_files SET evidence_item_id=NULL WHERE evidence_item_id=?",
+            "UPDATE grid_evidence_files SET evidence_item_id=NULL WHERE evidence_item_id=%s",
             (item_id,),
         )
-        db.execute("DELETE FROM grid_evidence_items WHERE id=?", (item_id,))
+        db.execute("DELETE FROM grid_evidence_items WHERE id=%s", (item_id,))
         db.commit()
     finally:
@@ -594,5 +594,5 @@
             # ── Version-up: archive the current row, then update in-place ───
             existing = _dict(db.execute(
-                "SELECT * FROM grid_evidence_files WHERE id=?", (replace_id,)
+                "SELECT * FROM grid_evidence_files WHERE id=%s", (replace_id,)
             ).fetchone())
             if existing:
@@ -601,5 +601,5 @@
                     "(evidence_id, version, filename, original_name, file_path, "
                     "file_size, mime_type, uploaded_by, notes, created_at) "
-                    "VALUES (?,?,?,?,?,?,?,?,?,?)",
+                    "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
                     (replace_id, existing["version"], existing["filename"],
                      existing["original_name"], existing["file_path"],
@@ -611,9 +611,9 @@
                 db.execute(
                     "UPDATE grid_evidence_files SET "
-                    "filename=?, original_name=?, file_path=?, file_size=?, "
-                    "mime_type=?, uploaded_by=?, notes=?, version=?, "
+                    "filename=%s, original_name=%s, file_path=%s, file_size=%s, "
+                    "mime_type=%s, uploaded_by=%s, notes=%s, version=%s, "
                     "status='Uploaded', approved_by=NULL, approved_at=NULL, "
-                    "expiry_notified=0, expires_at=?, created_at=datetime('now') "
-                    "WHERE id=?",
+                    "expiry_notified=0, expires_at=%s, created_at=datetime('now') "
+                    "WHERE id=%s",
                     (data["filename"], data["original_name"], data["file_path"],
                      data.get("file_size", 0), data.get("mime_type", ""),
@@ -628,5 +628,5 @@
         fid = insert_returning_id(db,
             "INSERT INTO grid_evidence_files (control_id, evidence_item_id, filename, original_name, "
-            "file_path, file_size, mime_type, uploaded_by, notes, expires_at) VALUES (?,?,?,?,?,?,?,?,?,?)",
+            "file_path, file_size, mime_type, uploaded_by, notes, expires_at) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data["control_id"], data.get("evidence_item_id"), data["filename"],
              data["original_name"], data["file_path"], data.get("file_size", 0),
@@ -652,5 +652,5 @@
             "FROM grid_evidence_versions ev "
             "LEFT JOIN users u ON ev.uploaded_by = u.id "
-            "WHERE ev.evidence_id = ? ORDER BY ev.version ASC",
+            "WHERE ev.evidence_id = %s ORDER BY ev.version ASC",
             (evidence_id,),
         ).fetchall()
@@ -662,5 +662,5 @@
     db = get_db()
     try:
-        return _dict(db.execute("SELECT * FROM grid_evidence_files WHERE id=?", (eid,)).fetchone())
+        return _dict(db.execute("SELECT * FROM grid_evidence_files WHERE id=%s", (eid,)).fetchone())
     finally:
         db.close()
@@ -669,7 +669,7 @@
     db = get_db()
     try:
-        db.execute("UPDATE grid_evidence_files SET status=?, approved_by=?, approved_at=datetime('now') WHERE id=?", (status, approved_by, eid))
-        db.commit()
-        ef = _dict(db.execute("SELECT control_id FROM grid_evidence_files WHERE id=?", (eid,)).fetchone())
+        db.execute("UPDATE grid_evidence_files SET status=%s, approved_by=%s, approved_at=datetime('now') WHERE id=%s", (status, approved_by, eid))
+        db.commit()
+        ef = _dict(db.execute("SELECT control_id FROM grid_evidence_files WHERE id=%s", (eid,)).fetchone())
         if ef:
             _auto_status_control(db, ef["control_id"])
@@ -680,6 +680,6 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM grid_approvals WHERE evidence_id=?", (eid,))
-        db.execute("DELETE FROM grid_evidence_files WHERE id=?", (eid,))
+        db.execute("DELETE FROM grid_approvals WHERE evidence_id=%s", (eid,))
+        db.execute("DELETE FROM grid_evidence_files WHERE id=%s", (eid,))
         db.commit()
     finally:
@@ -690,5 +690,5 @@
     db = get_db()
     try:
-        cur = insert_returning_id(db,"INSERT INTO grid_control_comments (control_id, user_id, content) VALUES (?,?,?)", (control_id, user_id, content))
+        cur = insert_returning_id(db,"INSERT INTO grid_control_comments (control_id, user_id, content) VALUES (%s,%s,%s)", (control_id, user_id, content))
         db.commit()
         return cur
@@ -700,5 +700,5 @@
     db = get_db()
     try:
-        cur = insert_returning_id(db,"INSERT INTO grid_reminders (control_id, audit_id, user_id, frequency) VALUES (?,?,?,?)",
+        cur = insert_returning_id(db,"INSERT INTO grid_reminders (control_id, audit_id, user_id, frequency) VALUES (%s,%s,%s,%s)",
             (data.get("control_id"), data.get("audit_id"), data["user_id"], data.get("frequency", "weekly")))
         db.commit()
@@ -713,5 +713,5 @@
             SELECT c.*, u.full_name AS assignee_name, u.email AS assignee_email
             FROM grid_controls c LEFT JOIN users u ON c.assignee_id=u.id
-            WHERE c.audit_id=? AND c.status!='Complete' AND u.email IS NOT NULL
+            WHERE c.audit_id=%s AND c.status!='Complete' AND u.email IS NOT NULL
         """, (audit_id,)).fetchall())
     finally:
@@ -773,5 +773,5 @@
             "LEFT JOIN grid_controls c ON nc.control_id=c.id "
             "LEFT JOIN grid_audits a ON nc.audit_id=a.id "
-            "WHERE nc.id=?", (ncid,)).fetchone())
+            "WHERE nc.id=%s", (ncid,)).fetchone())
         return nc
     finally:
@@ -785,5 +785,5 @@
             "INSERT INTO grid_non_conformances "
             "(audit_id, control_id, title, description, severity, assigned_to, due_date, cap_status) "
-            "VALUES (?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
             (data["audit_id"], data.get("control_id"),
              data.get("title", "Untitled NC"), data.get("description", ""),
@@ -799,5 +799,5 @@
     try:
         # Check if parent audit is locked
-        row = db.execute("SELECT audit_id FROM grid_non_conformances WHERE id=?", (ncid,)).fetchone()
+        row = db.execute("SELECT audit_id FROM grid_non_conformances WHERE id=%s", (ncid,)).fetchone()
         if row:
             _assert_not_locked(row[0], db)
@@ -835,5 +835,5 @@
     try:
         row = db.execute(
-            "SELECT cap_status FROM grid_non_conformances WHERE id=?", (ncid,)
+            "SELECT cap_status FROM grid_non_conformances WHERE id=%s", (ncid,)
         ).fetchone()
         if not row:
@@ -868,5 +868,5 @@
     try:
         row = db.execute(
-            "SELECT cap_status FROM grid_non_conformances WHERE id=?", (ncid,)
+            "SELECT cap_status FROM grid_non_conformances WHERE id=%s", (ncid,)
         ).fetchone()
         if not row:
@@ -906,5 +906,5 @@
     try:
         row = db.execute(
-            "SELECT cap_status FROM grid_non_conformances WHERE id=?", (ncid,)
+            "SELECT cap_status FROM grid_non_conformances WHERE id=%s", (ncid,)
         ).fetchone()
         if not row:
@@ -934,5 +934,5 @@
         # Return the new cap_status
         new = db.execute(
-            "SELECT cap_status FROM grid_non_conformances WHERE id=?", (ncid,)
+            "SELECT cap_status FROM grid_non_conformances WHERE id=%s", (ncid,)
         ).fetchone()
         return new[0] if new else None
@@ -944,5 +944,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM grid_non_conformances WHERE id=?", (ncid,))
+        db.execute("DELETE FROM grid_non_conformances WHERE id=%s", (ncid,))
         db.commit()
     finally:
@@ -966,5 +966,5 @@
             "LEFT JOIN users u ON lnk.linked_by = u.id "
             "LEFT JOIN users up ON ef.uploaded_by = up.id "
-            "WHERE lnk.nc_id=? "
+            "WHERE lnk.nc_id=%s "
             "ORDER BY lnk.linked_at DESC",
             (ncid,)
@@ -995,5 +995,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM grid_nc_evidence WHERE id=?", (link_id,))
+        db.execute("DELETE FROM grid_nc_evidence WHERE id=%s", (link_id,))
         db.commit()
     finally:
@@ -1011,6 +1011,6 @@
             "JOIN grid_controls c ON ef.control_id = c.id "
             "JOIN grid_non_conformances nc ON nc.audit_id = c.audit_id "
-            "WHERE nc.id=? "
-            "AND ef.id NOT IN (SELECT evidence_file_id FROM grid_nc_evidence WHERE nc_id=?) "
+            "WHERE nc.id=%s "
+            "AND ef.id NOT IN (SELECT evidence_file_id FROM grid_nc_evidence WHERE nc_id=%s) "
             "ORDER BY ef.original_name",
             (ncid, ncid)
@@ -1033,5 +1033,5 @@
         vendors = _dicts(db.execute(q, params).fetchall())
         for v in vendors:
-            latest = _dict(db.execute("SELECT * FROM grid_vendor_assessments WHERE vendor_id=? ORDER BY assessment_date DESC LIMIT 1", (v["id"],)).fetchone())
+            latest = _dict(db.execute("SELECT * FROM grid_vendor_assessments WHERE vendor_id=%s ORDER BY assessment_date DESC LIMIT 1", (v["id"],)).fetchone())
             v["latest_assessment"] = latest
         return vendors
@@ -1042,7 +1042,7 @@
     db = get_db()
     try:
-        v = _dict(db.execute("SELECT * FROM grid_vendors WHERE id=?", (vid,)).fetchone())
+        v = _dict(db.execute("SELECT * FROM grid_vendors WHERE id=%s", (vid,)).fetchone())
         if v:
-            v["assessments"] = _dicts(db.execute("SELECT * FROM grid_vendor_assessments WHERE vendor_id=? ORDER BY assessment_date DESC", (vid,)).fetchall())
+            v["assessments"] = _dicts(db.execute("SELECT * FROM grid_vendor_assessments WHERE vendor_id=%s ORDER BY assessment_date DESC", (vid,)).fetchall())
         return v
     finally:
@@ -1061,5 +1061,5 @@
                 pass
         cur = insert_returning_id(db,
-            "INSERT INTO grid_vendors (name, contact_name, contact_email, services, risk_level, status, frameworks, contract_expiry, notes, canonical_id) VALUES (?,?,?,?,?,?,?,?,?,?)",
+            "INSERT INTO grid_vendors (name, contact_name, contact_email, services, risk_level, status, frameworks, contract_expiry, notes, canonical_id) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("name", "Unnamed"), data.get("contact_name"), data.get("contact_email"),
              data.get("services"), data.get("risk_level", "medium"), data.get("status", "active"),
@@ -1087,6 +1087,6 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM grid_vendor_assessments WHERE vendor_id=?", (vid,))
-        db.execute("DELETE FROM grid_vendors WHERE id=?", (vid,))
+        db.execute("DELETE FROM grid_vendor_assessments WHERE vendor_id=%s", (vid,))
+        db.execute("DELETE FROM grid_vendors WHERE id=%s", (vid,))
         db.commit()
     finally:
@@ -1097,5 +1097,5 @@
     try:
         cur = insert_returning_id(db,
-            "INSERT INTO grid_vendor_assessments (vendor_id, score, findings, action_required, assessed_by) VALUES (?,?,?,?,?)",
+            "INSERT INTO grid_vendor_assessments (vendor_id, score, findings, action_required, assessed_by) VALUES (%s,%s,%s,%s,%s)",
             (vid, data.get("score"), data.get("findings"), data.get("action_required"), data.get("assessed_by")))
         db.commit()
@@ -1112,8 +1112,8 @@
         for eid in eids:
             db.execute(
-                "UPDATE grid_evidence_files SET status=?, approved_by=?, approved_at=datetime('now') WHERE id=?",
+                "UPDATE grid_evidence_files SET status=%s, approved_by=%s, approved_at=datetime('now') WHERE id=%s",
                 (status, approved_by, eid),
             )
-            ef = db.execute("SELECT control_id FROM grid_evidence_files WHERE id=?", (eid,)).fetchone()
+            ef = db.execute("SELECT control_id FROM grid_evidence_files WHERE id=%s", (eid,)).fetchone()
             if ef:
                 control_ids.add(ef[0])
@@ -1162,5 +1162,5 @@
         controls = _dicts(db.execute(
             "SELECT c.id, c.control_id AS ctrl_ref, c.name, c.status "
-            "FROM grid_controls c WHERE c.audit_id=? ORDER BY c.control_id",
+            "FROM grid_controls c WHERE c.audit_id=%s ORDER BY c.control_id",
             (audit_id,),
         ).fetchall())
@@ -1180,19 +1180,19 @@
             cid = ctrl["id"]
             items = db.execute(
-                "SELECT COUNT(*) FROM grid_evidence_items WHERE control_id=?", (cid,)
+                "SELECT COUNT(*) FROM grid_evidence_items WHERE control_id=%s", (cid,)
             ).fetchone()[0]
             files = db.execute(
-                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=?", (cid,)
+                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=%s", (cid,)
             ).fetchone()[0]
             approved = db.execute(
-                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=? AND status IN ('approved','Approved')",
+                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=%s AND status IN ('approved','Approved')",
                 (cid,),
             ).fetchone()[0]
             pending = db.execute(
-                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=? AND (status IS NULL OR status IN ('Uploaded','pending',''))",
+                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=%s AND (status IS NULL OR status IN ('Uploaded','pending',''))",
                 (cid,),
             ).fetchone()[0]
             rejected = db.execute(
-                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=? AND status IN ('rejected','Rejected')",
+                "SELECT COUNT(*) FROM grid_evidence_files WHERE control_id=%s AND status IN ('rejected','Rejected')",
                 (cid,),
             ).fetchone()[0]
@@ -1208,10 +1208,10 @@
                     "SELECT COUNT(DISTINCT ei.id) FROM grid_evidence_items ei "
                     "JOIN grid_evidence_files ef ON ef.evidence_item_id=ei.id "
-                    "WHERE ei.control_id=?", (cid,),
+                    "WHERE ei.control_id=%s", (cid,),
                 ).fetchone()[0]
                 items_a = db.execute(
                     "SELECT COUNT(DISTINCT ei.id) FROM grid_evidence_items ei "
                     "JOIN grid_evidence_files ef ON ef.evidence_item_id=ei.id "
-                    "WHERE ei.control_id=? AND ef.status IN ('approved','Approved')", (cid,),
+                    "WHERE ei.control_id=%s AND ef.status IN ('approved','Approved')", (cid,),
                 ).fetchone()[0]
                 items_with_evidence += items_w
@@ -1266,5 +1266,5 @@
         return _dicts(db.execute(
             "SELECT a.*, u.full_name AS approver_name FROM grid_approvals a "
-            "LEFT JOIN users u ON a.approver_id=u.id WHERE a.evidence_id=? ORDER BY a.stage", (evidence_id,)).fetchall())
+            "LEFT JOIN users u ON a.approver_id=u.id WHERE a.evidence_id=%s ORDER BY a.stage", (evidence_id,)).fetchall())
     finally:
         db.close()
@@ -1273,6 +1273,6 @@
     db = get_db()
     try:
-        max_stage = db.execute("SELECT COALESCE(MAX(stage),0) FROM grid_approvals WHERE evidence_id=?", (evidence_id,)).fetchone()[0]
-        cur = insert_returning_id(db,"INSERT INTO grid_approvals (evidence_id, stage, approver_id) VALUES (?,?,?)", (evidence_id, max_stage + 1, approver_id))
+        max_stage = db.execute("SELECT COALESCE(MAX(stage),0) FROM grid_approvals WHERE evidence_id=%s", (evidence_id,)).fetchone()[0]
+        cur = insert_returning_id(db,"INSERT INTO grid_approvals (evidence_id, stage, approver_id) VALUES (%s,%s,%s)", (evidence_id, max_stage + 1, approver_id))
         db.commit()
         return cur
@@ -1283,7 +1283,7 @@
     db = get_db()
     try:
-        db.execute("UPDATE grid_approvals SET status=?, comments=?, decided_at=datetime('now') WHERE id=?", (status, comments, approval_id))
-        db.commit()
-        row = _dict(db.execute("SELECT evidence_id FROM grid_approvals WHERE id=?", (approval_id,)).fetchone())
+        db.execute("UPDATE grid_approvals SET status=%s, comments=%s, decided_at=datetime('now') WHERE id=%s", (status, comments, approval_id))
+        db.commit()
+        row = _dict(db.execute("SELECT evidence_id FROM grid_approvals WHERE id=%s", (approval_id,)).fetchone())
         if row and status == "approved":
             approve_evidence(row["evidence_id"], "Approved", None)
@@ -1301,5 +1301,5 @@
             JOIN grid_controls s ON m.source_control_id=s.id
             JOIN grid_controls t ON m.target_control_id=t.id
-            WHERE s.audit_id=? OR t.audit_id=?
+            WHERE s.audit_id=%s OR t.audit_id=%s
         """, (audit_id, audit_id)).fetchall())
     finally:
@@ -1309,5 +1309,5 @@
     db = get_db()
     try:
-        cur = insert_returning_id(db,"INSERT INTO grid_control_mappings (source_control_id, target_control_id, mapping_type, confidence) VALUES (?,?,?,?) ON CONFLICT DO NOTHING",
+        cur = insert_returning_id(db,"INSERT INTO grid_control_mappings (source_control_id, target_control_id, mapping_type, confidence) VALUES (%s,%s,%s,%s) ON CONFLICT DO NOTHING",
             (source_id, target_id, mapping_type, confidence))
         db.commit()
@@ -1320,5 +1320,5 @@
     try:
         for m in mappings:
-            db.execute("INSERT INTO grid_control_mappings (source_control_id, target_control_id, mapping_type, confidence) VALUES (?,?,?,?) ON CONFLICT DO NOTHING",
+            db.execute("INSERT INTO grid_control_mappings (source_control_id, target_control_id, mapping_type, confidence) VALUES (%s,%s,%s,%s) ON CONFLICT DO NOTHING",
                 (m["source_control_id"], m["target_control_id"], m.get("mapping_type", "equivalent"), m.get("confidence")))
         db.commit()
@@ -1329,5 +1329,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM grid_control_mappings WHERE id=?", (mid,))
+        db.execute("DELETE FROM grid_control_mappings WHERE id=%s", (mid,))
         db.commit()
     finally:
@@ -1340,5 +1340,5 @@
         token = secrets.token_urlsafe(32)
         expires = (utcnow() + timedelta(days=expires_days)).strftime("%Y-%m-%d %H:%M:%S")
-        cur = insert_returning_id(db,"INSERT INTO grid_share_links (audit_id, token, created_by, auditor_email, expires_at) VALUES (?,?,?,?,?)",
+        cur = insert_returning_id(db,"INSERT INTO grid_share_links (audit_id, token, created_by, auditor_email, expires_at) VALUES (%s,%s,%s,%s,%s)",
             (audit_id, token, created_by, auditor_email, expires))
         db.commit()
@@ -1350,5 +1350,5 @@
     db = get_db()
     try:
-        return _dicts(db.execute("SELECT * FROM grid_share_links WHERE audit_id=? AND active=1 ORDER BY created_at DESC", (audit_id,)).fetchall())
+        return _dicts(db.execute("SELECT * FROM grid_share_links WHERE audit_id=%s AND active=1 ORDER BY created_at DESC", (audit_id,)).fetchall())
     finally:
         db.close()
@@ -1357,10 +1357,10 @@
     db = get_db()
     try:
-        link = _dict(db.execute("SELECT * FROM grid_share_links WHERE token=? AND active=1", (token,)).fetchone())
+        link = _dict(db.execute("SELECT * FROM grid_share_links WHERE token=%s AND active=1", (token,)).fetchone())
         if not link:
             return None
         if link.get("expires_at") and datetime.strptime(link["expires_at"], "%Y-%m-%d %H:%M:%S") < utcnow():
             return None
-        db.execute("UPDATE grid_share_links SET access_count=access_count+1 WHERE id=?", (link["id"],))
+        db.execute("UPDATE grid_share_links SET access_count=access_count+1 WHERE id=%s", (link["id"],))
         db.commit()
         return link
@@ -1371,5 +1371,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE grid_share_links SET active=0 WHERE id=?", (sid,))
+        db.execute("UPDATE grid_share_links SET active=0 WHERE id=%s", (sid,))
         db.commit()
     finally:
@@ -1380,11 +1380,11 @@
     db = get_db()
     try:
-        total = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=?", (audit_id,)).fetchone()[0]
-        complete = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=? AND status='Complete'", (audit_id,)).fetchone()[0]
-        in_progress = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=? AND status='In Progress'", (audit_id,)).fetchone()[0]
-        overdue = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=? AND due_date < date('now') AND status!='Complete'", (audit_id,)).fetchone()[0]
-        ev_total = db.execute("SELECT COUNT(*) FROM grid_evidence_items ei JOIN grid_controls c ON ei.control_id=c.id WHERE c.audit_id=?", (audit_id,)).fetchone()[0]
-        ev_uploaded = db.execute("SELECT COUNT(*) FROM grid_evidence_files ef JOIN grid_controls c ON ef.control_id=c.id WHERE c.audit_id=?", (audit_id,)).fetchone()[0]
-        audit = _dict(db.execute("SELECT audit_date FROM grid_audits WHERE id=?", (audit_id,)).fetchone())
+        total = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=%s", (audit_id,)).fetchone()[0]
+        complete = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=%s AND status='Complete'", (audit_id,)).fetchone()[0]
+        in_progress = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=%s AND status='In Progress'", (audit_id,)).fetchone()[0]
+        overdue = db.execute("SELECT COUNT(*) FROM grid_controls WHERE audit_id=%s AND due_date < date('now') AND status!='Complete'", (audit_id,)).fetchone()[0]
+        ev_total = db.execute("SELECT COUNT(*) FROM grid_evidence_items ei JOIN grid_controls c ON ei.control_id=c.id WHERE c.audit_id=%s", (audit_id,)).fetchone()[0]
+        ev_uploaded = db.execute("SELECT COUNT(*) FROM grid_evidence_files ef JOIN grid_controls c ON ef.control_id=c.id WHERE c.audit_id=%s", (audit_id,)).fetchone()[0]
+        audit = _dict(db.execute("SELECT audit_date FROM grid_audits WHERE id=%s", (audit_id,)).fetchone())
         days = None
         if audit and audit.get("audit_date"):
@@ -1407,5 +1407,5 @@
     db = get_db()
     try:
-        db.execute("INSERT INTO audit_log (user_id, action, module, entity_type, entity_id, details) VALUES (?,?,?,?,?,?)",
+        db.execute("INSERT INTO audit_log (user_id, action, module, entity_type, entity_id, details) VALUES (%s,%s,%s,%s,%s,%s)",
             (user_id, action, "grid", entity_type, entity_id, json.dumps(details) if details else None))
         db.commit()
@@ -1419,5 +1419,5 @@
             "SELECT a.*, u.full_name AS user_name FROM audit_log a "
             "LEFT JOIN users u ON a.user_id=u.id WHERE a.module='grid' "
-            "ORDER BY a.created_at DESC LIMIT ?", (limit,)).fetchall())
+            "ORDER BY a.created_at DESC LIMIT %s", (limit,)).fetchall())
     finally:
         db.close()
@@ -1442,5 +1442,5 @@
     db = get_db()
     try:
-        cur = insert_returning_id(db,"INSERT INTO grid_compliance_scores (audit_id, score, details) VALUES (?,?,?)",
+        cur = insert_returning_id(db,"INSERT INTO grid_compliance_scores (audit_id, score, details) VALUES (%s,%s,%s)",
             (audit_id, score, json.dumps(details) if details else None))
         db.commit()
@@ -1453,5 +1453,5 @@
     try:
         return _dicts(db.execute(
-            "SELECT * FROM grid_compliance_scores WHERE audit_id=? ORDER BY created_at DESC LIMIT ?",
+            "SELECT * FROM grid_compliance_scores WHERE audit_id=%s ORDER BY created_at DESC LIMIT %s",
             (audit_id, limit)).fetchall())
     finally:
@@ -1470,5 +1470,5 @@
             "INSERT INTO grid_reports "
             "(audit_id, report_type, title, filename, file_path, file_size, notes, generated_by) "
-            "VALUES (?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 data["audit_id"],
@@ -1521,5 +1521,5 @@
             "LEFT JOIN grid_audits a ON r.audit_id=a.id "
             "LEFT JOIN users u ON r.generated_by=u.id "
-            "WHERE r.id=?", (rid,)
+            "WHERE r.id=%s", (rid,)
         ).fetchone())
     finally:
@@ -1531,5 +1531,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM grid_reports WHERE id=?", (rid,))
+        db.execute("DELETE FROM grid_reports WHERE id=%s", (rid,))
         db.commit()
     finally:
@@ -1550,5 +1550,5 @@
     try:
         parent = _dict(db.execute(
-            "SELECT * FROM grid_audits WHERE id=?", (parent_audit_id,)
+            "SELECT * FROM grid_audits WHERE id=%s", (parent_audit_id,)
         ).fetchone())
         if not parent:
@@ -1565,5 +1565,5 @@
             " end_date, audit_date, scope, objective, criteria, methodology, "
             " parent_audit_id) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 name, fw_id, audit_type,
@@ -1583,5 +1583,5 @@
         if fw_id:
             gf = db.execute(
-                "SELECT name FROM grid_frameworks WHERE id=?", (fw_id,)
+                "SELECT name FROM grid_frameworks WHERE id=%s", (fw_id,)
             ).fetchone()
             if gf:
@@ -1590,5 +1590,5 @@
                     rows = db.execute(
                         "SELECT ref, name, description, priority "
-                        "FROM controls WHERE framework_id=? ORDER BY ref",
+                        "FROM controls WHERE framework_id=%s ORDER BY ref",
                         (uf_id,),
                     ).fetchall()
@@ -1597,5 +1597,5 @@
                             "INSERT INTO grid_controls "
                             "(audit_id, framework_id, control_id, name, description, risk_level) "
-                            "VALUES (?,?,?,?,?,?)",
+                            "VALUES (%s,%s,%s,%s,%s,%s)",
                             (new_aid, fw_id, c[0], c[1], c[2], c[3] or "Medium"),
                         )
@@ -1621,5 +1621,5 @@
             "cap_status "
             "FROM grid_non_conformances "
-            "WHERE audit_id=? AND status != 'closed' "
+            "WHERE audit_id=%s AND status != 'closed' "
             "AND cap_status NOT IN ('Closed', 'Verification')",
             (parent_audit_id,),
@@ -1632,5 +1632,5 @@
             if nc["control_id"]:
                 old_ctrl = db.execute(
-                    "SELECT control_id FROM grid_controls WHERE id=?",
+                    "SELECT control_id FROM grid_controls WHERE id=%s",
                     (nc["control_id"],),
                 ).fetchone()
@@ -1638,5 +1638,5 @@
                     match = db.execute(
                         "SELECT id FROM grid_controls "
-                        "WHERE audit_id=? AND control_id=?",
+                        "WHERE audit_id=%s AND control_id=%s",
                         (new_audit_id, old_ctrl[0]),
                     ).fetchone()
@@ -1649,5 +1649,5 @@
                 " assigned_to, root_cause, corrective_action, "
                 " preventive_action, due_date, cap_status, source_nc_id) "
-                "VALUES (?,?,?,?,?,?,?,?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
                 (
                     new_audit_id,
@@ -1692,5 +1692,5 @@
                 "FROM grid_audits a "
                 "LEFT JOIN grid_frameworks f ON a.framework_id=f.id "
-                "WHERE a.id=?",
+                "WHERE a.id=%s",
                 (current_id,),
             ).fetchone()
@@ -1711,5 +1711,5 @@
             "FROM grid_audits a "
             "LEFT JOIN grid_frameworks f ON a.framework_id=f.id "
-            "WHERE a.parent_audit_id=? "
+            "WHERE a.parent_audit_id=%s "
             "ORDER BY a.created_at DESC",
             (audit_id,),
@@ -1734,5 +1734,5 @@
     try:
         audit = db.execute(
-            "SELECT parent_audit_id FROM grid_audits WHERE id=?",
+            "SELECT parent_audit_id FROM grid_audits WHERE id=%s",
             (audit_id,),
         ).fetchone()
@@ -1745,5 +1745,5 @@
                 "LEFT JOIN users u ON nc.assigned_to=u.id "
                 "LEFT JOIN grid_controls c ON nc.control_id=c.id "
-                "WHERE nc.audit_id=? ORDER BY nc.created_at",
+                "WHERE nc.audit_id=%s ORDER BY nc.created_at",
                 (audit_id,),
             ).fetchall())
@@ -1769,5 +1769,5 @@
             "LEFT JOIN grid_controls c ON cur.control_id=c.id "
             "LEFT JOIN grid_non_conformances src ON cur.source_nc_id=src.id "
-            "WHERE cur.audit_id=? AND cur.source_nc_id IS NOT NULL "
+            "WHERE cur.audit_id=%s AND cur.source_nc_id IS NOT NULL "
             "ORDER BY cur.created_at",
             (audit_id,),
@@ -1781,5 +1781,5 @@
             "LEFT JOIN users u ON nc.assigned_to=u.id "
             "LEFT JOIN grid_controls c ON nc.control_id=c.id "
-            "WHERE nc.audit_id=? AND nc.source_nc_id IS NULL "
+            "WHERE nc.audit_id=%s AND nc.source_nc_id IS NULL "
             "ORDER BY nc.created_at",
             (audit_id,),
@@ -1796,5 +1796,5 @@
             "LEFT JOIN users u ON nc.assigned_to=u.id "
             "LEFT JOIN grid_controls c ON nc.control_id=c.id "
-            "WHERE nc.audit_id=? AND (nc.status='closed' OR nc.cap_status='Closed') "
+            "WHERE nc.audit_id=%s AND (nc.status='closed' OR nc.cap_status='Closed') "
             "ORDER BY nc.closed_at DESC",
             (parent_id,),
@@ -1826,5 +1826,5 @@
             "FROM grid_audit_signoffs s "
             "LEFT JOIN users u ON s.user_id=u.id "
-            "WHERE s.audit_id=? ORDER BY s.signed_at",
+            "WHERE s.audit_id=%s ORDER BY s.signed_at",
             (audit_id,),
         ).fetchall()
@@ -1848,5 +1848,5 @@
         if role == "reviewer":
             lead = db.execute(
-                "SELECT id FROM grid_audit_signoffs WHERE audit_id=? AND role='lead'",
+                "SELECT id FROM grid_audit_signoffs WHERE audit_id=%s AND role='lead'",
                 (audit_id,),
             ).fetchone()
@@ -1857,5 +1857,5 @@
             "INSERT INTO grid_audit_signoffs "
             "(audit_id, role, user_id, comment, signed_at) "
-            "VALUES (?, ?, ?, ?, datetime('now')) "
+            "VALUES (%s, %s, %s, %s, datetime('now')) "
             "ON CONFLICT (audit_id, role) DO UPDATE SET "
             "user_id=excluded.user_id, comment=excluded.comment, signed_at=datetime('now')",
@@ -1875,15 +1875,15 @@
             # Revoking lead also revokes reviewer
             db.execute(
-                "DELETE FROM grid_audit_signoffs WHERE audit_id=? AND role IN ('lead','reviewer')",
+                "DELETE FROM grid_audit_signoffs WHERE audit_id=%s AND role IN ('lead','reviewer')",
                 (audit_id,),
             )
         else:
             db.execute(
-                "DELETE FROM grid_audit_signoffs WHERE audit_id=? AND role=?",
+                "DELETE FROM grid_audit_signoffs WHERE audit_id=%s AND role=%s",
                 (audit_id, role),
             )
         # Also unlock if revoking sign-offs
         db.execute(
-            "UPDATE grid_audits SET is_locked=0, locked_at=NULL, locked_by=NULL WHERE id=?",
+            "UPDATE grid_audits SET is_locked=0, locked_at=NULL, locked_by=NULL WHERE id=%s",
             (audit_id,),
         )
@@ -1901,5 +1901,5 @@
     try:
         signoffs = db.execute(
-            "SELECT role FROM grid_audit_signoffs WHERE audit_id=?",
+            "SELECT role FROM grid_audit_signoffs WHERE audit_id=%s",
             (audit_id,),
         ).fetchall()
@@ -1910,5 +1910,5 @@
         db.execute(
             "UPDATE grid_audits SET is_locked=1, locked_at=datetime('now'), "
-            "locked_by=?, status='Completed' WHERE id=?",
+            "locked_by=%s, status='Completed' WHERE id=%s",
             (user_id, audit_id),
         )
@@ -1924,8 +1924,8 @@
     try:
         db.execute(
-            "UPDATE grid_audits SET is_locked=0, locked_at=NULL, locked_by=NULL WHERE id=?",
+            "UPDATE grid_audits SET is_locked=0, locked_at=NULL, locked_by=NULL WHERE id=%s",
             (audit_id,),
         )
-        db.execute("DELETE FROM grid_audit_signoffs WHERE audit_id=?", (audit_id,))
+        db.execute("DELETE FROM grid_audit_signoffs WHERE audit_id=%s", (audit_id,))
         db.commit()
     finally:
@@ -2046,5 +2046,5 @@
         doc = db.execute(
             "SELECT id, title, doc_id, framework, control_ref, version "
-            "FROM aria_documents WHERE id=?",
+            "FROM aria_documents WHERE id=%s",
             (aria_doc_id,),
         ).fetchone()
@@ -2055,5 +2055,5 @@
         existing = db.execute(
             "SELECT id FROM grid_evidence_files "
-            "WHERE control_id=? AND original_name=? AND notes LIKE '%aria_doc_id=%'",
+            "WHERE control_id=%s AND original_name=%s AND notes LIKE '%aria_doc_id=%'",
             (control_id, doc["title"]),
         ).fetchone()
@@ -2065,5 +2065,5 @@
             "(control_id, filename, original_name, file_path, file_size, "
             " mime_type, uploaded_by, notes, status) "
-            "VALUES (?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 control_id,
@@ -2096,5 +2096,5 @@
             "(audit_id, control_id, framework_name, control_ref, "
             " title, description, requested_by) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (
                 data["audit_id"],
@@ -2145,6 +2145,6 @@
         db.execute(
             "UPDATE grid_policy_requests "
-            "SET status='fulfilled', aria_document_id=?, resolved_at=datetime('now') "
-            "WHERE id=?",
+            "SET status='fulfilled', aria_document_id=%s, resolved_at=datetime('now') "
+            "WHERE id=%s",
             (aria_document_id, request_id),
         )
@@ -2179,5 +2179,5 @@
             "JOIN grid_controls c ON ef.control_id=c.id "
             "JOIN grid_audits a ON c.audit_id=a.id "
-            "WHERE ef.id=?",
+            "WHERE ef.id=%s",
             (grid_evidence_id,),
         ).fetchone()
@@ -2188,5 +2188,5 @@
         tag = f"grid_evidence_id={grid_evidence_id}"
         existing = db.execute(
-            "SELECT id FROM evidence_items WHERE tags LIKE ?",
+            "SELECT id FROM evidence_items WHERE tags LIKE %s",
             (f"%{tag}%",),
         ).fetchone()
@@ -2230,5 +2230,5 @@
             "(title, description, file_path, file_name, file_size, file_hash, "
             " mime_type, category, tags, status, expiry_date, uploaded_by) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 ef["original_name"] or ef["filename"],
@@ -2252,5 +2252,5 @@
             "INSERT INTO evidence_links "
             "(evidence_id, module, entity_type, entity_id, linked_by) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (vault_id, "grid", "control", ef["control_id"], ef.get("uploaded_by")),
         )
@@ -2260,5 +2260,5 @@
             "INSERT INTO evidence_links "
             "(evidence_id, module, entity_type, entity_id, linked_by) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (vault_id, "grid", "audit", ef["audit_id"], ef.get("uploaded_by")),
         )
@@ -2293,5 +2293,5 @@
             params.append(category)
         if module:
-            q += " AND e.id IN (SELECT evidence_id FROM evidence_links WHERE module=?)"
+            q += " AND e.id IN (SELECT evidence_id FROM evidence_links WHERE module=%s)"
             params.append(module)
         if search:
@@ -2316,5 +2316,5 @@
         # Get vault item
         item = db.execute(
-            "SELECT * FROM evidence_items WHERE id=? AND status != 'archived'",
+            "SELECT * FROM evidence_items WHERE id=%s AND status != 'archived'",
             (vault_evidence_id,),
         ).fetchone()
@@ -2325,5 +2325,5 @@
         existing = db.execute(
             "SELECT id FROM grid_evidence_files "
-            "WHERE control_id=? AND notes LIKE ?",
+            "WHERE control_id=%s AND notes LIKE %s",
             (control_id, f"%vault_evidence_id={vault_evidence_id}%"),
         ).fetchone()
@@ -2336,5 +2336,5 @@
             "(control_id, filename, original_name, file_path, file_size, "
             " mime_type, uploaded_by, notes, status) "
-            "VALUES (?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 control_id,
@@ -2354,5 +2354,5 @@
         existing_link = db.execute(
             "SELECT id FROM evidence_links "
-            "WHERE evidence_id=? AND module='grid' AND entity_type='control' AND entity_id=?",
+            "WHERE evidence_id=%s AND module='grid' AND entity_type='control' AND entity_id=%s",
             (vault_evidence_id, control_id),
         ).fetchone()
@@ -2361,5 +2361,5 @@
                 "INSERT INTO evidence_links "
                 "(evidence_id, module, entity_type, entity_id, linked_by) "
-                "VALUES (?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s)",
                 (vault_evidence_id, "grid", "control", control_id, user_id),
             )
--- a/modules/grid/report_service.py
+++ b/modules/grid/report_service.py
@@ -109,5 +109,5 @@
             "SELECT a.*, f.name AS framework_name, f.color AS framework_color "
             "FROM grid_audits a LEFT JOIN grid_frameworks f ON a.framework_id=f.id "
-            "WHERE a.id=?", (audit_id,)
+            "WHERE a.id=%s", (audit_id,)
         ).fetchone()
         if not audit:
@@ -122,5 +122,5 @@
             FROM grid_controls c
             LEFT JOIN users u ON c.assignee_id=u.id
-            WHERE c.audit_id=?
+            WHERE c.audit_id=%s
             ORDER BY c.control_id
         """, (audit_id,)).fetchall()]
@@ -151,5 +151,5 @@
             LEFT JOIN users u ON nc.assigned_to=u.id
             LEFT JOIN grid_controls c ON nc.control_id=c.id
-            WHERE nc.audit_id=?
+            WHERE nc.audit_id=%s
             ORDER BY CASE nc.severity WHEN 'critical' THEN 1 WHEN 'major' THEN 2
                      WHEN 'minor' THEN 3 ELSE 4 END
@@ -168,5 +168,5 @@
             FROM grid_evidence_files ef
             JOIN grid_controls c ON ef.control_id=c.id
-            WHERE c.audit_id=?
+            WHERE c.audit_id=%s
         """, (audit_id,)).fetchone())
 
@@ -178,5 +178,5 @@
             FROM grid_non_conformances nc
             LEFT JOIN users u ON nc.mgmt_response_by=u.id
-            WHERE nc.audit_id=? AND nc.mgmt_response_status IS NOT NULL
+            WHERE nc.audit_id=%s AND nc.mgmt_response_status IS NOT NULL
             ORDER BY nc.mgmt_response_at DESC
         """, (audit_id,)).fetchall()]
@@ -187,5 +187,5 @@
             FROM grid_audit_signoffs s
             LEFT JOIN users u ON s.user_id=u.id
-            WHERE s.audit_id=?
+            WHERE s.audit_id=%s
             ORDER BY s.signed_at
         """, (audit_id,)).fetchall()]
[WARN] modules/grid/routes.py:
  line 487: f-string SQL with ? — manual review needed
  line 1489: f-string SQL with ? — manual review needed
  line 1495: f-string SQL with ? — manual review needed
  line 1599: f-string SQL with ? — manual review needed
  line 1603: f-string SQL with ? — manual review needed
--- a/modules/grid/routes.py
+++ b/modules/grid/routes.py
@@ -194,5 +194,5 @@
     try:
         audit = db.execute(
-            "SELECT framework_id FROM grid_audits WHERE id=?", (aid,)
+            "SELECT framework_id FROM grid_audits WHERE id=%s", (aid,)
         ).fetchone()
         if not audit:
@@ -201,5 +201,5 @@
         # Check if controls already exist
         existing = db.execute(
-            "SELECT COUNT(*) FROM grid_controls WHERE audit_id=?", (aid,)
+            "SELECT COUNT(*) FROM grid_controls WHERE audit_id=%s", (aid,)
         ).fetchone()[0]
         if existing:
@@ -212,5 +212,5 @@
         # Match grid_framework name → unified framework → controls
         gf = db.execute(
-            "SELECT name FROM grid_frameworks WHERE id=?", (grid_fw_id,)
+            "SELECT name FROM grid_frameworks WHERE id=%s", (grid_fw_id,)
         ).fetchone()
         if not gf:
@@ -225,5 +225,5 @@
         rows = db.execute(
             "SELECT ref, name, description, priority "
-            "FROM controls WHERE framework_id=? ORDER BY ref",
+            "FROM controls WHERE framework_id=%s ORDER BY ref",
             (uf_id,),
         ).fetchall()
@@ -233,5 +233,5 @@
                 "INSERT INTO grid_controls "
                 "(audit_id, framework_id, control_id, name, description, risk_level) "
-                "VALUES (?,?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s,%s)",
                 (aid, grid_fw_id, c[0], c[1], c[2], c[3] or "Medium"),
             )
@@ -286,5 +286,5 @@
         try:
             audit = db.execute(
-                "SELECT is_integrated, framework_ids FROM grid_audits WHERE id=?",
+                "SELECT is_integrated, framework_ids FROM grid_audits WHERE id=%s",
                 (int(audit_id),)
             ).fetchone()
@@ -304,5 +304,5 @@
                             "SELECT c2.id AS uid, c2.framework_id "
                             "FROM controls c2 JOIN grid_controls gc ON gc.control_id=c2.ref "
-                            "WHERE gc.id=? LIMIT 1",
+                            "WHERE gc.id=%s LIMIT 1",
                             (c["id"],)
                         ).fetchone()
@@ -315,5 +315,5 @@
                         mapped_count = db.execute("""
                             SELECT COUNT(DISTINCT
-                                CASE WHEN gcm.source_control_id=? THEN
+                                CASE WHEN gcm.source_control_id=%s THEN
                                     (SELECT gc2.framework_id FROM grid_controls gc2 WHERE gc2.id=gcm.target_control_id)
                                 ELSE
@@ -321,5 +321,5 @@
                                 END)
                             FROM grid_control_mappings gcm
-                            WHERE (gcm.source_control_id=? OR gcm.target_control_id=?)
+                            WHERE (gcm.source_control_id=%s OR gcm.target_control_id=%s)
                               AND gcm.mapping_type='ims_equivalent'
                         """, (c["id"], c["id"], c["id"])).fetchone()[0] or 0
@@ -746,9 +746,9 @@
         try:
             user = db.execute(
-                "SELECT full_name, email FROM users WHERE id=?",
+                "SELECT full_name, email FROM users WHERE id=%s",
                 (nc["assigned_to"],)
             ).fetchone()
             assigner = db.execute(
-                "SELECT full_name FROM users WHERE id=?",
+                "SELECT full_name FROM users WHERE id=%s",
                 (assigner_id,)
             ).fetchone() if assigner_id else None
@@ -1388,5 +1388,5 @@
                 "SELECT rs.*, u.full_name as auditor_name FROM grid_remote_sessions rs "
                 "LEFT JOIN users u ON rs.auditor_id = u.id "
-                "WHERE rs.audit_id = ? ORDER BY rs.scheduled_start DESC", (int(audit_id),)
+                "WHERE rs.audit_id = %s ORDER BY rs.scheduled_start DESC", (int(audit_id),)
             ).fetchall()
         else:
@@ -1412,5 +1412,5 @@
             "INSERT INTO grid_remote_sessions "
             "(audit_id, title, description, session_type, scheduled_start, scheduled_end, "
-            "meeting_link, auditor_id, created_by) VALUES (?,?,?,?,?,?,?,?,?)",
+            "meeting_link, auditor_id, created_by) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 data.get("audit_id"),
@@ -1432,5 +1432,5 @@
             db.execute(
                 "INSERT INTO grid_remote_participants (session_id, user_id, external_name, external_email, role) "
-                "VALUES (?,?,?,?,?)",
+                "VALUES (%s,%s,%s,%s,%s)",
                 (sid, p.get("user_id"), p.get("name", ""), p.get("email", ""), p.get("role", "auditee"))
             )
@@ -1451,5 +1451,5 @@
         session = db.execute(
             "SELECT rs.*, u.full_name as auditor_name FROM grid_remote_sessions rs "
-            "LEFT JOIN users u ON rs.auditor_id = u.id WHERE rs.id = ?", (sid,)
+            "LEFT JOIN users u ON rs.auditor_id = u.id WHERE rs.id = %s", (sid,)
         ).fetchone()
         if not session:
@@ -1458,13 +1458,13 @@
         result["participants"] = [dict(r) for r in db.execute(
             "SELECT rp.*, u.full_name FROM grid_remote_participants rp "
-            "LEFT JOIN users u ON rp.user_id = u.id WHERE rp.session_id = ?", (sid,)
+            "LEFT JOIN users u ON rp.user_id = u.id WHERE rp.session_id = %s", (sid,)
         ).fetchall()]
         result["findings"] = [dict(r) for r in db.execute(
             "SELECT rf.*, u.full_name as raised_by_name FROM grid_remote_findings rf "
-            "LEFT JOIN users u ON rf.raised_by = u.id WHERE rf.session_id = ? ORDER BY rf.created_at", (sid,)
+            "LEFT JOIN users u ON rf.raised_by = u.id WHERE rf.session_id = %s ORDER BY rf.created_at", (sid,)
         ).fetchall()]
         result["notes"] = [dict(r) for r in db.execute(
             "SELECT rn.*, u.full_name FROM grid_remote_notes rn "
-            "LEFT JOIN users u ON rn.user_id = u.id WHERE rn.session_id = ? ORDER BY rn.created_at", (sid,)
+            "LEFT JOIN users u ON rn.user_id = u.id WHERE rn.session_id = %s ORDER BY rn.created_at", (sid,)
         ).fetchall()]
     finally:
@@ -1510,5 +1510,5 @@
         db.execute(
             "UPDATE grid_remote_sessions SET status = 'in_progress', actual_start = datetime('now'), "
-            "updated_at = datetime('now') WHERE id = ?", (sid,)
+            "updated_at = datetime('now') WHERE id = %s", (sid,)
         )
         db.commit()
@@ -1527,5 +1527,5 @@
         db.execute(
             "UPDATE grid_remote_sessions SET status = 'completed', actual_end = datetime('now'), "
-            "updated_at = datetime('now') WHERE id = ?", (sid,)
+            "updated_at = datetime('now') WHERE id = %s", (sid,)
         )
         db.commit()
@@ -1548,5 +1548,5 @@
             "INSERT INTO grid_remote_findings "
             "(session_id, control_id, finding_type, severity, title, description, evidence_ref, raised_by) "
-            "VALUES (?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 sid,
@@ -1619,5 +1619,5 @@
     try:
         db.execute(
-            "INSERT INTO grid_remote_notes (session_id, user_id, content, timestamp_offset) VALUES (?,?,?,?)",
+            "INSERT INTO grid_remote_notes (session_id, user_id, content, timestamp_offset) VALUES (%s,%s,%s,%s)",
             (sid, _uid(request), data.get("content", ""), data.get("timestamp_offset", 0))
         )
@@ -1641,5 +1641,5 @@
         db.execute(
             "INSERT INTO grid_remote_participants (session_id, user_id, external_name, external_email, role) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (sid, data.get("user_id"), data.get("name", ""), data.get("email", ""), data.get("role", "auditee"))
         )
--- a/modules/grid/scheduler.py
+++ b/modules/grid/scheduler.py
@@ -82,5 +82,5 @@
                 ),
             )
-            db.execute("UPDATE grid_reminders SET last_sent=? WHERE id=?",
+            db.execute("UPDATE grid_reminders SET last_sent=%s WHERE id=%s",
                         (utcnow().isoformat(), r["id"]))
             sent += 1
@@ -133,5 +133,5 @@
                 ),
             )
-            db.execute("UPDATE grid_evidence_files SET expiry_notified=1 WHERE id=?", (ev["id"],))
+            db.execute("UPDATE grid_evidence_files SET expiry_notified=1 WHERE id=%s", (ev["id"],))
             sent += 1
         db.commit()
@@ -268,5 +268,5 @@
             db.execute(
                 "INSERT INTO grid_compliance_scores (audit_id, score, total_controls, complete_controls) "
-                "VALUES (?, ?, ?, ?)",
+                "VALUES (%s, %s, %s, %s)",
                 (r["id"], score, total, complete),
             )
@@ -506,5 +506,5 @@
                     db.execute(
                         "INSERT INTO notifications (user_id, module, title, message, link) "
-                        "SELECT id, 'platform', ?, ?, '/evidence/' "
+                        "SELECT id, 'platform', %s, %s, '/evidence/' "
                         "FROM users u JOIN user_roles ur ON u.id=ur.user_id WHERE ur.role_key IN ('super_admin','admin') AND u.is_active=1",
                         (
@@ -534,5 +534,5 @@
                     db.execute(
                         "INSERT INTO notifications (user_id, module, title, message, link) "
-                        "SELECT id, 'platform', ?, ?, '/evidence/' "
+                        "SELECT id, 'platform', %s, %s, '/evidence/' "
                         "FROM users u JOIN user_roles ur ON u.id=ur.user_id WHERE ur.role_key IN ('super_admin','admin') AND u.is_active=1",
                         (
--- a/modules/launcher/_route_helpers.py
+++ b/modules/launcher/_route_helpers.py
@@ -52,5 +52,5 @@
     try:
         row = db.execute(
-            "SELECT must_change_password FROM users WHERE id=?", (uid,)
+            "SELECT must_change_password FROM users WHERE id=%s", (uid,)
         ).fetchone()
         return bool(row and row["must_change_password"])
@@ -70,5 +70,5 @@
         for u in users:
             role_rows = db.execute(
-                "SELECT role_key FROM user_roles WHERE user_id=? ORDER BY role_key",
+                "SELECT role_key FROM user_roles WHERE user_id=%s ORDER BY role_key",
                 (u["id"],),
             ).fetchall()
[WARN] modules/launcher/routes_admin.py:
  SyntaxError: invalid syntax (<unknown>, line 8)
--- a/modules/launcher/routes_auth.py
+++ b/modules/launcher/routes_auth.py
@@ -199,5 +199,5 @@
     try:
         row = db.execute(
-            "SELECT password_hash FROM users WHERE id=?", (int(user["id"]),)
+            "SELECT password_hash FROM users WHERE id=%s", (int(user["id"]),)
         ).fetchone()
         if not forced:
@@ -216,5 +216,5 @@
 
         db.execute(
-            "UPDATE users SET password_hash=?, must_change_password=0 WHERE id=?",
+            "UPDATE users SET password_hash=%s, must_change_password=0 WHERE id=%s",
             (hash_password(new_password), int(user["id"])),
         )
[WARN] modules/launcher/routes_dashboard.py:
  SyntaxError: invalid syntax (<unknown>, line 11)
--- a/modules/launcher/routes_frameworks.py
+++ b/modules/launcher/routes_frameworks.py
@@ -60,5 +60,5 @@
     db = get_db()
     try:
-        existing = db.execute("SELECT id FROM frameworks WHERE name = ?", (name,)).fetchone()
+        existing = db.execute("SELECT id FROM frameworks WHERE name = %s", (name,)).fetchone()
         if existing:
             return _JSONResp({"error": "Framework already exists"}, status_code=409)
@@ -66,5 +66,5 @@
         db.execute(
             "INSERT INTO frameworks (name, description, color, relevant_modules, is_active) "
-            "VALUES (?, ?, ?, ?, 1)",
+            "VALUES (%s, %s, %s, %s, 1)",
             (name, description, color, relevant_modules),
         )
@@ -129,9 +129,9 @@
             fid = fw["id"]
             total = fw.get("total_controls") or db.execute(
-                "SELECT COUNT(*) FROM controls WHERE framework_id=?", (fid,)
+                "SELECT COUNT(*) FROM controls WHERE framework_id=%s", (fid,)
             ).fetchone()[0]
             compliant = db.execute(
                 "SELECT COUNT(*) FROM controls "
-                "WHERE framework_id=? AND status IN ('Implemented','Compliant','Complete')",
+                "WHERE framework_id=%s AND status IN ('Implemented','Compliant','Complete')",
                 (fid,),
             ).fetchone()[0]
[WARN] modules/launcher/routes_platform.py:
  line 324: f-string SQL with ? — manual review needed
  line 327: f-string SQL with ? — manual review needed
  line 702: f-string SQL with ? — manual review needed
  line 706: f-string SQL with ? — manual review needed
--- a/modules/launcher/routes_platform.py
+++ b/modules/launcher/routes_platform.py
@@ -30,9 +30,9 @@
     try:
         rows = db.execute(
-            "SELECT * FROM notifications WHERE user_id = ? ORDER BY created_at DESC LIMIT 50",
+            "SELECT * FROM notifications WHERE user_id = %s ORDER BY created_at DESC LIMIT 50",
             (uid,)
         ).fetchall()
         unread = db.execute(
-            "SELECT COUNT(*) FROM notifications WHERE user_id = ? AND is_read = 0",
+            "SELECT COUNT(*) FROM notifications WHERE user_id = %s AND is_read = 0",
             (uid,)
         ).fetchone()[0]
@@ -49,5 +49,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE notifications SET is_read = 1 WHERE id = ? AND user_id = ?", (nid, uid))
+        db.execute("UPDATE notifications SET is_read = 1 WHERE id = %s AND user_id = %s", (nid, uid))
         db.commit()
     finally:
@@ -63,5 +63,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE notifications SET is_read = 1 WHERE user_id = ? AND is_read = 0", (uid,))
+        db.execute("UPDATE notifications SET is_read = 1 WHERE user_id = %s AND is_read = 0", (uid,))
         db.commit()
     finally:
@@ -77,5 +77,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM notifications WHERE id = ? AND user_id = ?", (nid, uid))
+        db.execute("DELETE FROM notifications WHERE id = %s AND user_id = %s", (nid, uid))
         db.commit()
     finally:
@@ -104,5 +104,5 @@
             "SELECT c.id, c.ref, c.name, f.name as framework_name "
             "FROM controls c JOIN frameworks f ON c.framework_id = f.id "
-            "WHERE c.name LIKE ? OR c.ref LIKE ? LIMIT 10",
+            "WHERE c.name LIKE %s OR c.ref LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -113,5 +113,5 @@
         # ARIA documents
         for r in db.execute(
-            "SELECT id, doc_id, title FROM aria_documents WHERE title LIKE ? OR doc_id LIKE ? LIMIT 10",
+            "SELECT id, doc_id, title FROM aria_documents WHERE title LIKE %s OR doc_id LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -121,5 +121,5 @@
         # Sentinel RoPA
         for r in db.execute(
-            "SELECT id, ref_number, processing_name FROM sentinel_ropa WHERE processing_name LIKE ? OR ref_number LIKE ? LIMIT 10",
+            "SELECT id, ref_number, processing_name FROM sentinel_ropa WHERE processing_name LIKE %s OR ref_number LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -129,5 +129,5 @@
         # GRID audits
         for r in db.execute(
-            "SELECT id, name FROM grid_audits WHERE name LIKE ? LIMIT 10",
+            "SELECT id, name FROM grid_audits WHERE name LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -137,5 +137,5 @@
         # BCM plans
         for r in db.execute(
-            "SELECT id, title FROM bcm_plans WHERE title LIKE ? LIMIT 10",
+            "SELECT id, title FROM bcm_plans WHERE title LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -145,5 +145,5 @@
         # Risk register
         for r in db.execute(
-            "SELECT id, title, source_module FROM risk_register WHERE title LIKE ? LIMIT 10",
+            "SELECT id, title, source_module FROM risk_register WHERE title LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -153,5 +153,5 @@
         # Evidence
         for r in db.execute(
-            "SELECT id, title, category FROM evidence_items WHERE title LIKE ? OR tags LIKE ? LIMIT 10",
+            "SELECT id, title, category FROM evidence_items WHERE title LIKE %s OR tags LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -161,5 +161,5 @@
         # Sentinel breaches
         for r in db.execute(
-            "SELECT id, ref_number, title, severity FROM sentinel_breaches WHERE title LIKE ? OR ref_number LIKE ? LIMIT 10",
+            "SELECT id, ref_number, title, severity FROM sentinel_breaches WHERE title LIKE %s OR ref_number LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -170,5 +170,5 @@
         # Sentinel DPIAs
         for r in db.execute(
-            "SELECT id, ref_number, title FROM sentinel_dpias WHERE title LIKE ? OR ref_number LIKE ? LIMIT 10",
+            "SELECT id, ref_number, title FROM sentinel_dpias WHERE title LIKE %s OR ref_number LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -178,5 +178,5 @@
         # Sentinel DSRs
         for r in db.execute(
-            "SELECT id, ref_number, requester_name, request_type FROM sentinel_dsr WHERE requester_name LIKE ? OR ref_number LIKE ? LIMIT 10",
+            "SELECT id, ref_number, requester_name, request_type FROM sentinel_dsr WHERE requester_name LIKE %s OR ref_number LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -187,5 +187,5 @@
         # Sentinel vendors
         for r in db.execute(
-            "SELECT id, name, type FROM sentinel_vendors WHERE name LIKE ? LIMIT 10",
+            "SELECT id, name, type FROM sentinel_vendors WHERE name LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -195,5 +195,5 @@
         # ERM enterprise risks
         for r in db.execute(
-            "SELECT id, title, category, status FROM erm_enterprise_risks WHERE title LIKE ? LIMIT 10",
+            "SELECT id, title, category, status FROM erm_enterprise_risks WHERE title LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -204,5 +204,5 @@
         # ERM regulatory obligations
         for r in db.execute(
-            "SELECT id, regulation_name, regulator, obligation FROM erm_regulatory_obligations WHERE regulation_name LIKE ? OR obligation LIKE ? LIMIT 10",
+            "SELECT id, regulation_name, regulator, obligation FROM erm_regulatory_obligations WHERE regulation_name LIKE %s OR obligation LIKE %s LIMIT 10",
             (search_term, search_term)
         ).fetchall():
@@ -213,5 +213,5 @@
         # ORM events
         for r in db.execute(
-            "SELECT id, title, event_type, severity FROM orm_events WHERE title LIKE ? LIMIT 10",
+            "SELECT id, title, event_type, severity FROM orm_events WHERE title LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -222,5 +222,5 @@
         # ORM KRIs
         for r in db.execute(
-            "SELECT id, name, description FROM orm_kris WHERE name LIKE ? LIMIT 10",
+            "SELECT id, name, description FROM orm_kris WHERE name LIKE %s LIMIT 10",
             (search_term,)
         ).fetchall():
@@ -288,5 +288,5 @@
             "INSERT INTO calendar_events (title, description, event_type, module, entity_type, "
             "entity_id, start_date, end_date, all_day, recurrence, assigned_to, created_by) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 data.get("title", ""),
@@ -338,5 +338,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM calendar_events WHERE id = ?", (eid,))
+        db.execute("DELETE FROM calendar_events WHERE id = %s", (eid,))
         db.commit()
     finally:
@@ -402,5 +402,5 @@
             db.execute(
                 "INSERT INTO analytics_snapshots (snapshot_date, metric_name, metric_value, module) "
-                "VALUES (?,?,?,?) "
+                "VALUES (%s,%s,%s,%s) "
                 "ON CONFLICT (snapshot_date, metric_name, module) DO UPDATE SET "
                 "metric_value=excluded.metric_value",
@@ -511,5 +511,5 @@
                     db.execute(
                         "INSERT INTO aria_controls (framework_id, control_id, title, description, status, evidence_notes) "
-                        "VALUES (?,?,?,?,?,?)",
+                        "VALUES (%s,%s,%s,%s,%s,%s)",
                         (rec.get("framework_id", 1), rec.get("control_id", ""), rec.get("title", ""),
                          rec.get("description", ""), rec.get("status", "not_implemented"), rec.get("evidence_notes", ""))
@@ -529,5 +529,5 @@
                         "INSERT INTO risk_register (title, description, source_module, category, "
                         "likelihood, impact, risk_level, treatment, status, created_by) "
-                        "VALUES (?,?,?,?,?,?,?,?,?,?)",
+                        "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
                         (rec.get("title", ""), rec.get("description", ""), rec.get("source_module", ""),
                          rec.get("category", "operational"), lh, imp, level,
@@ -543,5 +543,5 @@
                     db.execute(
                         "INSERT INTO evidence_items (title, description, category, tags, created_by) "
-                        "VALUES (?,?,?,?,?)",
+                        "VALUES (%s,%s,%s,%s,%s)",
                         (rec.get("title", ""), rec.get("description", ""), rec.get("category", "policy"),
                          rec.get("tags", ""), request.state.user["id"])
@@ -557,5 +557,5 @@
                         "INSERT INTO sentinel_ropa (ref_number, name, purpose, lawful_basis, "
                         "data_subjects, data_categories, status) "
-                        "VALUES (?,?,?,?,?,?,?)",
+                        "VALUES (%s,%s,%s,%s,%s,%s,%s)",
                         (rec.get("ref_number", ""), rec.get("name", ""), rec.get("purpose", ""),
                          rec.get("lawful_basis", ""), rec.get("data_subjects", ""),
@@ -656,5 +656,5 @@
             "INSERT INTO task_board (title, description, module, entity_type, entity_id, "
             "assigned_to, priority, status, due_date, tags, created_by) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 data.get("title", ""),
@@ -680,5 +680,5 @@
         try:
             db2.execute(
-                "INSERT INTO notifications (user_id, title, message, link, category) VALUES (?,?,?,?,?)",
+                "INSERT INTO notifications (user_id, title, message, link, category) VALUES (%s,%s,%s,%s,%s)",
                 (data["assigned_to"], f"New Task: {data.get('title','')}", data.get("description","")[:100],
                  "/tasks", "task")
@@ -717,5 +717,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM task_board WHERE id = ?", (tid,))
+        db.execute("DELETE FROM task_board WHERE id = %s", (tid,))
         db.commit()
     finally:
@@ -735,5 +735,5 @@
         ).fetchall()
         my_pending = db.execute(
-            "SELECT COUNT(*) FROM task_board WHERE assigned_to = ? AND status NOT IN ('done','cancelled')",
+            "SELECT COUNT(*) FROM task_board WHERE assigned_to = %s AND status NOT IN ('done','cancelled')",
             (uid,)
         ).fetchone()[0]
@@ -838,5 +838,5 @@
                FROM email_reminders r
                LEFT JOIN users u ON r.recipient_id = u.id
-               WHERE r.created_by = ?"""
+               WHERE r.created_by = %s"""
         params = [uid]
         if module:
@@ -873,5 +873,5 @@
         db = get_db()
         try:
-            u = db.execute("SELECT email FROM users WHERE id = ?", (recipient_id,)).fetchone()
+            u = db.execute("SELECT email FROM users WHERE id = %s", (recipient_id,)).fetchone()
             recipient_email = u["email"] if u else user.get("email", "")
         finally:
@@ -884,5 +884,5 @@
                                          recipient_id, recipient_email, remind_at,
                                          repeat_interval, created_by)
-            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
+            VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
         """, (
             data.get("module", "platform"),
@@ -910,5 +910,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM email_reminders WHERE id = ? AND created_by = ?", (rid, uid))
+        db.execute("DELETE FROM email_reminders WHERE id = %s AND created_by = %s", (rid, uid))
         db.commit()
     finally:
[WARN] modules/launcher/routes_reports.py:
  line 91: f-string SQL with ? — manual review needed
  line 98: f-string SQL with ? — manual review needed
--- a/modules/launcher/routes_reports.py
+++ b/modules/launcher/routes_reports.py
@@ -60,5 +60,5 @@
         db.execute(
             "INSERT INTO report_definitions (name, description, report_type, modules, parameters_json, schedule, created_by) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (
                 data.get("name", ""),
@@ -111,5 +111,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE report_definitions SET is_active = 0 WHERE id = ?", (rid,))
+        db.execute("UPDATE report_definitions SET is_active = 0 WHERE id = %s", (rid,))
         db.commit()
     finally:
@@ -124,5 +124,5 @@
     db = get_db()
     try:
-        defn = db.execute("SELECT * FROM report_definitions WHERE id = ?", (rid,)).fetchone()
+        defn = db.execute("SELECT * FROM report_definitions WHERE id = %s", (rid,)).fetchone()
         if not defn:
             return _JSONResp({"error": "Report not found"}, status_code=404)
@@ -130,5 +130,5 @@
         # Create a run record
         db.execute(
-            "INSERT INTO report_runs (definition_id, status, triggered_by) VALUES (?,?,?)",
+            "INSERT INTO report_runs (definition_id, status, triggered_by) VALUES (%s,%s,%s)",
             (rid, "running", "manual")
         )
@@ -203,5 +203,5 @@
         # Update run record
         db.execute(
-            "UPDATE report_runs SET status = 'completed', completed_at = datetime('now'), result_json = ? WHERE id = ?",
+            "UPDATE report_runs SET status = 'completed', completed_at = datetime('now'), result_json = %s WHERE id = %s",
             (json_lib.dumps(result), run_id)
         )
@@ -247,5 +247,5 @@
             "SELECT rr.*, rd.name as report_name, rd.report_type, rd.description "
             "FROM report_runs rr LEFT JOIN report_definitions rd ON rr.definition_id = rd.id "
-            "WHERE rr.id = ?", (run_id,)
+            "WHERE rr.id = %s", (run_id,)
         ).fetchone()
         if not row:
[WARN] modules/launcher/routes_risks.py:
  line 225: f-string SQL with ? — manual review needed
  line 239: f-string SQL with ? — manual review needed
--- a/modules/launcher/routes_risks.py
+++ b/modules/launcher/routes_risks.py
@@ -123,5 +123,5 @@
             "source_entity_id, category, likelihood, impact, risk_level, owner_id, treatment, "
             "treatment_plan, status, review_date, created_by) "
-            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (
                 data.get("title", "Untitled Risk"),
@@ -200,5 +200,5 @@
         row = db.execute(
             "SELECT r.*, u.full_name as owner_name FROM risk_register r "
-            "LEFT JOIN users u ON r.owner_id = u.id WHERE r.id = ?", (rid,)
+            "LEFT JOIN users u ON r.owner_id = u.id WHERE r.id = %s", (rid,)
         ).fetchone()
         if not row:
@@ -227,5 +227,5 @@
         # Recalculate risk_level if likelihood or impact changed
         if "likelihood" in data or "impact" in data:
-            current = db.execute("SELECT likelihood, impact FROM risk_register WHERE id = ?", (rid,)).fetchone()
+            current = db.execute("SELECT likelihood, impact FROM risk_register WHERE id = %s", (rid,)).fetchone()
             l = int(data.get("likelihood", current["likelihood"]))
             i = int(data.get("impact", current["impact"]))
@@ -250,5 +250,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE risk_register SET status = 'closed', updated_at = datetime('now') WHERE id = ?", (rid,))
+        db.execute("UPDATE risk_register SET status = 'closed', updated_at = datetime('now') WHERE id = %s", (rid,))
         db.commit()
     finally:
[WARN] modules/launcher/routes_vendors.py:
  line 65: f-string SQL with ? — manual review needed
--- a/modules/launcher/routes_vendors.py
+++ b/modules/launcher/routes_vendors.py
@@ -67,5 +67,5 @@
                 )
         db.commit()
-        vendor = dict(db.execute("SELECT * FROM canonical_vendors WHERE id=?", (cid,)).fetchone())
+        vendor = dict(db.execute("SELECT * FROM canonical_vendors WHERE id=%s", (cid,)).fetchone())
         return JSONResponse({"id": cid, "vendor": vendor}, status_code=201)
     finally:
[WARN] modules/launcher/routes_workflows.py:
  line 264: f-string SQL with ? — manual review needed
  line 352: f-string SQL with ? — manual review needed
  line 432: f-string SQL with ? — manual review needed
  line 448: f-string SQL with ? — manual review needed
  line 462: f-string SQL with ? — manual review needed
  line 570: f-string SQL with ? — manual review needed
  line 574: f-string SQL with ? — manual review needed
  line 889: f-string SQL with ? — manual review needed
  line 898: f-string SQL with ? — manual review needed
--- a/modules/launcher/routes_workflows.py
+++ b/modules/launcher/routes_workflows.py
@@ -152,5 +152,5 @@
         # Check if already installed
         existing = db.execute(
-            "SELECT id FROM workflow_definitions WHERE name = ?", (template["name"],)
+            "SELECT id FROM workflow_definitions WHERE name = %s", (template["name"],)
         ).fetchone()
         if existing:
@@ -158,5 +158,5 @@
         db.execute(
             "INSERT INTO workflow_definitions (name, description, trigger_module, trigger_action, steps_json, created_by) "
-            "VALUES (?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s)",
             (
                 template["name"],
@@ -215,5 +215,5 @@
         db.execute(
             "INSERT INTO workflow_definitions (name, description, trigger_module, trigger_action, steps_json, created_by) "
-            "VALUES (?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s)",
             (
                 data.get("name", "Untitled Workflow"),
@@ -277,5 +277,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE workflow_definitions SET is_active = 0 WHERE id = ?", (wid,))
+        db.execute("UPDATE workflow_definitions SET is_active = 0 WHERE id = %s", (wid,))
         db.commit()
     finally:
@@ -321,5 +321,5 @@
     db = get_db()
     try:
-        defn = db.execute("SELECT * FROM workflow_definitions WHERE id = ? AND is_active = 1",
+        defn = db.execute("SELECT * FROM workflow_definitions WHERE id = %s AND is_active = 1",
                           (data.get("definition_id"),)).fetchone()
         if not defn:
@@ -328,5 +328,5 @@
         db.execute(
             "INSERT INTO workflow_instances (definition_id, entity_module, entity_type, entity_id, started_by) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (defn["id"], data.get("entity_module", ""), data.get("entity_type", ""),
              data.get("entity_id"), request.state.user["id"])
@@ -340,5 +340,5 @@
             first_step = steps[0]
             db.execute(
-                "INSERT INTO workflow_actions (instance_id, step_index, action_type, assigned_to) VALUES (?,?,?,?)",
+                "INSERT INTO workflow_actions (instance_id, step_index, action_type, assigned_to) VALUES (%s,%s,%s,%s)",
                 (iid, 0, first_step.get("action_type", "approve"), first_step.get("assigned_to"))
             )
@@ -347,5 +347,5 @@
             if first_step.get("assigned_to"):
                 db.execute(
-                    "INSERT INTO notifications (user_id, title, message, link, category) VALUES (?,?,?,?,?)",
+                    "INSERT INTO notifications (user_id, title, message, link, category) VALUES (%s,%s,%s,%s,%s)",
                     (first_step["assigned_to"], f"Action Required: {defn['name']}",
                      f"Step 1: {first_step.get('label', 'Review & Approve')}",
@@ -369,5 +369,5 @@
             "LEFT JOIN workflow_definitions wd ON wi.definition_id = wd.id "
             "LEFT JOIN users u ON wi.started_by = u.id "
-            "WHERE wi.id = ?", (iid,)
+            "WHERE wi.id = %s", (iid,)
         ).fetchone()
         if not inst:
@@ -376,5 +376,5 @@
             "SELECT wa.*, u.full_name as assigned_name "
             "FROM workflow_actions wa LEFT JOIN users u ON wa.assigned_to = u.id "
-            "WHERE wa.instance_id = ? ORDER BY wa.step_index",
+            "WHERE wa.instance_id = %s ORDER BY wa.step_index",
             (iid,)
         ).fetchall()
@@ -396,5 +396,5 @@
     db = get_db()
     try:
-        action = db.execute("SELECT * FROM workflow_actions WHERE id = ?", (aid,)).fetchone()
+        action = db.execute("SELECT * FROM workflow_actions WHERE id = %s", (aid,)).fetchone()
         if not action:
             return _JSONResp({"error": "Action not found"}, status_code=404)
@@ -404,5 +404,5 @@
         # Update action
         db.execute(
-            "UPDATE workflow_actions SET status = ?, comment = ?, acted_at = datetime('now') WHERE id = ?",
+            "UPDATE workflow_actions SET status = %s, comment = %s, acted_at = datetime('now') WHERE id = %s",
             (decision + "d", comment, aid)  # approved, rejected, returned
         )
@@ -410,6 +410,6 @@
 
         iid = action["instance_id"]
-        inst = db.execute("SELECT * FROM workflow_instances WHERE id = ?", (iid,)).fetchone()
-        defn = db.execute("SELECT * FROM workflow_definitions WHERE id = ?", (inst["definition_id"],)).fetchone()
+        inst = db.execute("SELECT * FROM workflow_instances WHERE id = %s", (iid,)).fetchone()
+        defn = db.execute("SELECT * FROM workflow_definitions WHERE id = %s", (inst["definition_id"],)).fetchone()
         steps = json_lib.loads(defn["steps_json"]) if defn["steps_json"] else []
 
@@ -418,8 +418,8 @@
             if next_step < len(steps):
                 # Advance to next step
-                db.execute("UPDATE workflow_instances SET current_step = ? WHERE id = ?", (next_step, iid))
+                db.execute("UPDATE workflow_instances SET current_step = %s WHERE id = %s", (next_step, iid))
                 next_s = steps[next_step]
                 db.execute(
-                    "INSERT INTO workflow_actions (instance_id, step_index, action_type, assigned_to) VALUES (?,?,?,?)",
+                    "INSERT INTO workflow_actions (instance_id, step_index, action_type, assigned_to) VALUES (%s,%s,%s,%s)",
                     (iid, next_step, next_s.get("action_type", "approve"), next_s.get("assigned_to"))
                 )
@@ -427,5 +427,5 @@
                 if next_s.get("assigned_to"):
                     db.execute(
-                        "INSERT INTO notifications (user_id, title, message, link, category) VALUES (?,?,?,?,?)",
+                        "INSERT INTO notifications (user_id, title, message, link, category) VALUES (%s,%s,%s,%s,%s)",
                         (next_s["assigned_to"], f"Action Required: {defn['name']}",
                          f"Step {next_step + 1}: {next_s.get('label', 'Review')}",
@@ -436,5 +436,5 @@
                 # Workflow complete
                 db.execute(
-                    "UPDATE workflow_instances SET status = 'completed', completed_at = datetime('now') WHERE id = ?",
+                    "UPDATE workflow_instances SET status = 'completed', completed_at = datetime('now') WHERE id = %s",
                     (iid,)
                 )
@@ -443,5 +443,5 @@
                 if inst["started_by"]:
                     db.execute(
-                        "INSERT INTO notifications (user_id, title, message, link, category) VALUES (?,?,?,?,?)",
+                        "INSERT INTO notifications (user_id, title, message, link, category) VALUES (%s,%s,%s,%s,%s)",
                         (inst["started_by"], f"Workflow Completed: {defn['name']}",
                          "All approval steps have been completed.",
@@ -451,5 +451,5 @@
         elif decision == "reject":
             db.execute(
-                "UPDATE workflow_instances SET status = 'rejected', completed_at = datetime('now') WHERE id = ?",
+                "UPDATE workflow_instances SET status = 'rejected', completed_at = datetime('now') WHERE id = %s",
                 (iid,)
             )
@@ -457,5 +457,5 @@
             if inst["started_by"]:
                 db.execute(
-                    "INSERT INTO notifications (user_id, title, message, link, category) VALUES (?,?,?,?,?)",
+                    "INSERT INTO notifications (user_id, title, message, link, category) VALUES (%s,%s,%s,%s,%s)",
                     (inst["started_by"], f"Workflow Rejected: {defn['name']}",
                      f"Rejected at step {action['step_index'] + 1}: {comment}",
@@ -464,5 +464,5 @@
                 db.commit()
         elif decision == "return":
-            db.execute("UPDATE workflow_instances SET status = 'returned' WHERE id = ?", (iid,))
+            db.execute("UPDATE workflow_instances SET status = 'returned' WHERE id = %s", (iid,))
             db.commit()
     finally:
@@ -483,5 +483,5 @@
             "JOIN workflow_instances wi ON wa.instance_id = wi.id "
             "JOIN workflow_definitions wd ON wi.definition_id = wd.id "
-            "WHERE wa.assigned_to = ? AND wa.status = 'pending' "
+            "WHERE wa.assigned_to = %s AND wa.status = 'pending' "
             "ORDER BY wa.created_at DESC",
             (uid,)
@@ -504,5 +504,5 @@
         module = request.query_params.get("module", "")
         if module:
-            rows = db.execute("SELECT * FROM sla_definitions WHERE module = ? ORDER BY name", (module,)).fetchall()
+            rows = db.execute("SELECT * FROM sla_definitions WHERE module = %s ORDER BY name", (module,)).fetchall()
         else:
             rows = db.execute("SELECT * FROM sla_definitions ORDER BY module, name").fetchall()
@@ -545,5 +545,5 @@
         db.execute(
             "INSERT INTO sla_definitions (name, module, entity_type, response_hours, resolution_hours, escalation_hours, priority) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (name, module, entity_type, response_hours, resolution_hours, escalation_hours, priority)
         )
@@ -592,5 +592,5 @@
     db = get_db()
     try:
-        defn = db.execute("SELECT * FROM sla_definitions WHERE id = ? AND is_active = 1",
+        defn = db.execute("SELECT * FROM sla_definitions WHERE id = %s AND is_active = 1",
                           (int(def_id),)).fetchone()
         if not defn:
@@ -604,13 +604,13 @@
         if defn["response_hours"]:
             response_due = db.execute(
-                "SELECT datetime(?, '+' || ? || ' hours')", (now, int(defn["response_hours"]))
+                "SELECT datetime(%s, '+' || %s || ' hours')", (now, int(defn["response_hours"]))
             ).fetchone()[0]
         if defn["resolution_hours"]:
             resolution_due = db.execute(
-                "SELECT datetime(?, '+' || ? || ' hours')", (now, int(defn["resolution_hours"]))
+                "SELECT datetime(%s, '+' || %s || ' hours')", (now, int(defn["resolution_hours"]))
             ).fetchone()[0]
         if defn["escalation_hours"]:
             escalation_due = db.execute(
-                "SELECT datetime(?, '+' || ? || ' hours')", (now, int(defn["escalation_hours"]))
+                "SELECT datetime(%s, '+' || %s || ' hours')", (now, int(defn["escalation_hours"]))
             ).fetchone()[0]
 
@@ -618,5 +618,5 @@
             "INSERT INTO sla_instances (definition_id, entity_module, entity_type, entity_id, "
             "started_at, response_due, resolution_due, escalation_due) "
-            "VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
+            "VALUES (%s, %s, %s, %s, %s, %s, %s, %s)",
             (defn["id"], entity_module, entity_type, entity_id,
              now, response_due, resolution_due, escalation_due)
@@ -670,5 +670,5 @@
     db = get_db()
     try:
-        inst = db.execute("SELECT * FROM sla_instances WHERE id = ?", (iid,)).fetchone()
+        inst = db.execute("SELECT * FROM sla_instances WHERE id = %s", (iid,)).fetchone()
         if not inst:
             return _JSONResp({"error": "Instance not found"}, status_code=404)
@@ -682,5 +682,5 @@
             breach_type = "response"
         db.execute(
-            "UPDATE sla_instances SET responded_at = ?, breached = MAX(breached, ?), breach_type = COALESCE(breach_type, ?) WHERE id = ?",
+            "UPDATE sla_instances SET responded_at = %s, breached = MAX(breached, %s), breach_type = COALESCE(breach_type, %s) WHERE id = %s",
             (now, breached, breach_type, iid)
         )
@@ -698,5 +698,5 @@
     db = get_db()
     try:
-        inst = db.execute("SELECT * FROM sla_instances WHERE id = ?", (iid,)).fetchone()
+        inst = db.execute("SELECT * FROM sla_instances WHERE id = %s", (iid,)).fetchone()
         if not inst:
             return _JSONResp({"error": "Instance not found"}, status_code=404)
@@ -710,6 +710,6 @@
             breach_type = breach_type or "resolution"
         db.execute(
-            "UPDATE sla_instances SET resolved_at = ?, status = 'resolved', "
-            "breached = MAX(breached, ?), breach_type = COALESCE(breach_type, ?) WHERE id = ?",
+            "UPDATE sla_instances SET resolved_at = %s, status = 'resolved', "
+            "breached = MAX(breached, %s), breach_type = COALESCE(breach_type, %s) WHERE id = %s",
             (now, breached, breach_type, iid)
         )
@@ -727,10 +727,10 @@
     db = get_db()
     try:
-        inst = db.execute("SELECT * FROM sla_instances WHERE id = ?", (iid,)).fetchone()
+        inst = db.execute("SELECT * FROM sla_instances WHERE id = %s", (iid,)).fetchone()
         if not inst:
             return _JSONResp({"error": "Instance not found"}, status_code=404)
         now = db.execute("SELECT datetime('now')").fetchone()[0]
         db.execute(
-            "UPDATE sla_instances SET escalated_at = ? WHERE id = ? AND escalated_at IS NULL",
+            "UPDATE sla_instances SET escalated_at = %s WHERE id = %s AND escalated_at IS NULL",
             (now, iid)
         )
@@ -753,5 +753,5 @@
             "UPDATE sla_instances SET breached = 1, breach_type = COALESCE(breach_type, 'response') "
             "WHERE status = 'active' AND breached = 0 AND response_due IS NOT NULL "
-            "AND responded_at IS NULL AND response_due < ?", (now,)
+            "AND responded_at IS NULL AND response_due < %s", (now,)
         ).rowcount
         # Resolution breaches
@@ -759,5 +759,5 @@
             "UPDATE sla_instances SET breached = 1, breach_type = COALESCE(breach_type, 'resolution') "
             "WHERE status = 'active' AND breached = 0 AND resolution_due IS NOT NULL "
-            "AND resolved_at IS NULL AND resolution_due < ?", (now,)
+            "AND resolved_at IS NULL AND resolution_due < %s", (now,)
         ).rowcount
         # Escalation breaches (mark escalation_due passed but don't double-flag breached)
@@ -765,5 +765,5 @@
             "UPDATE sla_instances SET breach_type = CASE WHEN breach_type IS NULL THEN 'escalation' ELSE breach_type END "
             "WHERE status = 'active' AND escalation_due IS NOT NULL "
-            "AND escalated_at IS NULL AND escalation_due < ? AND resolved_at IS NULL", (now,)
+            "AND escalated_at IS NULL AND escalation_due < %s AND resolved_at IS NULL", (now,)
         ).rowcount
         db.commit()
@@ -859,5 +859,5 @@
         db.execute(
             "INSERT INTO comm_templates (name, category, module, subject_template, body_template, variables_json, created_by) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (
                 data.get("name", ""),
@@ -911,5 +911,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE comm_templates SET is_active = 0 WHERE id = ?", (tid,))
+        db.execute("UPDATE comm_templates SET is_active = 0 WHERE id = %s", (tid,))
         db.commit()
     finally:
@@ -925,5 +925,5 @@
     db = get_db()
     try:
-        tmpl = db.execute("SELECT * FROM comm_templates WHERE id = ? AND is_active = 1", (tid,)).fetchone()
+        tmpl = db.execute("SELECT * FROM comm_templates WHERE id = %s AND is_active = 1", (tid,)).fetchone()
         if not tmpl:
             return _JSONResp({"error": "Template not found"}, status_code=404)
[WARN] modules/orm/data_service.py:
  line 46: f-string SQL with ? — manual review needed
  line 121: f-string SQL with ? — manual review needed
  line 126: f-string SQL with ? — manual review needed
  line 200: f-string SQL with ? — manual review needed
  line 206: f-string SQL with ? — manual review needed
  line 295: f-string SQL with ? — manual review needed
  line 298: f-string SQL with ? — manual review needed
  line 374: f-string SQL with ? — manual review needed
  line 491: f-string SQL with ? — manual review needed
  line 494: f-string SQL with ? — manual review needed
  line 559: f-string SQL with ? — manual review needed
  line 562: f-string SQL with ? — manual review needed
  line 616: f-string SQL with ? — manual review needed
  line 619: f-string SQL with ? — manual review needed
  line 680: f-string SQL with ? — manual review needed
  line 683: f-string SQL with ? — manual review needed
  line 712: f-string SQL with ? — manual review needed
--- a/modules/orm/data_service.py
+++ b/modules/orm/data_service.py
@@ -63,5 +63,5 @@
             "LEFT JOIN users u ON u.id=e.reported_by "
             "LEFT JOIN users o ON o.id=e.owner_id "
-            "WHERE e.id=?", (event_id,)
+            "WHERE e.id=%s", (event_id,)
         ).fetchone())
     finally:
@@ -81,5 +81,5 @@
                 reported_by, owner_id, corrective_action, preventive_action, is_recurring,
                 workflow_step, response_due_at, resolution_due_at, basel_category)
-               VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)""",
+               VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)""",
             (data.get("title"), data.get("description"),
              data.get("event_type", "process_failure"),
@@ -99,5 +99,5 @@
         db.execute(
             "INSERT INTO orm_event_workflow_history (event_id, from_step, to_step, changed_by, notes) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (eid, None, "identified", data.get("reported_by"), "Event created")
         )
@@ -133,5 +133,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_events WHERE id=?", (event_id,))
+        db.execute("DELETE FROM orm_events WHERE id=%s", (event_id,))
         db.commit()
     finally:
@@ -142,5 +142,5 @@
     db = get_db()
     try:
-        db.execute("UPDATE orm_events SET erm_risk_id=?, updated_at=? WHERE id=?",
+        db.execute("UPDATE orm_events SET erm_risk_id=%s, updated_at=%s WHERE id=%s",
                    (erm_risk_id, _now(), event_id))
         db.commit()
@@ -159,5 +159,5 @@
             "SELECT k.*, u.full_name AS owner_name "
             "FROM orm_kris k LEFT JOIN users u ON u.id=k.owner_id "
-            "WHERE k.status='active' ORDER BY k.name LIMIT ?", (limit,)
+            "WHERE k.status='active' ORDER BY k.name LIMIT %s", (limit,)
         ).fetchall())
     finally:
@@ -168,5 +168,5 @@
     db = get_db()
     try:
-        return _dict(db.execute("SELECT * FROM orm_kris WHERE id=?", (kri_id,)).fetchone())
+        return _dict(db.execute("SELECT * FROM orm_kris WHERE id=%s", (kri_id,)).fetchone())
     finally:
         db.close()
@@ -178,5 +178,5 @@
         cur = insert_returning_id(db,
             "INSERT INTO orm_kris (name, description, metric_type, threshold_warn, threshold_crit, "
-            "current_value, unit, frequency, owner_id, status) VALUES (?,?,?,?,?,?,?,?,?,?)",
+            "current_value, unit, frequency, owner_id, status) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("name"), data.get("description"), data.get("metric_type", "count"),
              data.get("threshold_warn"), data.get("threshold_crit"),
@@ -208,10 +208,10 @@
             # Record history row for sparkline / trend analysis
             db.execute(
-                "INSERT INTO orm_kri_history (kri_id, value, recorded_by) VALUES (?,?,?)",
+                "INSERT INTO orm_kri_history (kri_id, value, recorded_by) VALUES (%s,%s,%s)",
                 (kri_id, data["current_value"], user_id)
             )
             # Auto-compute trend from last 3 history values
             hist = db.execute(
-                "SELECT value FROM orm_kri_history WHERE kri_id=? ORDER BY recorded_at DESC LIMIT 3",
+                "SELECT value FROM orm_kri_history WHERE kri_id=%s ORDER BY recorded_at DESC LIMIT 3",
                 (kri_id,)
             ).fetchall()
@@ -219,5 +219,5 @@
                 latest = hist[0][0]; prior = hist[-1][0]
                 trend = "improving" if latest < prior else ("worsening" if latest > prior else "stable")
-                db.execute("UPDATE orm_kris SET trend=? WHERE id=?", (trend, kri_id))
+                db.execute("UPDATE orm_kris SET trend=%s WHERE id=%s", (trend, kri_id))
         db.commit()
     finally:
@@ -228,5 +228,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_kris WHERE id=?", (kri_id,))
+        db.execute("DELETE FROM orm_kris WHERE id=%s", (kri_id,))
         db.commit()
     finally:
@@ -256,5 +256,5 @@
     try:
         return _dict(db.execute(
-            "SELECT * FROM orm_event_templates WHERE id=?", (template_id,)
+            "SELECT * FROM orm_event_templates WHERE id=%s", (template_id,)
         ).fetchone())
     finally:
@@ -269,5 +269,5 @@
             "(title, description, category, event_type, severity, department, "
             "process_affected, root_cause_category, corrective_action, preventive_action, "
-            "basel_category, tags, is_active) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)",
+            "basel_category, tags, is_active) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("title"), data.get("description"),
              data.get("category", "Process & Operations"),
@@ -305,5 +305,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_event_templates WHERE id=?", (template_id,))
+        db.execute("DELETE FROM orm_event_templates WHERE id=%s", (template_id,))
         db.commit()
     finally:
@@ -315,5 +315,5 @@
     try:
         db.execute(
-            "UPDATE orm_event_templates SET usage_count = usage_count + 1 WHERE id=?",
+            "UPDATE orm_event_templates SET usage_count = usage_count + 1 WHERE id=%s",
             (template_id,)
         )
@@ -330,5 +330,5 @@
             "SELECT h.*, u.full_name AS recorded_by_name "
             "FROM orm_kri_history h LEFT JOIN users u ON u.id=h.recorded_by "
-            "WHERE h.kri_id=? ORDER BY h.recorded_at DESC LIMIT ?",
+            "WHERE h.kri_id=%s ORDER BY h.recorded_at DESC LIMIT %s",
             (kri_id, limit)
         ).fetchall())
@@ -358,5 +358,5 @@
     db = get_db()
     try:
-        ev = db.execute("SELECT workflow_step, status FROM orm_events WHERE id=?", (event_id,)).fetchone()
+        ev = db.execute("SELECT workflow_step, status FROM orm_events WHERE id=%s", (event_id,)).fetchone()
         if not ev:
             return None
@@ -375,5 +375,5 @@
         db.execute(
             "INSERT INTO orm_event_workflow_history (event_id, from_step, to_step, changed_by, notes) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (event_id, from_step, to_step, user_id, notes)
         )
@@ -391,5 +391,5 @@
             "SELECT h.*, u.full_name AS changed_by_name "
             "FROM orm_event_workflow_history h LEFT JOIN users u ON u.id=h.changed_by "
-            "WHERE h.event_id=? ORDER BY h.changed_at ASC",
+            "WHERE h.event_id=%s ORDER BY h.changed_at ASC",
             (event_id,)
         ).fetchall())
@@ -406,5 +406,5 @@
             "SELECT COUNT(*) FROM orm_events "
             "WHERE status NOT IN ('closed','resolved') "
-            "AND (response_due_at < ? OR resolution_due_at < ?)",
+            "AND (response_due_at < %s OR resolution_due_at < %s)",
             (now, now)
         ).fetchone()
@@ -459,5 +459,5 @@
             "SELECT a.*, u.full_name AS owner_name "
             "FROM orm_rcsa_assessments a LEFT JOIN users u ON u.id=a.owner_id "
-            "WHERE a.id=?", (assessment_id,)
+            "WHERE a.id=%s", (assessment_id,)
         ).fetchone())
     finally:
@@ -470,5 +470,5 @@
         cur = insert_returning_id(db,
             "INSERT INTO orm_rcsa_assessments (title, scope, period_start, period_end, "
-            "status, owner_id, due_date, notes, created_by) VALUES (?,?,?,?,?,?,?,?,?)",
+            "status, owner_id, due_date, notes, created_by) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("title"), data.get("scope"), data.get("period_start"),
              data.get("period_end"), data.get("status", "draft"),
@@ -501,5 +501,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_rcsa_assessments WHERE id=?", (assessment_id,))
+        db.execute("DELETE FROM orm_rcsa_assessments WHERE id=%s", (assessment_id,))
         db.commit()
     finally:
@@ -514,5 +514,5 @@
             "(SELECT COUNT(*) FROM orm_rcsa_controls c WHERE c.risk_id=r.id) AS control_count "
             "FROM orm_rcsa_risks r LEFT JOIN users u ON u.id=r.owner_id "
-            "WHERE r.assessment_id=? ORDER BY r.inherent_likelihood*r.inherent_impact DESC",
+            "WHERE r.assessment_id=%s ORDER BY r.inherent_likelihood*r.inherent_impact DESC",
             (assessment_id,)
         ).fetchall())
@@ -531,5 +531,5 @@
             "INSERT INTO orm_rcsa_risks (assessment_id, title, category, inherent_likelihood, "
             "inherent_impact, control_effectiveness, residual_score, owner_id, notes) "
-            "VALUES (?,?,?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("assessment_id"), data.get("title"), data.get("category", "operational"),
              il, ii, ce, residual, data.get("owner_id"), data.get("notes"))
@@ -547,5 +547,5 @@
         if any(k in data for k in ("inherent_likelihood", "inherent_impact", "control_effectiveness")):
             cur = db.execute("SELECT inherent_likelihood, inherent_impact, control_effectiveness "
-                             "FROM orm_rcsa_risks WHERE id=?", (risk_id,)).fetchone()
+                             "FROM orm_rcsa_risks WHERE id=%s", (risk_id,)).fetchone()
             if cur:
                 il = data.get("inherent_likelihood", cur["inherent_likelihood"])
@@ -569,5 +569,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_rcsa_risks WHERE id=?", (risk_id,))
+        db.execute("DELETE FROM orm_rcsa_risks WHERE id=%s", (risk_id,))
         db.commit()
     finally:
@@ -581,5 +581,5 @@
             "SELECT c.*, u.full_name AS tested_by_name "
             "FROM orm_rcsa_controls c LEFT JOIN users u ON u.id=c.tested_by "
-            "WHERE c.risk_id=? ORDER BY c.created_at",
+            "WHERE c.risk_id=%s ORDER BY c.created_at",
             (risk_id,)
         ).fetchall())
@@ -594,5 +594,5 @@
             "INSERT INTO orm_rcsa_controls (risk_id, name, aria_control_id, "
             "design_effectiveness, operating_effectiveness, test_date, tested_by, "
-            "evidence_notes, gap_description) VALUES (?,?,?,?,?,?,?,?,?)",
+            "evidence_notes, gap_description) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s)",
             (data.get("risk_id"), data.get("name"), data.get("aria_control_id"),
              data.get("design_effectiveness", "adequate"),
@@ -626,5 +626,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_rcsa_controls WHERE id=?", (control_id,))
+        db.execute("DELETE FROM orm_rcsa_controls WHERE id=%s", (control_id,))
         db.commit()
     finally:
@@ -643,5 +643,5 @@
                 "JOIN orm_rcsa_controls c ON c.id=a.control_id "
                 "JOIN orm_rcsa_risks r ON r.id=c.risk_id "
-                "WHERE r.assessment_id=? ORDER BY a.due_date",
+                "WHERE r.assessment_id=%s ORDER BY a.due_date",
                 (assessment_id,)
             ).fetchall())
@@ -649,5 +649,5 @@
             "SELECT a.*, u.full_name AS owner_name "
             "FROM orm_rcsa_actions a LEFT JOIN users u ON u.id=a.owner_id "
-            "WHERE a.control_id=? ORDER BY a.due_date",
+            "WHERE a.control_id=%s ORDER BY a.due_date",
             (control_id,)
         ).fetchall())
@@ -661,5 +661,5 @@
         cur = insert_returning_id(db,
             "INSERT INTO orm_rcsa_actions (control_id, title, description, owner_id, due_date, status, notes) "
-            "VALUES (?,?,?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s,%s,%s)",
             (data.get("control_id"), data.get("title"), data.get("description"),
              data.get("owner_id"), data.get("due_date"), data.get("status", "open"),
@@ -690,5 +690,5 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_rcsa_actions WHERE id=?", (action_id,))
+        db.execute("DELETE FROM orm_rcsa_actions WHERE id=%s", (action_id,))
         db.commit()
     finally:
@@ -716,32 +716,32 @@
         total = count_period()
         prev_total = db.execute(
-            "SELECT COUNT(*) FROM orm_events WHERE created_at >= ? AND created_at < ?",
+            "SELECT COUNT(*) FROM orm_events WHERE created_at >= %s AND created_at < %s",
             (prev_since, prev_to)
         ).fetchone()[0]
 
         fin_loss = db.execute(
-            "SELECT COALESCE(SUM(financial_impact),0) FROM orm_events WHERE created_at >= ?",
+            "SELECT COALESCE(SUM(financial_impact),0) FROM orm_events WHERE created_at >= %s",
             (since,)
         ).fetchone()[0]
         prev_fin = db.execute(
-            "SELECT COALESCE(SUM(financial_impact),0) FROM orm_events WHERE created_at >= ? AND created_at < ?",
+            "SELECT COALESCE(SUM(financial_impact),0) FROM orm_events WHERE created_at >= %s AND created_at < %s",
             (prev_since, prev_to)
         ).fetchone()[0]
 
         customers = db.execute(
-            "SELECT COALESCE(SUM(customers_affected),0) FROM orm_events WHERE created_at >= ?",
+            "SELECT COALESCE(SUM(customers_affected),0) FROM orm_events WHERE created_at >= %s",
             (since,)
         ).fetchone()[0]
         prev_customers = db.execute(
-            "SELECT COALESCE(SUM(customers_affected),0) FROM orm_events WHERE created_at >= ? AND created_at < ?",
+            "SELECT COALESCE(SUM(customers_affected),0) FROM orm_events WHERE created_at >= %s AND created_at < %s",
             (prev_since, prev_to)
         ).fetchone()[0]
 
         downtime_mins = db.execute(
-            "SELECT COALESCE(SUM(downtime_minutes),0) FROM orm_events WHERE created_at >= ?",
+            "SELECT COALESCE(SUM(downtime_minutes),0) FROM orm_events WHERE created_at >= %s",
             (since,)
         ).fetchone()[0]
         prev_downtime = db.execute(
-            "SELECT COALESCE(SUM(downtime_minutes),0) FROM orm_events WHERE created_at >= ? AND created_at < ?",
+            "SELECT COALESCE(SUM(downtime_minutes),0) FROM orm_events WHERE created_at >= %s AND created_at < %s",
             (prev_since, prev_to)
         ).fetchone()[0]
@@ -749,5 +749,5 @@
         # By type
         type_rows = _dicts(db.execute(
-            "SELECT event_type, COUNT(*) AS cnt FROM orm_events WHERE created_at >= ? GROUP BY event_type ORDER BY cnt DESC",
+            "SELECT event_type, COUNT(*) AS cnt FROM orm_events WHERE created_at >= %s GROUP BY event_type ORDER BY cnt DESC",
             (since,)
         ).fetchall())
@@ -756,5 +756,5 @@
         top5 = _dicts(db.execute(
             "SELECT id, title, event_type, financial_impact, severity FROM orm_events "
-            "WHERE created_at >= ? AND financial_impact > 0 ORDER BY financial_impact DESC LIMIT 5",
+            "WHERE created_at >= %s AND financial_impact > 0 ORDER BY financial_impact DESC LIMIT 5",
             (since,)
         ).fetchall())
@@ -771,5 +771,5 @@
             "SELECT COUNT(*) FROM orm_events "
             "WHERE status NOT IN ('closed','resolved') "
-            "AND (response_due_at < ? OR resolution_due_at < ?)",
+            "AND (response_due_at < %s OR resolution_due_at < %s)",
             (now_str, now_str)
         ).fetchone()[0]
@@ -846,5 +846,5 @@
     try:
         rows = _dicts(db.execute(
-            "SELECT * FROM orm_chat_messages WHERE user_id=? ORDER BY created_at DESC LIMIT ?",
+            "SELECT * FROM orm_chat_messages WHERE user_id=%s ORDER BY created_at DESC LIMIT %s",
             (user_id, limit)
         ).fetchall())
@@ -859,5 +859,5 @@
     try:
         db.execute(
-            "INSERT INTO orm_chat_messages (user_id, role, content, provider) VALUES (?,?,?,?)",
+            "INSERT INTO orm_chat_messages (user_id, role, content, provider) VALUES (%s,%s,%s,%s)",
             (user_id, role, content, provider)
         )
@@ -870,6 +870,6 @@
     db = get_db()
     try:
-        db.execute("DELETE FROM orm_chat_messages WHERE user_id=?", (user_id,))
-        db.commit()
-    finally:
-        db.close()
+        db.execute("DELETE FROM orm_chat_messages WHERE user_id=%s", (user_id,))
+        db.commit()
+    finally:
+        db.close()
[WARN] modules/sentinel/ai_service.py:
  line 97: f-string SQL with ? — manual review needed
  line 725: f-string SQL with ? — manual review needed
[WARN] modules/sentinel/data_service.py:
  line 111: f-string SQL with ? — manual review needed
  line 122: f-string SQL with ? — manual review needed
  line 137: f-string SQL with ? — manual review needed
  line 139: f-string SQL with ? — manual review needed
--- a/modules/sentinel/data_service.py
+++ b/modules/sentinel/data_service.py
@@ -778,5 +778,5 @@
     db = get_db()
     try:
-        row = db.execute("SELECT value FROM settings WHERE key=?", (key,)).fetchone()
+        row = db.execute("SELECT value FROM settings WHERE key=%s", (key,)).fetchone()
     finally:
         db.close()
@@ -788,5 +788,5 @@
     try:
         db.execute(
-            "INSERT INTO settings(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value",
+            "INSERT INTO settings(key,value) VALUES(%s,%s) ON CONFLICT(key) DO UPDATE SET value=excluded.value",
             (key, value),
         )
@@ -813,5 +813,5 @@
     try:
         rows = db.execute(
-            "SELECT * FROM audit_log WHERE module='sentinel' ORDER BY created_at DESC LIMIT ?",
+            "SELECT * FROM audit_log WHERE module='sentinel' ORDER BY created_at DESC LIMIT %s",
             (limit,),
         ).fetchall()
@@ -1023,5 +1023,5 @@
     try:
         db.execute(
-            "UPDATE sentinel_jurisdiction_config SET is_active=0 WHERE jurisdiction_key=?",
+            "UPDATE sentinel_jurisdiction_config SET is_active=0 WHERE jurisdiction_key=%s",
             (key,),
         )
[WARN] modules/sentinel/scheduler.py:
  line 236: f-string SQL with ? — manual review needed
--- a/modules/sentinel/scheduler.py
+++ b/modules/sentinel/scheduler.py
@@ -69,5 +69,5 @@
     try:
         db.execute(
-            "INSERT INTO notifications (user_id, module, title, message, link) VALUES (?,?,?,?,?)",
+            "INSERT INTO notifications (user_id, module, title, message, link) VALUES (%s,%s,%s,%s,%s)",
             (user_id, module, title, message, link),
         )
@@ -89,5 +89,5 @@
         for r in rows:
             db.execute(
-                "INSERT INTO notifications (user_id, module, title, message, link) VALUES (?,?,?,?,?)",
+                "INSERT INTO notifications (user_id, module, title, message, link) VALUES (%s,%s,%s,%s,%s)",
                 (r[0], module, title, message, link),
             )
@@ -392,5 +392,5 @@
         db2.execute(
             "INSERT INTO task_board (title, description, module, priority, status) "
-            "VALUES (?,?,?,?,?)",
+            "VALUES (%s,%s,%s,%s,%s)",
             (f"Retention Review: {len(due)} schedule(s) due",
              f"Review retention periods for: {items}. "
--- a/seeds/control_mappings.py
+++ b/seeds/control_mappings.py
@@ -360,9 +360,9 @@
                 # Resolve framework IDs from names
                 src_fw_row = db.execute(
-                    "SELECT id FROM frameworks WHERE name=? OR name LIKE ?",
+                    "SELECT id FROM frameworks WHERE name=%s OR name LIKE %s",
                     (src_fw_name, src_fw_name + '%')
                 ).fetchone()
                 tgt_fw_row = db.execute(
-                    "SELECT id FROM frameworks WHERE name=? OR name LIKE ?",
+                    "SELECT id FROM frameworks WHERE name=%s OR name LIKE %s",
                     (tgt_fw_name, tgt_fw_name + '%')
                 ).fetchone()
@@ -377,9 +377,9 @@
                 # Resolve control IDs from refs
                 src_ctrl = db.execute(
-                    "SELECT id FROM controls WHERE framework_id=? AND ref=?",
+                    "SELECT id FROM controls WHERE framework_id=%s AND ref=%s",
                     (src_fw_id, src_ref)
                 ).fetchone()
                 tgt_ctrl = db.execute(
-                    "SELECT id FROM controls WHERE framework_id=? AND ref=?",
+                    "SELECT id FROM controls WHERE framework_id=%s AND ref=%s",
                     (tgt_fw_id, tgt_ref)
                 ).fetchone()
@@ -398,5 +398,5 @@
                     "target_control_id, mapping_type, confidence, notes, "
                     "auto_generated, match_method) "
-                    "VALUES (?, ?, ?, ?, ?, ?, ?, 0, 'prebuilt') ON CONFLICT DO NOTHING",
+                    "VALUES (%s, %s, %s, %s, %s, %s, %s, 0, 'prebuilt') ON CONFLICT DO NOTHING",
                     (src_fw_id, src_ctrl_id, tgt_fw_id, tgt_ctrl_id, mtype, conf, "")
                 )
--- a/seeds/seed.py
+++ b/seeds/seed.py
@@ -45,10 +45,10 @@
     db.execute(
         "INSERT INTO users (username, email, full_name, password_hash, avatar_initials) "
-        "VALUES (?, ?, ?, ?, ?)",
+        "VALUES (%s, %s, %s, %s, %s)",
         ("admin", "admin@oneforall.local", "System Administrator", pw, "SA"),
     )
     admin_id = db.execute("SELECT last_insert_rowid()").fetchone()[0]
     db.execute(
-        "INSERT INTO user_roles (user_id, role_key) VALUES (?, ?)",
+        "INSERT INTO user_roles (user_id, role_key) VALUES (%s, %s)",
         (admin_id, SUPER_ADMIN),
     )
@@ -58,10 +58,10 @@
     db.execute(
         "INSERT INTO users (username, email, full_name, password_hash, avatar_initials) "
-        "VALUES (?, ?, ?, ?, ?)",
+        "VALUES (%s, %s, %s, %s, %s)",
         ("compliance", "compliance@oneforall.local", "Compliance Manager", pw2, "CM"),
     )
     cm_id = db.execute("SELECT last_insert_rowid()").fetchone()[0]
     for role in [COMPLIANCE_MGR, AUDIT_LEAD]:
-        db.execute("INSERT INTO user_roles (user_id, role_key) VALUES (?, ?)", (cm_id, role))
+        db.execute("INSERT INTO user_roles (user_id, role_key) VALUES (%s, %s)", (cm_id, role))
 
     # DPO
@@ -69,9 +69,9 @@
     db.execute(
         "INSERT INTO users (username, email, full_name, password_hash, avatar_initials) "
-        "VALUES (?, ?, ?, ?, ?)",
+        "VALUES (%s, %s, %s, %s, %s)",
         ("dpo", "dpo@oneforall.local", "Data Protection Officer", pw3, "DP"),
     )
     dpo_id = db.execute("SELECT last_insert_rowid()").fetchone()[0]
-    db.execute("INSERT INTO user_roles (user_id, role_key) VALUES (?, ?)", (dpo_id, DPO))
+    db.execute("INSERT INTO user_roles (user_id, role_key) VALUES (%s, %s)", (dpo_id, DPO))
 
     # BCM Manager
@@ -79,9 +79,9 @@
     db.execute(
         "INSERT INTO users (username, email, full_name, password_hash, avatar_initials) "
-        "VALUES (?, ?, ?, ?, ?)",
+        "VALUES (%s, %s, %s, %s, %s)",
         ("bcm", "bcm@oneforall.local", "BCM Manager", pw4, "BM"),
     )
     bcm_id = db.execute("SELECT last_insert_rowid()").fetchone()[0]
-    db.execute("INSERT INTO user_roles (user_id, role_key) VALUES (?, ?)", (bcm_id, BCM_MANAGER))
+    db.execute("INSERT INTO user_roles (user_id, role_key) VALUES (%s, %s)", (bcm_id, BCM_MANAGER))
 
     db.commit()
@@ -98,5 +98,5 @@
                 "INSERT INTO frameworks "
                 "(name, description, color, relevant_modules, is_active) "
-                "VALUES (?, ?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                "VALUES (%s, %s, %s, %s, %s) ON CONFLICT DO NOTHING",
                 (name, desc, color, modules, 1),
             )
@@ -111,5 +111,5 @@
                 "INSERT INTO aria_frameworks "
                 "(name, description, color, relevant_modules, is_active) "
-                "VALUES (?, ?, ?, ?, ?) ON CONFLICT DO NOTHING",
+                "VALUES (%s, %s, %s, %s, %s) ON CONFLICT DO NOTHING",
                 (name, desc, color, modules, 1),
             )

DRY RUN — 38 files, 2056 replacements
